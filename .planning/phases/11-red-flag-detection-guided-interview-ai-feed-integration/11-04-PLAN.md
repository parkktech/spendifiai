---
phase: 11-red-flag-detection-guided-interview-ai-feed-integration
plan: 04
type: execute
wave: 3
depends_on: ['11-01', '11-02', '11-03']
files_modified:
  - app/Enums/QuestionType.php
  - database/migrations/2026_07_02_120000_create_interview_sessions_table.php
  - app/Models/InterviewSession.php
  - app/Policies/InterviewSessionPolicy.php
  - app/Services/InterviewOrchestratorService.php
  - app/Http/Controllers/Api/InterviewController.php
  - app/Http/Requests/AnswerOptimizationQuestionRequest.php
  - app/Listeners/SurfaceHighPriorityRedFlags.php
  - app/Listeners/UpdateOptimizationFromAnswer.php
  - app/Listeners/UpdateTransactionCategory.php
  - app/Http/Controllers/Api/AIQuestionController.php
  - app/Providers/AppServiceProvider.php
  - routes/api.php
  - tests/Feature/InterviewSessionStateTest.php
  - tests/Feature/InterviewOrchestratorServiceTest.php
  - tests/Feature/OptimizationFeedIntegrationTest.php
  - tests/Unit/QuestionTypeEnumTest.php
autonomous: true
requirements: [INT-01, INT-04, INT-05, INT-06, INT-07, FEED-01, FEED-02, FEED-03, FEED-04]

must_haves:
  truths:
    - "QuestionType::Optimization is added as an ADDITIVE enum case; a codebase grep proves no match() on question_type lacks a default, so no existing behavior breaks (FEED-01)"
    - "UpdateTransactionCategory is guarded at the top of handle(): optimization questions return early, so transaction-categorization behavior and all existing AIQuestion/subscription tests stay green (FEED-04)"
    - "SurfaceHighPriorityRedFlags creates standard AIQuestion rows (question_type=Optimization) for high-band findings so they appear in the existing /api/v1/questions feed (FEED-02)"
    - "UpdateOptimizationFromAnswer writes answers THROUGH to UserTaxFact (not just the snapshot) and updates the InterviewSession transcript (FEED-03)"
    - "InterviewOrchestratorService runs a persisted one-question-at-a-time state machine; it never asks anything answerable from snapshot+profile+durable facts (INT-03/CTX-04), caps the initial pass to a small number, and Claude is used ONLY to word each question"
    - "Deduction/eligibility probes are gated behind explicit prerequisite answers — e.g. the backdoor-Roth probe is blocked until the IRA-balance question is answered (INT-04)"
    - "A user can resume an in-progress session and re-answer a prior question; re-answer creates a new superseding UserTaxFact (INT-05)"
    - "Questions are batched by merchant pattern: one merchant group = one question, applied retroactively to all matched transactions via the finding's transaction_ids[] (INT-06)"
    - "Confidence bands drive response mode (INT-07): high-confidence PRE-FILLS a 'suggested — confirm' treatment excluded from user_assertions/estimated_value/pro_export_ready until confirmed; medium = one-tap multiple-choice; low/high-stakes = full module + pro routing; auto-classify-without-confirm is permitted ONLY for bookkeeping category, never tax-treatment fields"
  artifacts:
    - app/Services/InterviewOrchestratorService.php
    - app/Models/InterviewSession.php
    - app/Listeners/SurfaceHighPriorityRedFlags.php
    - app/Listeners/UpdateOptimizationFromAnswer.php
    - tests/Feature/OptimizationFeedIntegrationTest.php
  key_links:
    - "OptimizationProfileBuilt → SurfaceHighPriorityRedFlags → AIQuestion(Optimization) → /api/v1/questions feed → UI (11-05)"
    - "UserAnsweredQuestion → UpdateTransactionCategory (guarded, returns early) + UpdateOptimizationFromAnswer (writes UserTaxFact + session transcript)"
    - "InterviewOrchestrator skip-logic → IncomeOptimizationProfile::answerableFields() (with UserTaxFact proxy, 11-02)"
---

<objective>
Wire the guided interview and the AI-feed bridge onto the shipped hooks — the plumbing that turns findings into one-question-at-a-time educational prompts in the existing AI Questions feed, with zero regression to transaction categorization.

Implements the AI-feed bridge FEED-01..04 (additive QuestionType::Optimization; SurfaceHighPriorityRedFlags + UpdateOptimizationFromAnswer listeners; UpdateTransactionCategory guard — D7) and the interview engine INT-01/04/05/06/07 (persisted InterviewSession state machine; prerequisite gating; resume/re-answer; batch-by-merchant; confidence-band response modes — D5). CTX-04/INT-03 skip-logic is honored via answerableFields() from 11-02. Claude words questions only (via InterviewOrchestratorService, the second and last Claude call site alongside NarrationService).

This is the backend interview + feed plane; the interview UI surface (INT-02) ships in 11-05.

Purpose: Findings surface as resumable, skip-aware, band-driven educational questions in the existing feed without touching existing categorization behavior.
Output: enum case, interview_sessions table + model + policy, orchestrator + controller + routes, 3 listeners (1 new guard on an existing listener), AIQuestion cleanup guard, and regression + state-machine tests.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-CONTEXT.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-RESEARCH.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-PATTERNS.md
@app/Enums/QuestionType.php
@app/Listeners/UpdateTransactionCategory.php
@app/Http/Controllers/Api/AIQuestionController.php
@app/Models/AIQuestion.php
@app/Providers/AppServiceProvider.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuestionType::Optimization + AI-feed bridge listeners + UpdateTransactionCategory guard (FEED-01..04)</name>
  <files>app/Enums/QuestionType.php, app/Listeners/SurfaceHighPriorityRedFlags.php, app/Listeners/UpdateOptimizationFromAnswer.php, app/Listeners/UpdateTransactionCategory.php, app/Http/Controllers/Api/AIQuestionController.php, app/Providers/AppServiceProvider.php</files>
  <read_first>
    - 11-RESEARCH.md Pattern 6 (UpdateTransactionCategory guard), Pattern 7 (additive enum), Pitfall 1 (enum match regression — grep first), Pitfall 6 (AIQuestionController index cleanup scope for null-transaction optimization questions)
    - 11-CONTEXT.md D7 (feed bridge), D5 (question-engine rules)
    - app/Listeners/UpdateTransactionCategory.php (existing handle() — guard at top)
    - app/Http/Controllers/Api/AIQuestionController.php index() (whereHas('transaction') cleanup — scope to exclude optimization)
  </read_first>
  <behavior>
    - Adding QuestionType::Optimization does not break any match() (grep proves no defaultless match on question_type).
    - An optimization AIQuestion answered fires UpdateOptimizationFromAnswer (writes UserTaxFact) and UpdateTransactionCategory returns early (no subscription re-detection).
    - A high-band finding produces exactly one AIQuestion(Optimization) row per finding, visible in /api/v1/questions.
  </behavior>
  <action>
    Add case Optimization = 'optimization' to QuestionType (additive; no migration — question_type is VARCHAR). BEFORE relying on it, grep the codebase for match($...question_type) / match($...question_type->value) and add default handling anywhere a defaultless match exists (Pitfall 1; current grep shows none, re-verify). Guard UpdateTransactionCategory::handle() at the very top: if $event->question->question_type === QuestionType::Optimization return early (FEED-04, D7) — leave the existing subscription-detection body unchanged below the guard. Scope AIQuestionController::index() cleanup query to question_type != 'optimization' so null-transaction optimization questions are never auto-resolved (Pitfall 6); also ensure the existing ai:expire-questions path does not expire optimization questions (add a where clause or note it for the expire command — optimization questions persist until answered/dismissed or their InterviewSession completes).
    Create SurfaceHighPriorityRedFlags listener (ShouldQueue): on OptimizationProfileBuilt, query high-band OptimizationFindings for the user via scopeForUser and firstOrCreate a standard AIQuestion (question_type=Optimization, question text = the narrated description, options/ai_best_guess from the finding treatment for INT-07 suggested-confirm) idempotently (avoid duplicate feed rows on job retry). Create UpdateOptimizationFromAnswer listener (ShouldQueue): on UserAnsweredQuestion, if question_type===Optimization, write the answer THROUGH to UserTaxFact via recordFact() (source_type=interview_answer) and append to the InterviewSession transcript; it must NOT write estimated_value_cents or any dollar field. Register both listeners on their events in AppServiceProvider::boot() (Event::listen), alongside the existing block (do not remove existing registrations).
  </action>
  <verify>
    <automated>php artisan test --compact --filter="OptimizationFeedIntegrationTest|QuestionTypeEnumTest"</automated>
  </verify>
  <acceptance_criteria>
    - QuestionType::Optimization exists; no defaultless match() breaks; guard returns early for optimization answers; feed shows optimization questions; cleanup/expire never touches them.
    - All existing AIQuestion + subscription + categorization tests stay green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: interview_sessions table + InterviewSession + InterviewOrchestratorService (state machine, skip-logic, gating, batch, bands) + controller/routes (INT-01/04/05/06/07)</name>
  <files>database/migrations/2026_07_02_120000_create_interview_sessions_table.php, app/Models/InterviewSession.php, app/Policies/InterviewSessionPolicy.php, app/Services/InterviewOrchestratorService.php, app/Http/Controllers/Api/InterviewController.php, app/Http/Requests/AnswerOptimizationQuestionRequest.php, app/Providers/AppServiceProvider.php, routes/api.php</files>
  <read_first>
    - 11-CONTEXT.md D3 (interview_sessions schema: queue/asked as jsonb fact_keys, encrypted assertions transcript, partial unique one in-progress per user-year), D5 (question-engine rules incl. INT-07 reframe)
    - 11-RESEARCH.md Pattern 5 (state machine transitions), Pitfall 8 (multiple in-progress sessions → firstOrCreate + partial unique index), Open Question 2 (trigger on OptimizationProfileBuilt)
    - 11-PATTERNS.md InterviewOrchestratorService analog (TransactionCategorizerService Claude pattern), IncomeOptimizerController + AnswerQuestionRequest analogs, route/model-binding pattern
    - .planning/reference/transaction-detection-distilled.md §10 (question-engine rules); INT-04 canonical example (backdoor-Roth gated behind IRA-balance)
  </read_first>
  <behavior>
    - nextQuestion() pops a queued fact_key, skips it if answerableFields() (with UserTaxFact proxy) already answers it, else creates one AIQuestion(Optimization) and records it in asked[].
    - A prerequisite-gated probe (backdoor-Roth) is not queued until its prerequisite fact (IRA balance) is answered (INT-04).
    - Re-answering a prior asked key creates a new superseding UserTaxFact and re-queues if needed (INT-05).
    - One merchant pattern yields one question applied to all transaction_ids[] (INT-06).
    - High-confidence findings pre-fill 'suggested — confirm' (excluded from user_assertions/estimated_value/pro_export_ready until confirmed); band cutpoints come from config (INT-07).
  </behavior>
  <action>
    Create the interview_sessions migration (additive): user_id (FK cascadeOnDelete), tax_year, status (created|in_progress|paused|completed), queue (jsonb — ordered fact_keys), asked (jsonb — fact_keys asked), assertions (TEXT — encrypted, $hidden — ordered Q/A transcript for provenance), initial_cap (integer), timestamps. Add a Postgres partial unique index via DB::statement guaranteeing one in_progress session per (user_id, tax_year) WHERE status='in_progress'; drop by name in down(). Build InterviewSession model (encrypted assertions + $hidden, casts queue/asked => array, scopeForUser, User relationship) and InterviewSessionPolicy (owner-or-household pattern per 11-PATTERNS.md). Build InterviewOrchestratorService mirroring the TransactionCategorizerService Claude pattern for wording ONLY: startOrResume(userId, taxYear) uses firstOrCreate on the in_progress key (Pitfall 8) and seeds the queue from high-band findings + gated probes, capped to config('tax-detection.confidence') / a small initial cap (INT-03); nextQuestion(session) implements the Pattern-5 loop with answerableFields skip-logic (INT-03/CTX-04), prerequisite gating (INT-04), batch-by-merchant (INT-06 — one AIQuestion per merchant group, transaction_ids[] on the finding), and INT-07 band-driven response mode (suggested-confirm pre-fill from finding.treatment for high band; multiple-choice for medium; full-module + pro routing for low/high-stakes); Claude is called ONLY to phrase the question text (json_encode() the payload, exclude dollar figures — no estimated_value_cents to Claude). Transitions: created→in_progress→paused→in_progress (resume, INT-05)→completed. Create InterviewController (index/start/next/answer/resume) reusing AIQuestion feed patterns and AnswerOptimizationQuestionRequest (authorize via policy). Add routes under the authenticated api group (additive; e.g. /api/v1/optimizer/interview/*), Route::model binding for 'interview' => InterviewSession, and Gate::policy(InterviewSession::class, InterviewSessionPolicy::class) in AppServiceProvider. Trigger startOrResume from the OptimizationProfileBuilt flow (SurfaceHighPriorityRedFlags or a small orchestrator hook) so onboarding populates the queue (Open Question 2 recommendation — linear data flow).
  </action>
  <verify>
    <automated>php artisan test --compact --filter="InterviewSessionStateTest|InterviewOrchestratorServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - State machine transitions valid; skip-logic honors answerableFields; INT-04 gating blocks backdoor-Roth until IRA balance answered; resume + re-answer supersede; batch-by-merchant yields one question per group; INT-07 bands drive mode; Claude payload excludes dollar figures.
    - Partial unique index prevents a second in_progress session per user-year.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feed-integration regression + interview state + enum Pest coverage</name>
  <files>tests/Feature/OptimizationFeedIntegrationTest.php, tests/Feature/InterviewSessionStateTest.php, tests/Feature/InterviewOrchestratorServiceTest.php, tests/Unit/QuestionTypeEnumTest.php</files>
  <read_first>
    - 11-VALIDATION.md item 4 (feed integration regression), item 3 (state machine tests)
    - 11-RESEARCH.md "Phase Requirements → Test Map" (FEED/INT rows)
    - tests/Feature/CrossSourceReviewServiceTest.php (createUserWithBankAndProfile helper)
  </read_first>
  <action>
    QuestionTypeEnumTest: Optimization case exists with value 'optimization'; from() round-trips. OptimizationFeedIntegrationTest (zero-regression): guard_skips_optimization (answering an Optimization question does NOT trigger subscription detection / transaction categorization — assert the categorizer/detector is not invoked); creates_ai_question (high-band finding → one AIQuestion(Optimization) in the feed, idempotent on re-dispatch); answer_writes_tax_fact (answering writes a UserTaxFact via UpdateOptimizationFromAnswer, not just the snapshot); index_cleanup_excludes_optimization (a null-transaction optimization question is never auto-resolved by index()). InterviewSessionStateTest: creates_pending / one-in-progress-per-user-year (partial unique) / valid transitions / reanswer_supersedes (INT-05) / batches_by_merchant (INT-06 — 40 charges = one question, transaction_ids[] populated) / high_confidence_suggested_confirm (INT-07 pre-fill excluded from aggregation until confirmed). InterviewOrchestratorServiceTest: skips_known_facts (answerableFields skip) / prerequisite_gating (INT-04 backdoor-Roth blocked until IRA balance) / claude_wording_only (Http::fake payload excludes estimated_value_cents and dollar figures; Http::preventStrayRequests to non-Anthropic hosts). Run the FULL suite to prove all 349+ existing tests stay green (FEED-04 regression proof).
  </action>
  <verify>
    <automated>php artisan test --compact</automated>
  </verify>
  <acceptance_criteria>
    - All named tests pass; full suite shows zero NEW failures (known DashboardFinancialBlocksTest excepted).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → interview API | User answers cross into durable facts; validated via Form Request + policy |
| orchestrator → Claude (wording) | User-derived text crosses to the model |
| optimization question → categorization listener | Must be isolated so tax answers never mutate transactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04-01 | Tampering | enum case regression (UnhandledMatchError) | high | mitigate | Grep all match() on question_type; add default; QuestionTypeEnumTest + full-suite green |
| T-11-04-02 | Tampering | optimization answer mutates transactions | high | mitigate | FEED-04 guard returns early; regression test asserts categorizer not invoked |
| T-11-04-03 | Information Disclosure | cross-user interview/session access | high | mitigate | scopeForUser + InterviewSessionPolicy; cascadeOnDelete |
| T-11-04-04 | Tampering | prompt injection via answers/merchant names | high | mitigate | json_encode() payload; dollar figures excluded; Http::fake assertion |
| T-11-04-05 | Tampering | duplicate in-progress sessions / feed rows | medium | mitigate | firstOrCreate + partial unique index; idempotent SurfaceHighPriorityRedFlags |
| T-11-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages (RESEARCH audit empty) |
</threat_model>

<verification>
- `php artisan test --compact` full suite: zero NEW failures (the FEED-04 regression proof).
- `php artisan test --compact --filter="OptimizationFeedIntegrationTest|InterviewSessionStateTest|InterviewOrchestratorServiceTest|QuestionTypeEnumTest"` green.
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- Additive feed bridge with zero categorization regression; resumable one-question interview with skip-logic, prerequisite gating, batch-by-merchant, and band-driven modes; Claude words questions only, never sees numbers.
</success_criteria>

<output>
Create `.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-04-SUMMARY.md` when done.
</output>
