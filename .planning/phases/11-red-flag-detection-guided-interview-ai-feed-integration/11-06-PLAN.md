---
phase: 11-red-flag-detection-guided-interview-ai-feed-integration
plan: 06
type: execute
wave: 5
depends_on: ['11-01', '11-02', '11-03', '11-04']
files_modified:
  - app/Services/Detectors/FilingStatusDetector.php
  - app/Services/Detectors/WithholdingGapDetector.php
  - app/Services/Detectors/EmployerMatchGapDetector.php
  - app/Services/Detectors/DeductionProbeDetector.php
  - app/Services/Detectors/ComminglingMonitor.php
  - app/Services/Detectors/AuditRiskScorer.php
  - app/Services/Detectors/ProfileConformanceDetector.php
  - app/Services/RedFlagDetectorService.php
  - config/tax-detection.php
  - tests/Feature/CoreDetectorsTest.php
  - tests/Feature/ProfileConformanceDetectorTest.php
  - tests/Feature/AuditRiskScorerTest.php
autonomous: true
requirements: [FLAG-02, FLAG-03, FLAG-04, FLAG-05, FLAG-14, FLAG-15, FLAG-28]

must_haves:
  truths:
    - "Filing-status mismatch is SURFACED never ASSERTED (FLAG-02); FLAG-28 extends it with the paystub W-4/withholding evidence plane — 'your paystub appears to show X while your profile says Y' framing, never 'you should file as Z'"
    - "Withholding-gap detector flags an estimated gap greater than $500 using the config gap floor (FLAG-03); the gap magnitude comes from TaxRulesEngineService, never a detector inline"
    - "401(k) employer-match-gap detector flags unclaimed match from pay-stub/interview facts (FLAG-04)"
    - "Deduction probes (home office, vehicle, electronics, pet, meals) fire ONLY when their data prerequisites are verified from real user data (FLAG-05) and pass the config materiality gate"
    - "Commingling monitor flags personal-type spend in AccountPurpose::Business accounts with the LOCKED wording ('business owners commonly keep a separate account…') — warn-and-educate only, never 'you qualify as a business' (FLAG-14)"
    - "Audit-risk score feeds finding severity (FLAG-15) with LOCKED protective framing ('returns with patterns like [X] commonly receive additional IRS scrutiny — here is the documentation that typically resolves it') — never accusations, never a numeric audit probability"
    - "Profile-vs-reality conformance runs in BOTH directions across all three planes (filing status vs W-4; IRA/HSA facts vs transfers/payroll; checkbox facts vs transaction patterns) and every mismatch emits an OptimizationFinding + educational question; profile updates only via user confirmation (FLAG-28, D13)"
    - "No detector computes a dollar figure and none makes an HTTP call; all emit standard OptimizationFinding records through the 11-03 harness"
  artifacts:
    - app/Services/Detectors/ProfileConformanceDetector.php
    - app/Services/Detectors/AuditRiskScorer.php
    - tests/Feature/ProfileConformanceDetectorTest.php
  key_links:
    - "RedFlagDetectorService registry → these detector classes → OptimizationFinding upsert (11-03) → feed/interview (11-04)"
    - "AuditRiskScorer → finding severity (FLAG-06) → high-band surfacing priority"
---

<objective>
Load the core W-2-first red-flag detectors and the profile-vs-reality conformance layer onto the green 11a harness. These are the highest-value detectors for the W-2-first priority (D8): they read the snapshot, profile, durable facts, and paystub-derived facts — not merchant seed data (that is FLAG-10 in 11-07).

Implements FLAG-02 (filing-status mismatch, surfaced), FLAG-03 (withholding gap >$500), FLAG-04 (employer-match gap), FLAG-05 (deduction probes gated on verified data), FLAG-14 (commingling monitor, locked wording), FLAG-15 (audit-risk score → severity, locked protective framing), FLAG-28 (profile-vs-reality conformance in both directions, locked owner Decision 3 / D13). All content is educational; every reframe in D10/D13 is binding verbatim; nothing from the Blocked list appears.

Purpose: The money-finding core detectors for W-2 users plus the profile-conformance trust layer, all deterministic and no-Claude-in-numbers.
Output: 7 detector classes registered in RedFlagDetectorService, their rule entries in config/tax-detection.php, and per-detector tests.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-CONTEXT.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-PATTERNS.md
@.planning/reference/INTEGRATION-MAP.md
@.planning/reference/enhanced-profile-integration-notes.md
@app/Services/RedFlagDetectorService.php
@app/Services/SubscriptionDetectorService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filing-status, withholding-gap, and employer-match-gap detectors (FLAG-02, FLAG-03, FLAG-04)</name>
  <files>app/Services/Detectors/FilingStatusDetector.php, app/Services/Detectors/WithholdingGapDetector.php, app/Services/Detectors/EmployerMatchGapDetector.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php</files>
  <read_first>
    - INTEGRATION-MAP.md §5 (W2-1 match, W2-5 RSU under-withholding → FLAG-03) and §4 conformance row
    - .planning/reference/tax-strategy-playbook-distilled.md §2 (W-2 strategies) + tax-strategy-expansion-distilled.md M2 (benefit arbitrage question set — match content)
    - 11-CONTEXT.md D10 (reframes), D5 (question-engine rules); REQUIREMENTS.md FLAG-02/03/04
    - app/Services/RedFlagDetectorService.php (registry + registerFinding helper from 11-03)
  </read_first>
  <behavior>
    - FilingStatusDetector emits a SURFACED (never asserted) finding when stated filing status diverges from document/expected evidence.
    - WithholdingGapDetector emits a finding when the engine-computed withholding gap exceeds config('tax-detection...gap floor') (default $500).
    - EmployerMatchGapDetector emits a finding when pay-stub/interview facts show unclaimed employer match.
  </behavior>
  <action>
    Create three detector classes under app/Services/Detectors/ each exposing a detect(int $userId, int $taxYear, array $context): array method returning candidate findings for the RedFlagDetectorService harness (the harness applies validateRule/materiality/guards/severity — detectors never upsert or gate themselves). FilingStatusDetector (FLAG-02): compare UserFinancialProfile.tax_filing_status vs snapshot/document-derived expected status; emit a surfaced finding ("your documents may suggest a different filing status — a professional could confirm") — NEVER assert a status (the "asserting filing status" out-of-scope line is locked). WithholdingGapDetector (FLAG-03): call TaxRulesEngineService to compute the withholding gap; emit when it exceeds the config gap floor; the dollar magnitude is engine-only. EmployerMatchGapDetector (FLAG-04): read employer.match_pct / 401k contribution facts (UserTaxFact + snapshot); emit unclaimed-match finding with "if your plan allows" framing. Register the three class names in RedFlagDetectorService::detectorClasses(). Add any rule entries these detectors reference to config/tax-detection.php (reusing 11-01's schema); reference existing TAX-08 constants for numbers — do NOT add constants to config/tax-rules.php. Educational framing only; none of the SAFE-01 banned assertive phrases in question/description text.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=CoreDetectorsTest</automated>
  </verify>
  <acceptance_criteria>
    - Each detector emits the right finding on fixture data and stays silent otherwise; filing-status finding is surfaced never asserted; withholding magnitude traces to the engine; Http::preventStrayRequests passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deduction probes + commingling monitor + audit-risk scorer (FLAG-05, FLAG-14, FLAG-15)</name>
  <files>app/Services/Detectors/DeductionProbeDetector.php, app/Services/Detectors/ComminglingMonitor.php, app/Services/Detectors/AuditRiskScorer.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php, tests/Feature/AuditRiskScorerTest.php</files>
  <read_first>
    - INTEGRATION-MAP.md §8 (SH-1 home office, SH-2 vehicle, SH-3 §179 electronics, SH-4 meals → FLAG-05 content) and §4 (FLAG-14 commingling LOCKED wording; FLAG-15 audit-risk 9-factor inputs + LOCKED protective framing)
    - .planning/reference/transaction-detection-distilled.md §0.1 (materiality), §0.3 (method guards), §1.3 (audit-risk inputs); tax-strategy-playbook-distilled.md §9 (audit-risk 9-factor list)
    - 11-CONTEXT.md D2 (materiality), D4 (method guards run in the harness), D10 (FLAG-15 framing)
  </read_first>
  <behavior>
    - Each of the five deduction probes (home office, vehicle, electronics, pet, meals) fires ONLY when its prerequisite is verified (e.g. has_home_office fact true; a vehicle entity exists) AND the config materiality gate passes.
    - ComminglingMonitor emits when expense_type=personal spend sits in an AccountPurpose::Business account, using the locked wording.
    - AuditRiskScorer computes a deterministic score from the 9-factor inputs and raises the severity of related findings without ever stating a numeric audit probability.
  </behavior>
  <action>
    DeductionProbeDetector (FLAG-05): implement the five prerequisite-gated probes reading verified profile/entity/fact data — home office (has_home_office), vehicle (a vehicle TaxProfileEntity present), electronics (§179-eligible, business-purpose), pet, meals; each emits a question-only finding with a docs checklist, gated behind its prerequisite and the harness materiality gate; merchant-pattern enrichment for pet/vehicle lands in FLAG-10 (11-07) — note this in the class doc. ComminglingMonitor (FLAG-14): flag personal-type spend inside AccountPurpose::Business accounts and emit the LOCKED education copy verbatim ("business owners commonly keep a separate account for business activity; it is the single most effective record in a hobby-loss review") — warn-and-educate only, never "you qualify as a business". AuditRiskScorer (FLAG-15): compute a deterministic score from the 9-factor inputs (perpetual Sch C losses vs W-2, 100% business vehicle use, round-number patterns, deposit-vs-reported mismatch via CTX-03, outsized/non-cash>$5K-without-appraisal charitable, disproportionate HO+meals+travel, missing 1099s, mill-flag credits) and feed it into related findings' severity (FLAG-06); surface with the LOCKED protective framing ("returns with patterns like [X] commonly receive additional IRS scrutiny — here is the documentation that typically resolves it") — never accusations, never implying wrongdoing, never a numeric audit probability. Register all three in the RedFlagDetectorService registry. No detector computes a dollar amount or makes an HTTP call.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="CoreDetectorsTest|AuditRiskScorerTest"</automated>
  </verify>
  <acceptance_criteria>
    - Deduction probes stay silent without verified prerequisites and fire with them; commingling uses locked wording; audit-risk raises severity without a numeric probability; framing contains no banned assertive phrases.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Profile-vs-reality conformance detectors, both directions (FLAG-28, D13) + conformance tests</name>
  <files>app/Services/Detectors/ProfileConformanceDetector.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php, tests/Feature/CoreDetectorsTest.php, tests/Feature/ProfileConformanceDetectorTest.php</files>
  <read_first>
    - enhanced-profile-integration-notes.md Decision 3 (three conformance planes + both directions + user-confirm gate) — LOCKED
    - 11-CONTEXT.md D13 (FLAG-28 detail), INTEGRATION-MAP.md §4 conformance row
    - REQUIREMENTS.md FLAG-28
  </read_first>
  <behavior>
    - Plane 1: stated filing status vs paystub W-4/withholding evidence (extends FLAG-02).
    - Plane 2: stated IRA/HSA facts vs detected bank transfers/payroll deductions (profile says no HSA but paystub shows HSA deduction; profile says Roth-only but transactions show Traditional IRA contributions).
    - Plane 3: checkbox facts (rental property, childcare, student loans) vs transaction patterns (rent-income deposits, daycare merchants, loan-servicer payments) — BOTH "profile says X but no evidence" and "evidence says X but profile unchecked".
    - Every mismatch emits an OptimizationFinding + an educational question; a profile update only happens on user confirmation.
  </behavior>
  <action>
    Create ProfileConformanceDetector implementing all three planes in both directions (profile→evidence and evidence→profile). Each mismatch emits an OptimizationFinding plus an educational flow question worded exactly in the D13 pattern ("Your paystub appears to show X while your profile says Y — want to update your profile or tell us more?"); NEVER assert the correct value. Profile updates flow ONLY through the user-confirmation path (D3/D4 gate — the detector proposes, the user confirms via the interview/answer + Settings flow; the detector never writes to UserFinancialProfile directly). Register in the RedFlagDetectorService registry. Tests: CoreDetectorsTest (filing/withholding/match/deduction-probes/commingling behaviors). ProfileConformanceDetectorTest: each plane in both directions produces a finding + question; no plane asserts a value; no auto-write to the profile happens; banned assertive phrases absent from all conformance question/description text. Full suite green.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="CoreDetectorsTest|ProfileConformanceDetectorTest|AuditRiskScorerTest"</automated>
  </verify>
  <acceptance_criteria>
    - All three planes fire in both directions; every mismatch = finding + educational question; zero auto-writes to the profile; no banned assertive phrases; full suite zero NEW failures.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector → finding text | Educational framing boundary; a mis-framed finding could assert filing status or a deduction |
| detector → profile | Conformance detectors must never auto-write the profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06-01 | Tampering | filing-status assertion (out-of-scope) | high | mitigate | FLAG-02/28 surface never assert; banned-phrase test on conformance text |
| T-11-06-02 | Tampering | detector computes dollar amount | high | mitigate | All magnitudes via TaxRulesEngineService; SAFE-03 grep-gate (11-03) covers new files |
| T-11-06-03 | Repudiation | auto-write to profile from evidence | high | mitigate | FLAG-28 proposes only; user-confirm gate (D3/D4); test asserts no auto-write |
| T-11-06-04 | Information Disclosure | audit-risk as accusation/probability | medium | mitigate | Locked protective framing; no numeric probability; test asserts framing |
| T-11-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages (RESEARCH audit empty) |
</threat_model>

<verification>
- `php artisan test --compact --filter="CoreDetectorsTest|ProfileConformanceDetectorTest|AuditRiskScorerTest"` green.
- `php artisan test --compact` zero NEW failures; SAFE-03 grep-gate (from 11-03) still passes with the new detector files.
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- Seven core/conformance detectors emit correct educational findings on the harness; filing status surfaced not asserted; audit-risk feeds severity without probability; profile conformance both-directions with confirm-only updates.
</success_criteria>

<output>
Create `.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-06-SUMMARY.md` when done.
</output>
