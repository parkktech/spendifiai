---
phase: 11-red-flag-detection-guided-interview-ai-feed-integration
plan: 05
type: execute
wave: 4
depends_on: ['11-04']
files_modified:
  - resources/js/Pages/Questions/Index.tsx
  - resources/js/Components/SpendifiAI/InterviewCard.tsx
  - resources/js/Components/SpendifiAI/SuggestedConfirmCard.tsx
  - resources/js/Components/SpendifiAI/LearnedTaxFactsSection.tsx
  - resources/js/Pages/Settings/Index.tsx
  - resources/js/types/spendifiai.d.ts
autonomous: true
requirements: [INT-02]

must_haves:
  truths:
    - "Optimization questions render one high-value question at a time with skip, back, resume, and re-answer, reusing the existing Questions feed (INT-02) — no new route/page (the dedicated Optimize My Income page + nav are P12 UI-01/02)"
    - "SuggestedConfirmCard implements the INT-07 'suggested — confirm' flow: a pre-filled treatment shown with one-tap Confirm / Not quite (undo); until confirmed it is visibly non-committed and pro_export_ready stays false"
    - "An additive LearnedTaxFactsSection renders in Settings alongside EnhancedProfileSection showing confirmed + proposed durable facts with re-answer and proposal-confirmation, without changing EnhancedProfileSection behavior or any existing field's API shape (STORE-01 anchor, D3)"
    - "Every optimization surface uses educational modal framing (may/could/consider) and shows an inline disclaimer; no assertive language"
    - "sw-* design tokens only; Inter type stack, StatCard/card/badge patterns, recharts stack preserved — elevate, don't replace (D14, owner design intent)"
    - "Preservation audit is empty: no existing URL, primary nav label, form field name, heading/anchor, brand logo, or legal copy changed"
  artifacts:
    - resources/js/Components/SpendifiAI/SuggestedConfirmCard.tsx
    - resources/js/Components/SpendifiAI/LearnedTaxFactsSection.tsx
  key_links:
    - "/api/v1/optimizer/interview/* + /api/v1/questions (11-04) → Questions/Index.tsx routing by question_type==='optimization' → InterviewCard/SuggestedConfirmCard"
    - "durable-facts API (11-02/11-04) → LearnedTaxFactsSection in Settings/Index.tsx alongside EnhancedProfileSection"
---

<objective>
Ship the interview UI surface (INT-02) and the Settings learned-facts review hook — the user-facing half of the guided interview — by ELEVATING the existing Questions feed and Settings page, not replacing them. Optimization questions appear in the existing /questions feed routed by question_type; the Settings learned-facts list anchors the durable-facts store to the Enhanced Tax Profile (locked owner Decision 1 / D3).

Implements INT-02 (one-question-at-a-time with skip/back/resume/re-answer), the INT-07 suggested-confirm UI, and the STORE-01 Settings anchor UI.

D14 skill mandate (LOCKED owner Decision 6) — this plan touches interview UI, so BOTH design skills MUST be used, harmonized with the existing sw-* token system (elevate, don't replace). Decision 7's taste-skill v2 procedure is a set of BLOCKING self-audit gates written into the SUMMARY; any Fail blocks commit.

Purpose: A premium, educational, skip-aware one-question interview surface plus a transparent learned-facts review in Settings — additive, brand-preserving.
Output: InterviewCard + SuggestedConfirmCard + LearnedTaxFactsSection components, additive routing in Questions/Index.tsx, additive render in Settings/Index.tsx, type additions.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-CONTEXT.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-RESEARCH.md
@.planning/reference/enhanced-profile-integration-notes.md
@resources/js/Pages/Questions/Index.tsx
@resources/js/Components/SpendifiAI/QuestionCard.tsx
@resources/js/Pages/Settings/Index.tsx
@resources/js/Components/SpendifiAI/EnhancedProfileSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: InterviewCard + SuggestedConfirmCard + one-question routing in Questions/Index.tsx (INT-02, INT-07 UI)</name>
  <files>resources/js/Components/SpendifiAI/InterviewCard.tsx, resources/js/Components/SpendifiAI/SuggestedConfirmCard.tsx, resources/js/Pages/Questions/Index.tsx, resources/js/types/spendifiai.d.ts</files>
  <read_first>
    - 11-RESEARCH.md "Frontend Implementation Notes" (Interview UI surface; SuggestedConfirmCard spec; preservation checklist)
    - enhanced-profile-integration-notes.md Decision 6 (skill mandate + elevate-don't-replace) and Decision 7 (taste-skill v2 procedure — the 4-step per-task workflow + blocking audits)
    - 11-PATTERNS.md Questions/Index.tsx + QuestionCard + RecommendationCard analogs (useApi/useApiPost; sw-* tokens)
    - resources/js/Components/SpendifiAI/QuestionCard.tsx (existing card behavior to preserve for transaction questions)
  </read_first>
  <action>
    Before implementing, RUN the D14 design skills and record their outputs in the SUMMARY: invoke `/frontend-design:frontend-design` for the component direction, and query `python3 .claude/skills/ui-ux-pro-max/scripts/search.py "interview wizard one question at a time"` (and a second query for "confirm suggested action card") — apply their guidance only where it harmonizes with sw-* tokens/Inter/recharts. Apply soft-skill spacing/shadow/interaction-state standards and redesign-skill's audit-first approach to any touch of Questions/Index.tsx.
    Extend Questions/Index.tsx ADDITIVELY: route questions where question_type==='optimization' to a one-high-value-question-at-a-time flow (InterviewCard) with skip, back, resume, and re-answer controls hitting the 11-04 interview endpoints; transaction questions keep rendering identically through the existing QuestionCard (no change to their behavior). Build InterviewCard.tsx (single-question presentation, progress affordance, educational framing "may/could/consider" + inline disclaimer, skip/back/resume) and SuggestedConfirmCard.tsx (INT-07: renders the pre-suggested treatment as highlighted non-committed text using sw-accent/sw-accent-light, one-tap Confirm → sw-success and "Not quite"/undo; a band indicator via the existing Badge component; until confirmed, show the treatment as excluded from totals and pro_export_ready as blocked). Add the needed TypeScript types (OptimizationQuestion/InterviewSession/SuggestedTreatment) to spendifiai.d.ts additively; wrap decimal fields with Number() in arithmetic. Use sw-* tokens exclusively — no new palette. Do NOT add a new route, nav item, or page (UI-01/UI-02 are P12).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Taste-skill v2 blocking audits (Decision 7) recorded in SUMMARY: em-dash audit; Pre-Flight Check (Section 14); Preservation audit (list every URL/nav label/form field/anchor touched — must be empty); Brand fidelity audit (sw-* accent, Inter stack, logo untouched). Any Fail blocks the task.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` clean (TypeScript + Vite). Optimization questions render one at a time with skip/back/resume/re-answer; SuggestedConfirmCard confirm/undo works; transaction QuestionCard behavior unchanged.
    - All four Decision-7 blocking audits Pass and are recorded in the SUMMARY; both D14 skills named/used with outputs logged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LearnedTaxFactsSection additive in Settings (STORE-01 anchor UI, D3)</name>
  <files>resources/js/Components/SpendifiAI/LearnedTaxFactsSection.tsx, resources/js/Pages/Settings/Index.tsx, resources/js/types/spendifiai.d.ts</files>
  <read_first>
    - 11-RESEARCH.md "Settings Learned-Facts UI Hook" (additive section alongside EnhancedProfileSection; confirmed + proposed facts; per-field extraction confidence)
    - enhanced-profile-integration-notes.md Decision 1 (anchoring — additive UI section only), Decision 4 (proposals require confirmation)
    - resources/js/Pages/Settings/Index.tsx + EnhancedProfileSection.tsx (render location; do not alter existing behavior)
  </read_first>
  <action>
    Query `python3 .claude/skills/ui-ux-pro-max/scripts/search.py "review list with confirm and edit actions"` and apply `/frontend-design:frontend-design` direction (harmonized with sw-*). Build LearnedTaxFactsSection.tsx: a review list of the user's durable facts (confirmed + proposed) grouped/labeled by their non-PII label, with re-answer (creates a superseding fact via the interview/answer endpoint) and, for source_type=document_extraction proposals, a per-field extraction-confidence indicator + a one-tap Confirm that flips the proposal current (D4). Educational, non-assertive copy; sw-* tokens; existing Badge/Card primitives. Render it ADDITIVELY in Settings/Index.tsx immediately alongside the existing EnhancedProfileSection — no change to EnhancedProfileSection props/behavior, no change to any existing field's API shape, no route/nav/label rename (Settings→User/Family Profile reshape is P12). Add types additively.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Decision-7 blocking audits recorded in SUMMARY (em-dash, Pre-Flight §14, Preservation empty, Brand fidelity). Confirm EnhancedProfileSection renders and behaves identically.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` clean; LearnedTaxFactsSection renders alongside EnhancedProfileSection; proposals show confidence + confirm; confirmed facts show re-answer; no existing Settings behavior/label/field changed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → confirm/undo actions | UI confirmations flip proposal facts current; must call the confirm endpoint (server enforces the D4 gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05-01 | Repudiation | UI silently confirming a proposal | high | mitigate | Confirm is an explicit one-tap action calling the server confirm endpoint; server (11-02) is the authority on the D4 gate |
| T-11-05-02 | Information Disclosure | rendering encrypted fact internals | low | accept | Frontend only receives non-PII labels + decrypted display values via authorized API; $hidden protects raw columns |
| T-11-05-03 | Tampering | assertive/non-educational copy shipped | medium | mitigate | Educational framing + inline disclaimer required; Decision-7 framing audit in SUMMARY |
| T-11-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages; reuse existing React/Inertia/Tailwind stack (RESEARCH audit empty) |
</threat_model>

<verification>
- `npm run build` clean (TypeScript + production build).
- Decision-7 blocking self-audits (em-dash, Pre-Flight §14, Preservation empty, Brand fidelity) recorded and Passing in the SUMMARY for BOTH tasks.
- `php artisan test --compact` unaffected (no backend change) — zero NEW failures.
</verification>

<success_criteria>
- One-question interview surface + suggested-confirm + Settings learned-facts, all additive, brand-preserving, educational; both D14 skills used; all Decision-7 audits Pass.
</success_criteria>

<output>
Create `.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-05-SUMMARY.md` when done. Record the D14 skill outputs and the four Decision-7 blocking audits per task.
</output>
