---
phase: 11-red-flag-detection-guided-interview-ai-feed-integration
plan: 08
type: execute
wave: 7
depends_on: ['11-07']
files_modified:
  - app/Services/Detectors/SignalProbeMatrix.php
  - app/Services/Detectors/TimeCriticalAlarmDetector.php
  - app/Services/Detectors/W2BenefitArbitrageDetector.php
  - app/Services/Detectors/PublicSectorRetirementDetector.php
  - app/Services/Detectors/ReimbursementRoutingRule.php
  - app/Services/Detectors/IraToHsaQfdProbe.php
  - app/Services/Detectors/AcaCliffMonitor.php
  - app/Services/Detectors/RefundableCreditScanner.php
  - app/Services/RedFlagDetectorService.php
  - config/tax-detection.php
  - tests/Feature/SignalProbeMatrixTest.php
  - tests/Feature/W2BenefitDetectorsTest.php
  - tests/Feature/AcaCliffAndCreditsTest.php
autonomous: true
requirements: [FLAG-16, FLAG-17, FLAG-20, FLAG-21, FLAG-22, FLAG-23, FLAG-24, FLAG-25]

must_haves:
  truths:
    - "The signal→question→strategy matrix ships as prerequisite-gated income-signal probes (FLAG-05 pattern); entity-analysis content leads with the 60-month-lock warning BEFORE any classification content and tops out at 'commonly considered at this level' — the recommendation form is BLOCKED (FLAG-17, D10)"
    - "The income-drop Roth-conversion trigger keys off payroll/income signals ONLY, never asset values; 529/FEIE content is federal-parts-only; QBI above-threshold stays a professional-review sentinel with no implementation (D11); AMT/ISO ships only the safe one-liner (do not build); MFS ships only the 'may be worth modeling with your preparer' ceiling line"
    - "Time-critical alarms route at highest severity: 83(b) 30-day window, pre-2027 QOF mandatory recognition end-2026, QSBS early-eligibility at C-corp formation (paired with a §1244 note) — urgency stated as fact with consider-a-professional framing (FLAG-16)"
    - "W-2 benefit-arbitrage surfaces unelected-benefit gaps; ESPP education bans 'free money'/guaranteed-return language and models no disposition; NQDC routes to the education module with its employer-credit-risk warning; mega-backdoor is gated 'if your plan allows' (FLAG-20)"
    - "The public-sector 457(b)/403(b) branch ALWAYS surfaces the non-governmental-457(b) employer-creditor-risk caveat BEFORE any 457(b) content, asks the 3-year pre-retirement catch-up + pension-projection questions (answers persist as durable facts), limits from config (FLAG-21)"
    - "The IRA-to-HSA QFD probe fires only when all five prerequisite gates pass and its wording ALWAYS includes the testing-period caveat — 'it does not create an extra deduction, and testing-period rules apply' (FLAG-24)"
    - "The ACA cliff monitor computes cliff proximity purely in the rules engine against config FPL thresholds and surfaces AWARENESS never a computed subsidy/clawback amount; MAGI-management education is sequenced BEFORE any Traditional-vs-Roth narration for marketplace enrollees (FLAG-22)"
    - "The refundable-credit scanner surfaces 'may be eligible' never 'you qualify' from deterministic config math (EITC w/ investment-income limit, CTC/ACTC, dependent care, AOTC/LLC, Saver's Credit w/ Match date-gated to 2027, PTC, adoption, ABLE) (FLAG-23)"
    - "Reimbursement-beats-deduction reframes employee-type expenses for W-2 users as 'consider asking your employer about an accountable-plan reimbursement'; surviving above-the-line categories still route as deduction education (FLAG-25)"
  artifacts:
    - app/Services/Detectors/SignalProbeMatrix.php
    - app/Services/Detectors/AcaCliffMonitor.php
    - tests/Feature/SignalProbeMatrixTest.php
  key_links:
    - "SignalProbeMatrix / probes → OptimizationFinding + gated interview questions (INT-04) → feed (11-04)"
    - "AcaCliffMonitor → TaxRulesEngineService FPL math + MAGI-management sequencing before Trad-vs-Roth narration"
---

<objective>
Load the probe-matrix, W-2 benefit, alarm, ACA-cliff, and refundable-credit content — the "transaction hypothesis engine top categories" plus the W-2-first benefit plane (D8) that, for pure W-2 users, finds more money than transaction analysis. This is the last wave-11b content plan; it loads onto the fully green harness.

Implements FLAG-16 (time-critical alarms), FLAG-17 (signal→question→strategy probe matrix), FLAG-20 (W-2 benefit arbitrage), FLAG-21 (public-sector 457(b)/403(b) branch), FLAG-22 (ACA subsidy-cliff awareness), FLAG-23 (refundable-credit scanner), FLAG-24 (IRA→HSA QFD probe), FLAG-25 (reimbursement-beats-deduction routing). Verbatim question sets stay in the distillations. Every D10/D11 reframe and every Blocked-list exclusion is binding — the AMT/ISO safe one-liner only, the MFS ceiling line only, entity content 60-month-lock-first, income-drop trigger income-only, QBI sentinel retained.

Purpose: The W-2 benefit + probe-matrix + alarm + ACA + credit content that completes the Phase 11 detector surface — all deterministic, engine-computed numbers, educational framing.
Output: 8 detector/probe classes registered in the harness, their config rule entries, and tests asserting every reframe and gate.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-CONTEXT.md
@.planning/reference/INTEGRATION-MAP.md
@.planning/reference/tax-strategy-playbook-distilled.md
@.planning/reference/tax-strategy-expansion-distilled.md
@.planning/reference/tax-strategy-playbook-v2-delta-distilled.md
@app/Services/RedFlagDetectorService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Signal→question→strategy probe matrix + time-critical alarms (FLAG-17, FLAG-16)</name>
  <files>app/Services/Detectors/SignalProbeMatrix.php, app/Services/Detectors/TimeCriticalAlarmDetector.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php, tests/Feature/SignalProbeMatrixTest.php</files>
  <read_first>
    - tax-strategy-playbook-distilled.md §10 (the signal→question→strategy matrix — the FLAG-17 seed table) and §8/§8A (business + minutiae probes)
    - INTEGRATION-MAP.md §5–§11 FLAG-17 rows (every probe + its reframe: entity 60-month-lock-first ceiling; income-drop income-only; 529/FEIE federal-only; solo-401k employee-gate; QBI sentinel D11; AMT/ISO do-not-build one-liner; MFS ceiling line) and §7/§8/§11 FLAG-16 rows (83(b), QOF end-2026, QSBS+§1244)
    - 11-CONTEXT.md D10 (reframes binding), D11 (QBI sentinel), default rulings 2 (AMT), 10 (MFS)
    - Blocked section (INTEGRATION-MAP.md lines ~308-331) — nothing blocked may appear
  </read_first>
  <behavior>
    - Each probe fires only on its verified prerequisite signal (deferral gap, income-drop, OBBBA tips/OT/senior/auto-loan, solo-401k net>$10K with mandatory employee gate, SE health, dependent care, 529/Trump family, QCD 70½, cash-balance age>45+profit>$300K, §174A/§41 R&D with credit-mill warning, entity net>$50K, Augusta/hire-kids/accountable-plan/§105-HRA entity-gated, occupation-gated, NSO timing, loss probes).
    - TimeCriticalAlarmDetector emits highest-severity findings for 83(b) 30-day, QOF end-2026, QSBS-at-formation (+§1244), urgency stated as fact + consider-a-professional.
  </behavior>
  <action>
    Create SignalProbeMatrix implementing the PB-v1 §10 matrix as prerequisite-gated probes (each fires only on verified prerequisites per the FLAG-05 pattern). Bind every reframe verbatim: the entity-analysis probe (net>$50K) surfaces the 60-month-lock warning BEFORE any classification content and tops out at "commonly considered at this level" — NO recommendation form (blocked #16); the income-drop Roth trigger keys off payroll/income signals ONLY, never asset values (distinguishes it from blocked CR-2); 529 and FEIE surface federal parts ONLY (state → STATE-01); solo-401(k) requires the mandatory employee gate; §174A/§41 R&D carry the credit-mill audit warning; QBI above-threshold returns the professional-review sentinel with NO implementation (D11); the ISO/AMT path ships ONLY the safe one-liner ("ISO exercises may have AMT implications — consider a professional review before exercising") and does NOT build crossover modeling (default ruling 2); the NSO path states the mechanic with the directive stripped; loss probes (bad debt, worthless securities, Ponzi safe harbor, §1341) use config thresholds. All figures come from config (TAX-08)/engine; no probe computes dollars. Create TimeCriticalAlarmDetector (FLAG-16): 83(b) 30-day window on restricted-stock grant signals, pre-2027 QOF mandatory gain recognition at end-2026, QSBS early-eligibility at C-corp formation paired with a §1244 note — highest severity, urgency stated as fact with consider-a-professional framing. Register both in the harness registry; add referenced rule entries to config/tax-detection.php. Educational framing throughout; none of the SAFE-01 banned assertive phrases; nothing from the Blocked list.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=SignalProbeMatrixTest</automated>
  </verify>
  <acceptance_criteria>
    - Probes fire only on verified prerequisites; entity content leads with 60-month lock and never recommends; income-drop trigger uses income signals only; QBI returns the sentinel; AMT ships only the one-liner; alarms are highest severity with fact+professional framing; no blocked content present.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: W-2 benefit arbitrage + public-sector 457(b)/403(b) + reimbursement routing + IRA→HSA QFD (FLAG-20, FLAG-21, FLAG-25, FLAG-24)</name>
  <files>app/Services/Detectors/W2BenefitArbitrageDetector.php, app/Services/Detectors/PublicSectorRetirementDetector.php, app/Services/Detectors/ReimbursementRoutingRule.php, app/Services/Detectors/IraToHsaQfdProbe.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php, tests/Feature/W2BenefitDetectorsTest.php</files>
  <read_first>
    - tax-strategy-expansion-distilled.md M2 (W-2 benefit arbitrage question set), M3 (457(b)/403(b) block incl. Q5 pension projection), M5 (reimbursement-beats-deduction), M7 (IRA→HSA QFD 5-trigger gate + testing period)
    - INTEGRATION-MAP.md §6 rows (FLAG-20 13-item benefit list + group legal + Trump Account; FLAG-21 creditor-risk caveat MANDATORY; FLAG-25 survivor categories; FLAG-24 identical EXP canonical)
    - 11-CONTEXT.md D8 (W-2-first), D10 (ESPP/NQDC reframes), specifics (EXP M2/M3/M7 verbatim question sets)
  </read_first>
  <behavior>
    - W2BenefitArbitrageDetector surfaces unelected-benefit gaps (match, after-tax 401k + in-plan conversion mega-backdoor "if your plan allows", HSA/FSA/DCFSA, ESPP non-participation, NQDC existence, commuter, §127, group legal, employer Trump Account); ESPP uses participation-education only (no "free money"/guaranteed-return, no disposition modeling); NQDC routes to education with the employer-credit-risk warning.
    - PublicSectorRetirementDetector ALWAYS emits the non-governmental-457(b) creditor-risk caveat before any 457(b) content; asks the 3-year catch-up + pension-projection questions (answers persist as durable facts); limits from config.
    - ReimbursementRoutingRule reframes W-2 employee-type expenses as accountable-plan reimbursement asks; survivors (impairment-related, reservists, performing artists, fee-basis officials) still route as deduction education.
    - IraToHsaQfdProbe fires only when all five gates pass and always includes the testing-period caveat.
  </behavior>
  <action>
    Create W2BenefitArbitrageDetector (FLAG-20) surfacing the 13-item unelected-benefit gap list from interview answers and (when available) paystub/benefits facts; ESPP is participation-education only — ban "free money"/guaranteed-return language and model no disposition; NQDC routes to the education module with its mandatory employer-credit-risk warning; the mega-backdoor path is gated "if your plan allows"; 415(c) and other limits come from config. Create PublicSectorRetirementDetector (FLAG-21) employer-type-gated: ALWAYS surface the non-governmental-457(b) employer-creditor-risk caveat BEFORE any 457(b) content, ask the 3-year pre-retirement catch-up question and the pension-income-projection question (EXP M3 Q5 — the answer persists as a durable fact; the pension-projection DOCUMENT type is DOC-09 future, so ship the ASK only), separate $24,500 limits from config. Create ReimbursementRoutingRule (FLAG-25): reframe detected employee-type expenses for W-2 users as "consider asking your employer about an accountable-plan reimbursement" instead of deduction framing; the surviving above-the-line categories (impairment-related work expenses, reservists, performing artists, fee-basis officials) still route as deduction education; the Employer Reimbursement Request Packet is deferred (PKT-01 future). Create IraToHsaQfdProbe (FLAG-24) gated behind all five prerequisites (HSA eligibility, IRA balance, cash constraint, medical expenses or funding intent, QFD not previously used); its wording ALWAYS includes the testing-period caveat ("a one-time IRA-to-HSA transfer may be possible; it does not create an extra deduction, and testing-period rules apply"). Register all four in the harness registry; add referenced rule entries to config/tax-detection.php. No detector computes dollars; educational framing; no banned assertive phrases.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=W2BenefitDetectorsTest</automated>
  </verify>
  <acceptance_criteria>
    - Benefit-gap detector surfaces the list with ESPP/NQDC reframes; 457(b) creditor-risk caveat always precedes content; reimbursement routing reframes W-2 expenses with survivors still deducting; QFD fires only on all five gates with the testing-period caveat present.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ACA subsidy-cliff monitor + refundable-credit scanner (FLAG-22, FLAG-23) + full-suite gate</name>
  <files>app/Services/Detectors/AcaCliffMonitor.php, app/Services/Detectors/RefundableCreditScanner.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php, tests/Feature/AcaCliffAndCreditsTest.php</files>
  <read_first>
    - tax-strategy-playbook-v2-delta-distilled.md 2B.1 (ACA cliff numbers + MAGI management + sequencing) and 2B.6.6 (refundable-credit scanner 10-item list + EITC caveat + Saver's-Match 2027)
    - INTEGRATION-MAP.md §6 FLAG-22 row (awareness only, never a computed subsidy/clawback; MAGI-management before Trad-vs-Roth) and FLAG-23 row ("may be eligible" never "you qualify"; state credits → STATE-01)
    - 11-CONTEXT.md D10 (FLAG-22 sequencing); TAX-08 ACA FPL + credit constants (config)
  </read_first>
  <behavior>
    - AcaCliffMonitor detects marketplace-premium payments, computes cliff proximity in the rules engine against config FPL thresholds, and surfaces a mid-year awareness warning — never a computed subsidy or clawback amount; for marketplace enrollees, MAGI-management education is emitted BEFORE any Traditional-vs-Roth narration.
    - RefundableCreditScanner surfaces "may be eligible" findings from deterministic config math for the 10-item list; Saver's-Match content is date-gated to 2027; state credits are deferred.
  </behavior>
  <action>
    Create AcaCliffMonitor (FLAG-22): detect marketplace premium payments in bank data; compute cliff proximity purely in TaxRulesEngineService against config 400%-FPL thresholds; surface a mid-year educational awareness warning ("your income trend may be approaching the 400%-FPL threshold where premium credits end — a professional could review options such as pre-tax contributions") — the output NEVER presents a computed subsidy or clawback as the user's amount. Enforce sequencing: for marketplace enrollees, the MAGI-management education finding is ordered BEFORE any Traditional-vs-Roth narration (set a sequencing/severity signal the interview + report respect). Create RefundableCreditScanner (FLAG-23): surface "may be eligible" findings (never "you qualify") from deterministic config eligibility math for EITC (with the investment-income limit), CTC/ACTC, dependent care credit, AOTC/LLC, Saver's Credit (Saver's Match content date-gated to 2027 via TAX-09), Premium Tax Credit, adoption credit, and ABLE — state-level credits deferred to STATE-01. Register both in the harness registry; add referenced rule entries to config/tax-detection.php (Saver's-Match date-gate). Tests: AcaCliffAndCreditsTest (cliff awareness never emits a computed subsidy/clawback amount — assert the finding carries no user-specific subsidy dollar figure; MAGI-management sequenced before Trad-vs-Roth for a marketplace enrollee; refundable-credit findings use "may be eligible" phrasing and never "you qualify"; Saver's Match suppressed pre-2027). Run the FULL suite as the phase content gate.
  </action>
  <verify>
    <automated>php artisan test --compact</automated>
  </verify>
  <acceptance_criteria>
    - Cliff monitor surfaces awareness with no computed subsidy/clawback amount and sequences MAGI-management before Roth narration; credit scanner uses "may be eligible" phrasing; Saver's Match date-gated; full suite zero NEW failures.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector → finding text | Educational-framing boundary; blocked strategies (AMT modeling, entity recommendation, MFS optimizer, RIA items) must never surface |
| engine → ACA/credit numbers | FPL/credit math is engine-only; findings surface awareness/eligibility, never the user's computed subsidy/liability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08-01 | Tampering | blocked strategy surfaces (entity rec / AMT model / MFS optimizer / RIA) | high | mitigate | 60-month-lock-first + ceiling-line-only entity content; AMT one-liner only; MFS ceiling line only; income-drop income-only; blocked-list test |
| T-11-08-02 | Information Disclosure | ACA computed subsidy/clawback presented as the user's amount | high | mitigate | FLAG-22 awareness only; test asserts no user-specific subsidy dollar figure |
| T-11-08-03 | Tampering | credit scanner asserts eligibility | medium | mitigate | "may be eligible" never "you qualify"; Saver's Match date-gated (TAX-09); banned-phrase test |
| T-11-08-04 | Tampering | detector computes dollar amount | high | mitigate | All numbers via engine/config; SAFE-03 grep-gate covers new files |
| T-11-08-05 | Information Disclosure | 457(b) content without creditor-risk caveat | medium | mitigate | Caveat ALWAYS precedes 457(b) content; test asserts ordering |
| T-11-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages (RESEARCH audit empty) |
</threat_model>

<verification>
- `php artisan test --compact --filter="SignalProbeMatrixTest|W2BenefitDetectorsTest|AcaCliffAndCreditsTest"` green.
- `php artisan test --compact` full suite: zero NEW failures (known DashboardFinancialBlocksTest excepted); SAFE-03 grep-gate + banned-phrase tests green with all new detector files.
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- Probe matrix, W-2 benefit plane, time-critical alarms, ACA cliff awareness, and refundable-credit scanner all ship as gated, educational, engine-computed detectors with every D10/D11 reframe intact and nothing from the Blocked list present; the full Phase 11 detector surface is green.
</success_criteria>

<output>
Create `.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-08-SUMMARY.md` when done.
</output>
