---
phase: 11-red-flag-detection-guided-interview-ai-feed-integration
plan: 07
type: execute
wave: 6
depends_on: ['11-06']
files_modified:
  - database/migrations/2026_07_02_130000_create_detection_merchants_table.php
  - app/Models/DetectionMerchant.php
  - database/seeders/DetectionMerchantSeeder.php
  - app/Services/Detectors/CategoryLibraryDetector.php
  - app/Services/Detectors/DeductibleSaasSweep.php
  - app/Services/Sweeps/RecurringPayeeSweep.php
  - app/Services/Sweeps/PenaltyPreventionSweep.php
  - app/Services/Scanners/RetroactiveScanner.php
  - app/Services/Scanners/SafeHarborBenchmark.php
  - app/Services/Scanners/LifeEventTriggerDetector.php
  - app/Services/RedFlagDetectorService.php
  - config/tax-detection.php
  - routes/console.php
  - tests/Feature/CategoryLibraryDetectorTest.php
  - tests/Feature/SweepsAndScannersTest.php
  - tests/Feature/SafeHarborBenchmarkTest.php
autonomous: true
requirements: [FLAG-07, FLAG-10, FLAG-11, FLAG-12, FLAG-18, FLAG-26, FLAG-27]

must_haves:
  truths:
    - "A seeded detection-merchant knowledge table (aliases JSON, following the CancellationProviderSeeder precedent) powers the FLAG-10 category library (vehicle/powersports, solar/energy, pool/spa, landscaping, home-improvement, animals/security, medical HSA-first, travel-cluster, RV/boat, Masters/14-day, gambling signals)"
    - "Every gray-area module (sponsorship, guard dog, medical pool, grounds, fuel credit) surfaces ONLY as (1) a question, (2) a documentation checklist, (3) a static config-sourced defensibility rating, and (4) pro-review routing — never a deduction assertion (FLAG-10)"
    - "The monthly recurring-payee sweep and the continuous penalty sweep run as activity-gated scheduled tasks (28-day precedent) and emit standard OptimizationFinding records (FLAG-11, FLAG-26, D9)"
    - "Retroactive scanners run at onboarding over 12–36 months (config depth) alongside BuildIncomeOptimizationProfile: missed-credit (§25D config RANGE + uncertainty framing; §30D strictly date-gated past-window; §25C pre-2026), missed-deduction, basis reconstruction feeding STORE-02, method-election comparison, estimated-tax exposure (FLAG-12)"
    - "FLAG-18 outputs a 'safe-harbor benchmark' NEVER 'your estimated taxes': arithmetic uses ONLY user-supplied/vault-extracted prior-year liability (1040 line 22) + detected IRS payment outflows; detected business inflows are a surfacing trigger only, never a computation input"
    - "Penalty-prevention sweep covers the transaction-observable subset: excess IRA/Roth/HSA (headroom inverted), Roth income-limit breach awareness, HSA-near-Medicare 6-month lookback heuristic, 1099-K/deposit mismatch — warn-and-educate only (FLAG-26)"
    - "Life-event triggers fire from existing signals (payroll stops, new mortgage → basis-ledger start, escrow inflow → §121, marketplace premiums → FLAG-22) plus a small annual interview battery whose answers persist as durable facts (FLAG-27)"
    - "FLAG-07 deductible-SaaS sweep reuses existing Subscription records as pure educational surfacing; gambling losses are NEVER surfaced as fully deductible (suppress via TAX-09)"
  artifacts:
    - database/seeders/DetectionMerchantSeeder.php
    - app/Services/Scanners/SafeHarborBenchmark.php
    - app/Services/Sweeps/PenaltyPreventionSweep.php
  key_links:
    - "DetectionMerchant seed → CategoryLibraryDetector → OptimizationFinding (11-03) → interview batch-by-merchant (INT-06)"
    - "RetroactiveScanner basis reconstruction → TaxProfileEntity basis ledger (STORE-02, 11-02)"
    - "routes/console.php activity-gated schedules → RecurringPayeeSweep + PenaltyPreventionSweep → findings"
---

<objective>
Load the transaction-plane detector content: the seeded merchant category library, the monthly/continuous sweeps, the onboarding retroactive scanners, the safe-harbor benchmark, and the life-event triggers — the "onboarding wow" + "retention" items 1–2 of the value÷effort roadmap (D8).

Implements FLAG-10 (merchant category library + seed table), FLAG-07 (deductible-SaaS sweep, promoted), FLAG-11 (recurring-payee sweeps), FLAG-12 (retroactive scanners feeding STORE-02), FLAG-18 (safe-harbor benchmark, reframed per D10), FLAG-26 (penalty-prevention sweep), FLAG-27 (life-event triggers). Verbatim question trees / merchant lists stay in the distillations (reference, don't restate). Every reframe in D10/INTEGRATION-MAP is binding; nothing from the Blocked list appears; scheduled tasks are activity-gated (D9).

Purpose: The transaction-detection engine content and the scheduled/scanner operational surface, all deterministic, config-driven, and no-Claude-in-numbers.
Output: detection-merchant seed table + seeder, category-library + sweep + scanner detector classes registered in the harness, activity-gated schedules, and tests.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-CONTEXT.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-PATTERNS.md
@.planning/reference/INTEGRATION-MAP.md
@.planning/reference/transaction-detection-distilled.md
@.planning/reference/transaction-detection-v2-delta-distilled.md
@app/Services/RedFlagDetectorService.php
@app/Services/SubscriptionDetectorService.php
@routes/console.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detection-merchant seed table + category-library detector + deductible-SaaS sweep (FLAG-10, FLAG-07)</name>
  <files>database/migrations/2026_07_02_130000_create_detection_merchants_table.php, app/Models/DetectionMerchant.php, database/seeders/DetectionMerchantSeeder.php, app/Services/Detectors/CategoryLibraryDetector.php, app/Services/Detectors/DeductibleSaasSweep.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php</files>
  <read_first>
    - transaction-detection-distilled.md §§1–8 (vehicle 6-question tree + sponsorship; solar WHEN-first; pool HELOC-financing; landscaping; home-improvement one-tap; animals; medical HSA-first; travel cluster) and §9 (SaaS sweep) — the verbatim question trees + merchant seed lists; §12 output-contract example
    - transaction-detection-v2-delta-distilled.md §13 (gambling merchant signals to register only)
    - INTEGRATION-MAP.md §3 (all category detectors + gray-area constraint + reframes) and §11 (minutiae content mapping to FLAG-10)
    - 11-PATTERNS.md config/CancellationProviderSeeder precedent (aliases JSON seed table)
  </read_first>
  <behavior>
    - The seeded merchant table (aliases JSON) maps known merchants to categories (AutoZone/Summit/RevZilla; GoodLeap/Mosaic/Dividend/Sunlight/EnFin; Presidential/Shasta/Leslie's; SiteOne; Home Depot/Grainger/McMaster; Chewy/Petco; CareCredit; DraftKings/FanDuel/BetMGM).
    - CategoryLibraryDetector groups transactions by merchant pattern and emits ONE gray-area finding per pattern (question + doc checklist + static defensibility rating + pro routing), applied to all matched transaction_ids[] (INT-06).
    - Gambling merchants are registered as signals only; losses are never surfaced as fully deductible.
  </behavior>
  <action>
    Create the detection_merchants migration (additive) mirroring CancellationProvider: company_name, aliases (jsonb), category, subdetector_key, defensibility_rating (static), gray_area (boolean), notes — plus a nullable rule_id linking to the config rule registry. Create the DetectionMerchant model (scopeForUser not needed — reference data; add a normalized-match scope). Create DetectionMerchantSeeder loading the TD-v1 §§1–8 + TD-v2Δ §13 merchant lists (reference the distillations for the exact lists — do not invent merchants). IMPORTANT: seeding is additive reference data; the seeder is registered for local/CI + a one-time production run via `db:seed --class=DetectionMerchantSeeder` (NEVER migrate:fresh/refresh — CLAUDE.md). Create CategoryLibraryDetector implementing the category modules (vehicle incl. GVWR §179 branch + auto-loan-interest sub-detector; solar/energy; pool/spa; landscaping/hardscape; home-improvement one-tap destination; animals/security; medical HSA-first incl. diagnosed-condition cluster; travel-cluster correlator; RV/boat-as-second-home; Masters/14-day standalone) — each gray-area module surfaces ONLY as (1) question, (2) doc checklist (docs_missing), (3) static config-sourced defensibility rating, (4) pro-review routing; NEVER a deduction assertion. The fuel-credit module sets pro_export_ready=false until the gallons log is captured (docs_missing blocks it). All numbers come from config (TAX-08); no detector computes dollars. Create DeductibleSaasSweep (FLAG-07): reuse existing Subscription records to surface likely-deductible SaaS as pure educational findings. Register both detectors in the RedFlagDetectorService registry; add referenced rule entries to config/tax-detection.php (gambling-loss rule band=suppress so it never surfaces as fully deductible).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=CategoryLibraryDetectorTest</automated>
  </verify>
  <acceptance_criteria>
    - Seed table populates via db:seed (not migrate:fresh); category detector emits one gray-area finding per merchant pattern with question+checklist+defensibility+pro-routing; fuel-credit pro_export_ready stays false without gallons log; SaaS sweep surfaces from Subscription records; gambling never surfaced as deductible.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recurring-payee sweep + retroactive scanners + activity-gated schedules (FLAG-11, FLAG-12)</name>
  <files>app/Services/Sweeps/RecurringPayeeSweep.php, app/Services/Scanners/RetroactiveScanner.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php, routes/console.php, tests/Feature/SweepsAndScannersTest.php</files>
  <read_first>
    - transaction-detection-distilled.md §9 (recurring-payee sweep modules) and §11 (retroactive scanners: missed-credit §25D/§30D/§25C, missed-deduction, basis reconstruction, method-election, estimated-tax exposure)
    - INTEGRATION-MAP.md §3 FLAG-11 row (charitable appreciated-asset REFRAME, scholarship narrate-carefully, worker-classification warn-and-educate) and §4 FLAG-12 row (§25D config RANGE reframe, §30D date-gated)
    - 11-CONTEXT.md D9 (operational surfaces, activity gating), D10 (reframes)
    - routes/console.php (Schedule::call activity-gating precedent with config('spendifiai.sync.active_threshold_days', 28))
  </read_first>
  <behavior>
    - RecurringPayeeSweep reuses SubscriptionDetectorService grouping and routes recurring payments into modules (worker-classification, childcare→dependent-care, tuition/loans→AOTC/LLC/§127/scholarship, charitable→bunching/DAF, storage/coworking→allocation, insurance→SE-health/§105-HRA) — warn-and-educate/question-only where flagged.
    - RetroactiveScanner runs over config('tax-detection.onboarding_history_months') of history, feeding basis reconstruction into STORE-02 and emitting standard findings; §25D uses the config educational RANGE with uncertainty framing; §30D is a strictly date-gated past-window fact.
    - The monthly sweep is activity-gated (28-day precedent).
  </behavior>
  <action>
    Create RecurringPayeeSweep reusing SubscriptionDetectorService grouping; implement the FLAG-11 modules with the binding reframes: charitable appreciated-asset education is mechanics-only ("some donors give appreciated holdings…") with NO directive, and >$5K non-cash always pairs the qualified-appraisal checklist; scholarship-election carries the narrate-carefully flag; worker-classification is pure warn-and-educate (never "reclassify them"). Create RetroactiveScanner implementing the five §11 scanners: missed-credit (§25D 3-year amended-return recovery presented ONLY as the config-sourced educational RANGE with uncertainty framing — "recoveries have commonly ranged from $A to $B; a tax professional could evaluate whether an amended return applies"; §30D EV strictly date-gated past-window, never "currently available"; §25C pre-2026), missed-deduction (SE health, unclaimed home office, auto-loan interest 2025+), basis reconstruction (contractor/improvement payments → TaxProfileEntity basis ledger via STORE-02), method-election comparison (engine math), estimated-tax exposure (companion to FLAG-18). Wire the onboarding retroactive run to fire alongside BuildIncomeOptimizationProfile on OptimizationProfileBuilt (may require plaid:backfill — note the operational dependency), emitting standard OptimizationFinding records. Add the monthly RecurringPayeeSweep schedule to routes/console.php, activity-gated with config('spendifiai.sync.active_threshold_days', 28) exactly like the existing detect-subscriptions task. Register both services in the harness registry; add referenced rule entries to config/tax-detection.php (§25D/§30D/§25C with their expiration/date-gating already seeded in 11-01 — reuse them).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=SweepsAndScannersTest</automated>
  </verify>
  <acceptance_criteria>
    - Sweep routes recurring payees into the right modules with reframes intact; retroactive scanner feeds basis ledger + emits findings; §25D shows the config range never a promised amount; §30D never presented as available; schedule is activity-gated.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Safe-harbor benchmark + penalty-prevention sweep + life-event triggers (FLAG-18, FLAG-26, FLAG-27)</name>
  <files>app/Services/Scanners/SafeHarborBenchmark.php, app/Services/Sweeps/PenaltyPreventionSweep.php, app/Services/Scanners/LifeEventTriggerDetector.php, app/Services/RedFlagDetectorService.php, config/tax-detection.php, routes/console.php, tests/Feature/SafeHarborBenchmarkTest.php, tests/Feature/SweepsAndScannersTest.php</files>
  <read_first>
    - INTEGRATION-MAP.md §8 SH-6 row (FLAG-18 REFRAMED — benchmark, prior-year-liability arithmetic only, inflows are a trigger only) and §6 FLAG-26 row (penalty subset) and §6 FLAG-27 row (4 data-detectable triggers + annual battery)
    - transaction-detection-v2-delta-distilled.md §8 (penalty sweeps) and §7 (life events)
    - 11-CONTEXT.md D10 (FLAG-18 boundary wording approved as written), D3 (durable facts for the annual battery)
  </read_first>
  <behavior>
    - SafeHarborBenchmark outputs a "safe-harbor benchmark" using ONLY prior-year liability (1040 line 22, user-supplied or vault-extracted) + detected IRS payment outflows; detected business inflows are a surfacing trigger only, never a computation input; wording never says "your estimated taxes".
    - PenaltyPreventionSweep flags excess IRA/Roth/HSA (headroom inverted), Roth income-limit breach awareness, HSA-near-Medicare 6-month lookback heuristic, 1099-K/deposit mismatch — warn-and-educate only; runs activity-gated.
    - LifeEventTriggerDetector fires from payroll-stop / new-mortgage / escrow-inflow / marketplace-premium signals and persists annual-battery answers as durable facts.
  </behavior>
  <action>
    Create SafeHarborBenchmark (FLAG-18) emitting the reframed benchmark finding: "to meet the 100%/110% prior-year safe harbor, total payments of $X would be needed; detected so far: $Y; remaining gap: $Z — a penalty-avoidance benchmark, not your tax bill." All arithmetic ($X/$Y/$Z) comes from TaxRulesEngineService consuming ONLY the prior-year liability (user-supplied or vault-extracted 1040 line 22) and detected IRS payment outflows; detected business inflows may TRIGGER surfacing but are NEVER passed into the computation. Never call the output "your estimated taxes". Create PenaltyPreventionSweep (FLAG-26) over the transaction-observable subset (excess IRA/Roth/HSA via inverted headroom from TaxRulesEngineService; Roth income-limit breach recharacterization awareness; HSA-near-Medicare 6-month Part A lookback heuristic; 1099-K/deposit mismatch via CrossSourceReviewService) — warn-and-educate framing only; schedule it continuously/activity-gated in routes/console.php (28-day precedent). Create LifeEventTriggerDetector (FLAG-27) firing the 4 data-detectable triggers (payroll deposits stopping → low-income-year education window; new mortgage → basis-ledger start on the property entity; escrow/title inflow → §121 education; marketplace premiums → activates FLAG-22) reusing IncomeDetectorService/HousingDetectionService/SubscriptionDetectorService signals, plus the small annual interview battery (marriage/divorce, birth/adoption, job change, inheritance → step-up-documentation-now prompt, Medicare enrollment) whose answers persist as durable facts (UserTaxFact via the interview flow). Register all three in the harness registry. Tests: SafeHarborBenchmarkTest (benchmark wording; inflows never entered into computation — assert the engine call args exclude business inflows; prior-year-liability-only math). SweepsAndScannersTest additions (penalty subset behaviors; life-event triggers fire on fixture signals; annual-battery answers persist as durable facts). Full suite green.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="SafeHarborBenchmarkTest|SweepsAndScannersTest"</automated>
  </verify>
  <acceptance_criteria>
    - Benchmark uses prior-year-liability arithmetic only, never labels output "your estimated taxes", and business inflows never enter the computation; penalty sweep warns only and is activity-gated; life-event triggers fire and persist battery answers as durable facts.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detected inflows → FLAG-18 computation | Business inflows may only trigger surfacing, never feed the safe-harbor math (liability boundary) |
| dead/limited provisions → findings | §30D/§25D/gambling must be date-gated/range-framed/suppressed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | Tampering | FLAG-18 uses inflows in computation | high | mitigate | Engine call args exclude business inflows; test asserts prior-year-liability-only inputs |
| T-11-07-02 | Information Disclosure | dead/limited provision surfaced as live | high | mitigate | §30D date-gated past-window; §25D config RANGE + uncertainty; gambling band=suppress (TAX-09) |
| T-11-07-03 | Tampering | gray-area module asserts a deduction | high | mitigate | FLAG-10 modules are question+checklist+defensibility+pro-routing only; banned-phrase test |
| T-11-07-04 | Tampering | detector computes dollar amount | high | mitigate | All numbers via engine/config; SAFE-03 grep-gate covers new files |
| T-11-07-05 | Denial of Service | unbounded retroactive scan | medium | mitigate | Config-bounded history depth (12–36 months); activity-gated schedules (28-day) |
| T-11-07-SC | Tampering | seeder run destructively | high | mitigate | Additive db:seed only; NEVER migrate:fresh/refresh (CLAUDE.md); seeder is idempotent reference data |
</threat_model>

<verification>
- `php artisan test --compact --filter="CategoryLibraryDetectorTest|SweepsAndScannersTest|SafeHarborBenchmarkTest"` green.
- `php artisan test --compact` zero NEW failures; SAFE-03 grep-gate still green.
- `db:seed --class=DetectionMerchantSeeder` idempotent; NO migrate:fresh ever run.
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- Seeded merchant category library + gray-area modules, activity-gated sweeps, onboarding retroactive scanners feeding STORE-02, reframed safe-harbor benchmark, penalty sweep, and life-event triggers — all deterministic, config-driven, reframes intact.
</success_criteria>

<output>
Create `.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-07-SUMMARY.md` when done.
</output>
