---
phase: 11-red-flag-detection-guided-interview-ai-feed-integration
plan: 03
type: execute
wave: 2
depends_on: ['11-01', '11-02']
files_modified:
  - database/migrations/2026_07_02_110000_add_optimization_finding_columns.php
  - app/Models/OptimizationFinding.php
  - app/Services/RedFlagDetectorService.php
  - app/Services/NarrationService.php
  - app/Listeners/RunRedFlagDetectors.php
  - app/Listeners/NarrateOptimizationFindings.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/OptimizationFindingExtensionTest.php
  - tests/Feature/RedFlagDetectorServiceTest.php
  - tests/Feature/NarrationServiceTest.php
  - tests/Unit/EstimatedValueGuardTest.php
autonomous: true
requirements: [FLAG-01, FLAG-06, FLAG-09, FLAG-13]

must_haves:
  truths:
    - "RedFlagDetectorService::detectAll(userId, taxYear) runs deterministic detectors and upserts OptimizationFinding rows keyed on (user_id, tax_year, finding_key); NO Claude call and NO dollar arithmetic happens in the detector"
    - "estimated_value_cents is written ONLY by TaxRulesEngineService — a grep-gate Pest test fails if any file outside TaxRulesEngineService assigns it (SAFE-03 continuation)"
    - "Method-conflict guards run BEFORE emission (FLAG-09): a standard-mileage election suppresses vehicle actual-expense findings (offering the engine-computed annual comparison instead), a simplified home-office election suppresses actual-allocation prompts, §121 tracks recapture years, and an existing accountable plan routes reimbursables through the plan"
    - "Every finding carries a severity/priority derived deterministically from rule band (via bandToSeverity) so high-value items surface first (FLAG-06)"
    - "OptimizationFinding is extended additively (FLAG-13): transaction_ids, treatment, legal_basis (static config citation, never Claude), assumptions, band, user_assertions (encrypted TEXT + $hidden), docs_captured, docs_missing, estimated_value_cents, pro_export_ready, plus year-end forward-compat fields (deadline, lead_time_days, net_cash_cost, tax_saved, cliff_bonus_value, reversible)"
    - "NarrationService is a Claude call site that writes ONLY description; estimated_value_cents and all dollar figures are excluded from the Claude payload, and user-derived text is json_encode()'d never interpolated"
    - "legal_basis and assumptions are static config-sourced citations, never Claude output (kills citation hallucination)"
  artifacts:
    - app/Services/RedFlagDetectorService.php
    - app/Services/NarrationService.php
    - database/migrations/2026_07_02_110000_add_optimization_finding_columns.php
    - tests/Unit/EstimatedValueGuardTest.php
  key_links:
    - "OptimizationProfileBuilt event → RunRedFlagDetectors → RedFlagDetectorService::detectAll → OptimizationFinding upsert → NarrateOptimizationFindings (description) → SurfaceHighPriorityRedFlags (11-04)"
    - "config validateRule/bandToSeverity/passesMaterialityGate (11-01) + UserTaxFact method-election facts (11-02) → detector guards & gating"
---

<objective>
Build the detector engine harness the wave-11b content loads onto: the deterministic RedFlagDetectorService core (materiality gating + method-conflict guards + severity assignment, NO detector content yet), the additive OptimizationFinding output contract (FLAG-13), the isolated Claude NarrationService (description-only), and the event wiring off the shipped OptimizationProfileBuilt hook.

Implements FLAG-01 (deterministic detectors → OptimizationFinding, Claude writes description only), FLAG-06 (severity/priority model, D per bandToSeverity), FLAG-09 (method-conflict guards before emission, D4), FLAG-13 (additive OptimizationFinding extension, D6). Uses the config from 11-01 and the durable facts from 11-02.

Design decision (Claude's discretion per RESEARCH): single RedFlagDetectorService with a detector registry now; wave 11b extracts per-category Detector classes and appends them to the registry. This plan ships the core + guards + zero detector content beyond a trivial reference detector proving the pipeline.

Purpose: A tested, stable, no-Claude-in-numbers detection harness with the full output contract, so 11b is pure content loading.
Output: Finding migration + model extension, detector core, narration service, 2 listeners wired to OptimizationProfileBuilt, and the SAFE-03 grep-gate test.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-CONTEXT.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-RESEARCH.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-PATTERNS.md
@app/Models/OptimizationFinding.php
@app/Events/OptimizationProfileBuilt.php
@app/Services/SubscriptionDetectorService.php
@app/Services/AI/TransactionCategorizerService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OptimizationFinding additive migration + model extension (FLAG-13)</name>
  <files>database/migrations/2026_07_02_110000_add_optimization_finding_columns.php, app/Models/OptimizationFinding.php</files>
  <read_first>
    - 11-RESEARCH.md Pattern 3 (additive migration stub — columns only, no drops/type changes), Pitfall 2 (encrypted cast needs TEXT)
    - 11-CONTEXT.md D6 (exact column list), D12 (encryption/PII)
    - app/Models/OptimizationFinding.php (existing fillable/casts/scopeForUser; description null by design)
  </read_first>
  <behavior>
    - Migration adds all FLAG-13 columns nullable; existing rows keep description=null and function unchanged.
    - user_assertions is encrypted TEXT + $hidden; estimated_value_cents is a plain bigInteger written only by the engine.
  </behavior>
  <action>
    Create an additive migration (Schema::table on optimization_findings, ADD COLUMN only — no drop/type change, CLAUDE.md rule) adding: transaction_ids (jsonb nullable), treatment (text nullable), legal_basis (text nullable — STATIC config citation, never Claude output), assumptions (jsonb nullable — static citations), band (string 20 nullable), user_assertions (TEXT nullable — encrypted + $hidden), docs_captured (jsonb nullable), docs_missing (jsonb nullable), estimated_value_cents (bigInteger nullable — written ONLY by TaxRulesEngineService), pro_export_ready (boolean default false), and year-end forward-compat fields deadline (date nullable), lead_time_days (integer nullable), net_cash_cost (bigInteger nullable), tax_saved (bigInteger nullable), cliff_bonus_value (bigInteger nullable), reversible (boolean nullable). Extend the OptimizationFinding model additively: add the new columns to $fillable, add 'transaction_ids'/'assumptions'/'docs_captured'/'docs_missing' => 'array' and 'user_assertions' => 'encrypted' + 'pro_export_ready'/'reversible' => 'boolean' + 'deadline' => 'date' to casts(), and add 'user_assertions' to $hidden. Keep the existing 'details' => 'array' cast and scopeForUser() intact.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=OptimizationFindingExtensionTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate` additive; all FLAG-13 columns present; user_assertions encrypted + hidden; existing OptimizationFinding tests green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RedFlagDetectorService core + method-conflict guards + severity + detector registry (FLAG-01, FLAG-06, FLAG-09)</name>
  <files>app/Services/RedFlagDetectorService.php, app/Listeners/RunRedFlagDetectors.php, app/Providers/AppServiceProvider.php</files>
  <read_first>
    - 11-PATTERNS.md RedFlagDetectorService analog (SubscriptionDetectorService: 6-month lookback, groupBy normalization, guards, DB/Log imports)
    - 11-RESEARCH.md Pattern 2 (materiality gate consumption), Pattern 8 (event registration), Pitfall 5 (updateOrCreate year-scoping), D4 (method-conflict guards)
    - 11-CONTEXT.md D4 (the four guards), D5 (question-engine rules — legal_basis is static metadata)
    - app/Events/OptimizationProfileBuilt.php (event contract: userId, taxYear, findingCount)
  </read_first>
  <behavior>
    - detectAll(userId, taxYear) runs the (initially near-empty) detector registry, applies passesMaterialityGate + checkMethodConflictGuards before emitting, and upserts OptimizationFinding keyed on (user_id, tax_year, finding_key).
    - A standard-mileage method fact suppresses a vehicle actual-expense finding; a simplified-home-office fact suppresses an actual-allocation finding.
    - Every emitted finding has severity set via TaxRulesEngineService::bandToSeverity(rule band).
  </behavior>
  <action>
    Create RedFlagDetectorService mirroring SubscriptionDetectorService structure. Public detectAll(int $userId, int $taxYear): array orchestrates: (1) load the user snapshot + UserTaxFact method-election facts + answerableFields; (2) iterate a protected detectorClasses() registry array (empty/one reference detector now — 11b appends class names here); (3) for each candidate finding, call TaxRulesEngineService::validateRule(rule_id) and DROP it if suppressed; call passesMaterialityGate(...) for transaction-derived candidates; run checkMethodConflictGuards() (FLAG-09) BEFORE emission — standard-mileage fact suppresses vehicle actual-expense findings (and instead attaches the engine-computed annual method comparison), simplified-home-office fact suppresses actual-allocation prompts, §121 property entities track recapture years, an accountable-plan fact routes reimbursables through the plan; (4) upsert via OptimizationFinding::updateOrCreate(['user_id'=>$userId,'tax_year'=>$taxYear,'finding_key'=>...], [...]) passing ALL THREE key fields (Pitfall 5), setting severity from bandToSeverity, band, treatment, legal_basis + assumptions from STATIC config (never computed), docs_missing, and leaving description null (narration fills it). The service performs NO dollar arithmetic and makes NO HTTP call; any estimated_value_cents it needs comes from a TaxRulesEngineService call, and the assignment of estimated_value_cents lives in the engine, not here. Include a helper the 11b detectors call to register a finding so content plans never touch upsert/gating plumbing.
    Create RunRedFlagDetectors listener (implements ShouldQueue) that on OptimizationProfileBuilt calls detectAll($event->userId, $event->taxYear). Register it in AppServiceProvider::boot() via Event::listen(OptimizationProfileBuilt::class, RunRedFlagDetectors::class) alongside the existing event block.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=RedFlagDetectorServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - detectAll upserts findings with year-scoped keys, severity from band, suppressed rules dropped, method-conflict findings suppressed; zero HTTP calls (Http::preventStrayRequests in tests).
    - Listener fires on OptimizationProfileBuilt; existing event registrations unchanged.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NarrationService (Claude description-only) + narration listener + SAFE-03 grep-gate (FLAG-01, FLAG-13)</name>
  <files>app/Services/NarrationService.php, app/Listeners/NarrateOptimizationFindings.php, app/Providers/AppServiceProvider.php, tests/Feature/OptimizationFindingExtensionTest.php, tests/Feature/RedFlagDetectorServiceTest.php, tests/Feature/NarrationServiceTest.php, tests/Unit/EstimatedValueGuardTest.php</files>
  <read_first>
    - 11-RESEARCH.md Pattern 9 (NarrationService — description only; estimated_value_cents excluded), Pitfall 7 (Claude echoing numbers), Pitfall 3 (estimated_value_cents grep-gate)
    - 11-PATTERNS.md InterviewOrchestratorService analog (TransactionCategorizerService Claude Http::post pattern, config('services.anthropic.*'))
    - 11-CONTEXT.md D10 (liability reframes), REQUIREMENTS.md SAFE-01 (the banned-assertive-phrase list — reference by requirement, do not restate verbatim)
    - 11-VALIDATION.md items 6, 7 (no-Claude-in-numbers; educational framing)
  </read_first>
  <behavior>
    - narrateFinding() sends finding_type/severity/treatment/legal_basis to Claude and returns a 1-3 sentence "may/could/consider" description; estimated_value_cents and every dollar figure are absent from the payload.
    - The system prompt contains none of the SAFE-01 banned assertive phrases (asserted by test against the requirement list).
    - No file outside TaxRulesEngineService assigns estimated_value_cents.
  </behavior>
  <action>
    Create NarrationService using the TransactionCategorizerService Http::post pattern (same config('services.anthropic.model')/api_key). narrateFinding(OptimizationFinding $finding): string builds a system prompt hard-coding educational framing ("may/could/consider", no dollar amounts — use a placeholder, no deduction assertions, no IRS first-person) and a user payload built with json_encode() from finding_type, severity, treatment, legal_basis ONLY — estimated_value_cents and all money fields are deliberately excluded (Pitfall 7); user-derived strings (merchant names, entity labels) are only ever inside the json_encode()'d payload, never interpolated into the prompt. The service writes ONLY description via OptimizationFinding::update(['description' => ...]); it never touches estimated_value_cents/legal_basis/assumptions/band. Create NarrateOptimizationFindings listener (ShouldQueue) that on OptimizationProfileBuilt narrates findings whose description is null; register via Event::listen in AppServiceProvider. Do NOT let this listener run before RunRedFlagDetectors produces findings — order registration so detectors run first (or have the narrator query only existing null-description findings, which is order-independent — prefer this).
    Tests: OptimizationFindingExtensionTest (columns/casts/$hidden present). RedFlagDetectorServiceTest (detectAll behavior + guards + Http::preventStrayRequests). NarrationServiceTest: claude_never_receives_value_cents (assert the outbound payload — captured via Http::fake — excludes estimated_value_cents and any dollar figure), no_stray_requests to non-Anthropic hosts, banned_phrases_absent (assert the system prompt contains none of the SAFE-01 banned assertive phrases enumerated in REQUIREMENTS.md SAFE-01). EstimatedValueGuardTest (SAFE-03 grep-gate): scan all app/ PHP files and fail if any file other than app/Services/TaxRulesEngineService.php contains an assignment to estimated_value_cents (filter out comments with grep -v '^\s*//' style before counting; a bare == 0 on an unfiltered file is forbidden).
  </action>
  <verify>
    <automated>php artisan test --compact --filter="NarrationServiceTest|EstimatedValueGuardTest|OptimizationFindingExtensionTest"</automated>
  </verify>
  <acceptance_criteria>
    - NarrationService writes only description; Claude payload excludes all dollar figures; system prompt free of banned assertive phrases.
    - EstimatedValueGuardTest fails if estimated_value_cents is assigned outside TaxRulesEngineService.
    - Full suite: zero NEW failures.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector → Claude (narration) | Untrusted user text (merchant names/labels) crosses to the model; prompt-injection surface |
| engine → finding.estimated_value_cents | Only the engine may write dollar amounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-01 | Tampering | estimated_value_cents bypass | critical | mitigate | SAFE-03 grep-gate test; only engine has a write path; detector calls engine for values |
| T-11-03-02 | Tampering | prompt injection via merchant names | high | mitigate | json_encode() all user-derived data; never interpolate into system prompt (NarrationService) |
| T-11-03-03 | Information Disclosure | Claude echoes dollar amount as claim | high | mitigate | estimated_value_cents excluded from narration payload (Pitfall 7); Http::fake payload assertion |
| T-11-03-04 | Tampering | legal_basis/citation hallucination | medium | mitigate | legal_basis + assumptions are static config citations, never Claude output |
| T-11-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages (RESEARCH audit empty) |
</threat_model>

<verification>
- `php artisan test --compact --filter="RedFlagDetectorServiceTest|NarrationServiceTest|EstimatedValueGuardTest|OptimizationFindingExtensionTest"` green.
- `php artisan test --compact` zero NEW failures (baseline 349+; known DashboardFinancialBlocksTest failure excepted).
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- Deterministic detector harness with method guards + severity + full FLAG-13 contract; isolated Claude narration proven to never see dollar figures; SAFE-03 grep-gate active.
</success_criteria>

<output>
Create `.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-03-SUMMARY.md` when done.
</output>
