---
phase: 11-red-flag-detection-guided-interview-ai-feed-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/tax-rules.php
  - config/tax-detection.php
  - app/Services/TaxRulesEngineService.php
  - tests/Unit/TaxConfigExtensionTest.php
  - tests/Unit/DetectorRuleExpirationTest.php
autonomous: true
requirements: [TAX-08, TAX-09, FLAG-08]

must_haves:
  truths:
    - "Every v2.1 detector constant (OBBBA caps + sunsets, SALT, 415(c), 457(b)/403(b), shared Roth+Traditional IRA limit, mileage, charitable floors, §179, home-office, ACA FPL cliff, credit maxima, retro-window ranges, materiality gates, confidence cutpoints) is readable from config and verified against Rev. Proc. 2025-32 / Notice 2025-67"
    - "TaxRulesEngineService::validateRule() suppresses expired rules, rules with band suppress/hard_block, and flags stale last_verified — no expired-but-active rule can ever render a finding"
    - "Materiality gate thresholds ($100 / $500-yr / $1,000, address-match, loan-servicer) live ONLY in config, never as literals in service code"
    - "Sunset boundary behavior (tips/OT/senior/auto-loan 2028; SALT 2029; QOF end-2026; §25D 2025; §30D pre-Oct-2025) is covered by effective-date boundary tests"
  artifacts:
    - config/tax-detection.php
    - app/Services/TaxRulesEngineService.php
    - tests/Unit/DetectorRuleExpirationTest.php
  key_links:
    - "config/tax-detection.php rule registry → TaxRulesEngineService::validateRule() → every downstream detector (11-03, 11-06/07/08) gates on band before emission"
    - "config materiality block → RedFlagDetectorService::passesGate() (11-03) — the only source of the thresholds"
---

<objective>
Ship the versioned-rule + materiality-gate foundation the entire Phase 11 detection layer reads from. This is the load-bearing config layer for waves 11a and 11b: every constant, every rule's expiration/provenance metadata, and every materiality/confidence threshold lives here so no detector hardcodes IRS numbers or dates.

Implements TAX-08 (config expansion — every constant the v2.1 detectors/education modules read), TAX-09 (per-rule provenance/expiration schema + automatic suppression validator, adopted day one per D1), and FLAG-08 (config-driven materiality gates per D2). Retrofitting effective-dating later is expensive — it ships in wave 11a (D1).

Purpose: Deterministic, verifiable, config-only tax math and detector gating with zero Claude involvement in numbers.
Output: `config/tax-detection.php` (new), additive `config/tax-rules.php` detection block, `validateRule()` + materiality helper on `TaxRulesEngineService`, and boundary/expiration tests.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-CONTEXT.md
@.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-RESEARCH.md
@.planning/reference/INTEGRATION-MAP.md
@config/tax-rules.php
@config/spendifiai.php
@app/Services/TaxRulesEngineService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand config/tax-rules.php + create config/tax-detection.php with all constants (TAX-08)</name>
  <files>config/tax-rules.php, config/tax-detection.php</files>
  <read_first>
    - config/tax-rules.php (existing 2026 year-keyed structure — brackets, standard_deduction, limits; extend ADDITIVELY, never edit existing keys)
    - config/spendifiai.php (nested-config + confidence-threshold precedent to mirror)
    - .planning/reference/INTEGRATION-MAP.md "Hard Numbers → config/tax-rules.php" consolidated table (lines ~335-435) — THE authoritative constant list; supersedes per-doc tables
    - 11-RESEARCH.md "Config Extension Reference" (the config/tax-detection.php shape) and "Additional constants for config/tax-rules.php extension"
  </read_first>
  <action>
    Add an additive `'detection'` block under the existing `2026` key of config/tax-rules.php (do NOT touch existing keys — CLAUDE.md additive rule) carrying every constant from the INTEGRATION-MAP consolidated table: OBBBA items (tips $25K cap + $150K/$300K phaseouts + 2025-2028 sunset; overtime; senior $6K age-65 MAGI $75K/$150K; auto-loan interest $10K cap US-assembled 2025+), SALT $40K through 2029, 415(c) ~$72K, 457(b)/403(b) $24,500 each, the SHARED Roth+Traditional IRA limit ($7,500 + $1,100 catch-up 2026 — with the locked D2 correctness note in a comment that IRA-headroom math consumes COMBINED Roth+Traditional contributions), standard mileage 72.5¢, charitable non-itemizer $1,000/$2,000-MFJ + 0.5%-AGI itemizer floor + $250 acknowledgment + $5,000 non-cash appraisal, §179 $2,560,000/$4,090,000 + GVWR 6,000 lb + mid-quarter/listed-property flags, §195 $5,000, de minimis $2,500, home-office $5/sqft $1,500 cap, solo-401(k) 20% employer share, cash-balance $150K-$350K, CTC $2,200, adoption ~$17K, AOTC $2,500, 529 $20K K-12 / $35K→Roth, Trump Account $5,000/$2,500-employer-excluded/$1,000-seed, gift $19K / estate $15M-$30M, QSBS $15M/10x + $75M gross-asset + 3-4-5yr tiers, §1244 $50K/$100K-MFJ, §1341 $3,000, kiddie-tax under-24-student, QCD ~$108K age-70½, NIIT 3.8% $200K/$250K, medical 7.5% AGI floor + $50/night lodging, student-loan interest $2,500, educator $300, FEIE ~$130K, cruise-convention $2,000, §127 $5,250, employer child-care $500K/$600K, estimated-tax safe-harbor 100%/110%-over-$150K, ACA 400%-FPL cliff (~$62,600 single / ~$128,600 family-of-4), EITC investment-income limit, Saver's-Match 2027 arrival, §25D/§30D/§25C retro windows + the §25D $10K-$20K educational recovery RANGE, and §48E/MACRS/guard-dog-7yr depreciation facts. Add a top-of-block source comment citing Rev. Proc. 2025-32 / Notice 2025-67; tag any value the INTEGRATION-MAP marks "(verify)" with an inline `// [ASSUMED]` comment and list it in the SUMMARY for the P13 sign-off gate.
    Create config/tax-detection.php (new file, sibling per D2 — document the 10-03 precedent distinction: CrossSourceReviewService tolerances are class constants because they are business logic; materiality gates + band cutpoints live in config because the spec demands it) with four blocks: `materiality` (single_txn_auto_floor_cents 10_000, recurring_pattern_annual_cents 50_000, single_txn_interrogate_cents 100_000, address_match_always true, loan_servicer_always true), `confidence` (suggested_confirm_threshold, conditional_threshold, specialist_threshold — mirror config/spendifiai.php confidence pattern; these drive INT-07/FLAG-06), `facts` (reconfirm_months 12), `staleness_days` 90, `onboarding_history_months` 36, and `doc_request_labels` (label map feeding docs_missing).
    Money constants that will flow into estimated_value math stay as whole-dollar or explicit `_cents` integers as the INTEGRATION-MAP specifies; never introduce a Claude-writable path to any of them.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TaxConfigExtensionTest</automated>
  </verify>
  <acceptance_criteria>
    - config('tax-rules.2026.detection.*') and config('tax-detection.*') return the full constant set; all pre-existing config/tax-rules.php keys unchanged (diff is purely additive).
    - Every constant traces to the INTEGRATION-MAP consolidated table; "(verify)" values carry an [ASSUMED] marker and appear in the SUMMARY.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Seed the versioned rule registry + validateRule() suppression validator + config-driven materiality gate (TAX-09, FLAG-08)</name>
  <files>config/tax-detection.php, app/Services/TaxRulesEngineService.php</files>
  <read_first>
    - .planning/reference/transaction-detection-v2-delta-distilled.md §9 (CANONICAL rule schema — copy the field set verbatim into rule definitions)
    - 11-RESEARCH.md Pattern 1 (validateRule signature) and Pattern 2 (config-driven materiality gate)
    - 11-CONTEXT.md D1 (rule schema + sunset calendar to seed), D2 (materiality gates in config)
    - app/Services/TaxRulesEngineService.php (existing method style; add ONLY additive methods — do not alter existing signatures such as remainingIraRoomCents)
  </read_first>
  <action>
    In config/tax-detection.php add a `rules` registry: one associative entry per Phase 11 rule keyed by rule_id, each carrying the TD-v2Δ §9 canonical fields: rule_id, authority, effective_start, effective_end, phaseouts (MAGI-keyed by filing status), inflation_adjusted, source_url, last_verified, status (verified|needs_review|expired|expired_pending_extension), band (auto|conditional|specialist|suppress|hard_block), plus cap_cents where applicable. Seed the sunset calendar from D1: tips_deduction / ot_deduction / senior_deduction / auto_loan_interest (effective_end 2028-12-31); salt_deduction_cap (2029-12-31); qof_recognition (2026-12-31); residential_energy_credit_25d (status expired, effective_end 2025-12-31, amendment-scanner only); ev_credit_30d (status expired, effective_end 2025-09-30, past-window only); and the never-surface trio (residential solar 2026+ primary home, gambling-losses-as-fully-deductible) as band=suppress. This registry is the mechanism Phase 13's SAFE-06 hard-block list will plug into.
    Add additive methods to TaxRulesEngineService: `validateRule(string $ruleId): array` returning [suppressed, band, status, stale] — suppressed when effective_end has passed OR band ∈ {suppress, hard_block}; stale when now minus last_verified exceeds config('tax-detection.staleness_days'); throws on unknown rule_id. Add `passesMaterialityGate(int $amountCents, bool $isRecurring, int $annualTotalCents, bool $addressMatch = false, bool $loanServicer = false): bool` reading ALL thresholds from config('tax-detection.materiality') — never a literal in the method body (a Pest test greps this method's file region for the raw threshold digits and fails if present). Add `bandToSeverity(string $band): string` mapping the rule band to the OptimizationFinding severity vocabulary (consumed by FLAG-06 in 11-03). Keep TaxRulesEngineService the ONLY writer of dollar figures; validateRule/materiality/band helpers compute no estimated values.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=DetectorRuleExpirationTest</automated>
  </verify>
  <acceptance_criteria>
    - validateRule() returns suppressed=true for an expired rule (§25D, §30D), for band=suppress/hard_block, and stale=true when last_verified is older than staleness_days.
    - passesMaterialityGate() reads every threshold from config; grep of the method region finds no raw 10000/50000/100000 literal.
    - bandToSeverity() maps auto/conditional/specialist/suppress/hard_block deterministically.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Config + expiration Pest coverage (TAX-08, TAX-09, FLAG-08 boundaries)</name>
  <files>tests/Unit/TaxConfigExtensionTest.php, tests/Unit/DetectorRuleExpirationTest.php</files>
  <read_first>
    - tests/Unit/Services/TaxRulesEngineServiceTest.php (existing engine test style; Http::preventStrayRequests() guard pattern)
    - 11-RESEARCH.md "Phase Requirements → Test Map" (TAX-08/TAX-09/FLAG-08 rows)
    - 11-VALIDATION.md items 1, 2 (config/data-driven tests; materiality boundary tests)
  </read_first>
  <action>
    Create tests/Unit/TaxConfigExtensionTest.php asserting the full TAX-08 constant set is readable via config() with expected values (assert against config, never against restated literals in the test where avoidable — read the value and assert its shape/presence), and that existing config/tax-rules.php keys are untouched. Create tests/Unit/DetectorRuleExpirationTest.php covering: expired rule → suppressed=true; band=suppress/hard_block → suppressed=true; stale last_verified → stale=true; a currently-effective rule on a date inside its window → suppressed=false; and sunset boundary assertions (a rule evaluated one day after effective_end is suppressed; one day before is active) for tips/OT/senior/auto-loan (2028), SALT (2029), QOF (end-2026), §25D (2025), §30D (pre-Oct-2025). Add materiality boundary tests: a $99 single non-recurring txn does not pass the gate, $1,000 does, a $500/yr recurring pattern does, and address-matched / loan-servicer always pass. Wrap all tests with Http::preventStrayRequests() to prove zero outbound calls (no Claude in numbers). Every threshold assertion pulls from config.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="TaxConfigExtensionTest|DetectorRuleExpirationTest"</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass; boundary tests fail if any sunset date or materiality threshold is wrong.
    - Http::preventStrayRequests() proves the config/validator paths make zero network calls.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → detectors | Rule bands/effective dates gate what may ever be surfaced; a mis-dated or mis-banded rule could surface a dead/abusive provision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01-01 | Tampering | estimated-value path | high | mitigate | No estimated_value math in this plan; validateRule/materiality helpers compute no dollar figures; Http::preventStrayRequests() in tests |
| T-11-01-02 | Information Disclosure | dead/abusive provision surfacing | high | mitigate | band=suppress/hard_block + effective-dating suppression validator (TAX-09) blocks never-surface trio and expired credits before any finding renders |
| T-11-01-03 | Tampering | hardcoded thresholds drift | medium | mitigate | Materiality gates config-only; grep test forbids literals in service code |
| T-11-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages this plan (RESEARCH Package Legitimacy Audit: empty); no install tasks |
</threat_model>

<verification>
- `php artisan test --compact --filter="TaxConfigExtensionTest|DetectorRuleExpirationTest"` green.
- `php artisan test --compact` shows zero NEW failures vs baseline (known pre-existing DashboardFinancialBlocksTest excepted — not ours, do not fix).
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- TAX-08 constants fully present and additive; TAX-09 registry + validator suppress expired/suppress/hard_block rules and flag staleness; FLAG-08 materiality gate is config-only and boundary-tested.
</success_criteria>

<output>
Create `.planning/phases/11-red-flag-detection-guided-interview-ai-feed-integration/11-01-SUMMARY.md` when done. List every [ASSUMED] "(verify)" constant for the P13 sign-off gate.
</output>
