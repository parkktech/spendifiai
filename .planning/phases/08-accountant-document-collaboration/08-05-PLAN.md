---
phase: 08-accountant-document-collaboration
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - tests/Feature/AccountantAuthorizationTest.php
  - tests/Feature/AccountantFirmTest.php
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "Owner can access own documents -- test passes"
    - "Linked accountant can access client documents -- test passes"
    - "Unlinked accountant is blocked from accessing other clients' documents -- test passes"
    - "Annotations only visible to document owner and linked accountant"
    - "Document requests only creatable by linked accountant"
  artifacts:
    - path: "tests/Feature/AccountantAuthorizationTest.php"
      provides: "Cross-role authorization test suite"
      min_lines: 80
    - path: "tests/Feature/AccountantFirmTest.php"
      provides: "Firm registration and invite flow tests"
      min_lines: 40
  key_links:
    - from: "tests/Feature/AccountantAuthorizationTest.php"
      to: "app/Policies/TaxDocumentPolicy.php"
      via: "Testing view/annotate/requestDocument policy methods"
      pattern: "assertOk|assertForbidden"
---

<objective>
Create comprehensive cross-role authorization tests and firm registration tests verifying that document access, annotations, and requests are properly scoped.

Purpose: Satisfies TEST-04 requirement -- verifies owner access, linked accountant access, and unlinked accountant blocking across all new endpoints.
Output: 2 test files covering authorization boundaries and firm workflows.
</objective>

<execution_context>
@/home/jratz/.claude/get-shit-done/workflows/execute-plan.md
@/home/jratz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-accountant-document-collaboration/08-CONTEXT.md
@.planning/phases/08-accountant-document-collaboration/08-RESEARCH.md
@.planning/phases/08-accountant-document-collaboration/08-02-SUMMARY.md
@.planning/phases/08-accountant-document-collaboration/08-03-SUMMARY.md

@app/Policies/TaxDocumentPolicy.php
@app/Models/AccountantClient.php
@app/Models/AccountingFirm.php
@tests/Feature/TaxVaultStorageServiceTest.php
@tests/Feature/TaxDocumentExtractorServiceTest.php

<interfaces>
From TaxDocumentPolicy (extended in Plan 03):
- view(User, TaxDocument): owner or linked accountant
- annotate(User, TaxDocument): owner or linked accountant
- requestDocument(User): accountant type only

API routes (Plans 02 + 03):
- GET /api/v1/accountant/documents/{id}/annotations
- POST /api/v1/accountant/documents/{id}/annotations
- POST /api/v1/accountant/clients/{id}/requests
- GET /api/v1/document-requests (client)
- GET /api/v1/tax-vault/documents/{id}/annotations (client)
- POST /api/v1/accountant/firm (firm registration)
- GET /api/v1/accountant/dashboard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-role authorization tests</name>
  <files>tests/Feature/AccountantAuthorizationTest.php</files>
  <action>
    Create `tests/Feature/AccountantAuthorizationTest.php` using Pest PHP syntax (consistent with existing test files).

    Use the existing helper pattern from Phase 7 tests: create users directly with `User::factory()->create()` and TaxDocuments with direct `TaxDocument::create()` (no factory exists for TaxDocument -- use helper function like in existing tests).

    **Test cases (Pest `it()` syntax):**

    **Document access:**
    1. `it('allows owner to view own document')` -- Personal user creates doc, GET /api/v1/tax-vault/documents/{id} returns 200
    2. `it('allows linked accountant to view client document')` -- Accountant linked via AccountantClient (status active), GET endpoint returns 200
    3. `it('blocks unlinked accountant from viewing document')` -- Accountant NOT linked to doc owner, GET returns 403
    4. `it('blocks pending-status accountant from viewing document')` -- AccountantClient with status 'pending', GET returns 403

    **Annotation access:**
    5. `it('allows owner to list annotations on own document')` -- GET annotations endpoint returns 200
    6. `it('allows linked accountant to list annotations')` -- GET annotations via accountant route returns 200
    7. `it('allows owner to create annotation on own document')` -- POST with body text returns 201
    8. `it('allows linked accountant to create annotation')` -- POST via accountant route returns 201
    9. `it('blocks unlinked accountant from listing annotations')` -- GET returns 403
    10. `it('blocks unlinked accountant from creating annotation')` -- POST returns 403

    **Document requests:**
    11. `it('allows linked accountant to create document request')` -- POST to /api/v1/accountant/clients/{id}/requests returns 201
    12. `it('blocks unlinked accountant from creating document request')` -- POST returns 403
    13. `it('blocks personal user from creating document request')` -- POST returns 403 (not accountant type)
    14. `it('allows client to view their own document requests')` -- GET /api/v1/document-requests returns 200 with correct requests
    15. `it('client does not see other clients document requests')` -- GET returns empty for requests belonging to different client

    **Helper function** at top of file:
    ```php
    function createDocumentForUser(User $user): TaxDocument {
        return TaxDocument::create([
            'user_id' => $user->id,
            'original_name' => 'test-w2.pdf',
            'stored_path' => 'tax-vault/test/test-w2.pdf',
            'mime_type' => 'application/pdf',
            'file_size' => 1024,
            'tax_year' => 2025,
            'category' => \App\Enums\TaxDocumentCategory::W2,
            'status' => \App\Enums\DocumentStatus::Ready,
        ]);
    }
    ```

    Each test should:
    - Create users with appropriate user_type ('personal' or 'accountant')
    - Set up AccountantClient links where needed (status 'active')
    - Create AccountingFirm and set accounting_firm_id on accountant user where needed for request tests
    - Use `actingAs($user)` for authentication
    - Assert specific HTTP status codes (200, 201, 403)
  </action>
  <verify>
    <automated>cd /var/www/html/ledgeriq && php artisan test --filter=AccountantAuthorizationTest --compact 2>&1 | tail -15</automated>
  </verify>
  <done>All 15 cross-role authorization tests pass. Owner access verified, linked accountant access verified, unlinked accountant blocked verified.</done>
</task>

<task type="auto">
  <name>Task 2: Create firm registration and workflow tests</name>
  <files>tests/Feature/AccountantFirmTest.php</files>
  <action>
    Create `tests/Feature/AccountantFirmTest.php` using Pest PHP syntax.

    **Test cases:**

    **Firm registration:**
    1. `it('allows accountant to register a firm')` -- POST /api/v1/accountant/firm with name, address, phone returns 201 with firm data and auto-generated invite_token
    2. `it('blocks personal user from registering firm')` -- POST returns 403 or redirect (non-accountant)
    3. `it('validates firm name is required')` -- POST without name returns 422

    **Firm management:**
    4. `it('returns accountant firm details')` -- GET /api/v1/accountant/firm returns firm
    5. `it('updates firm details')` -- PATCH /api/v1/accountant/firm with new name returns updated firm
    6. `it('generates invite link')` -- GET /api/v1/accountant/firm/invite-link returns URL containing firm token

    **Dashboard:**
    7. `it('returns dashboard stats for accountant')` -- GET /api/v1/accountant/dashboard returns total_clients, documents_pending_review, open_requests, clients array
    8. `it('dashboard client list includes document counts')` -- Dashboard clients have document_count field

    **Invite flow:**
    9. `it('renders firm invite page for valid token')` -- GET /invite/{token} returns 200
    10. `it('returns 404 for invalid invite token')` -- GET /invite/invalid returns 404

    Each test creates appropriate users and firm data. Use `actingAs()` for authenticated requests.
  </action>
  <verify>
    <automated>cd /var/www/html/ledgeriq && php artisan test --filter=AccountantFirmTest --compact 2>&1 | tail -15</automated>
  </verify>
  <done>All firm registration and workflow tests pass. Firm CRUD, invite link generation, dashboard stats, and invite page rendering verified.</done>
</task>

</tasks>

<verification>
- `php artisan test --filter=AccountantAuthorizationTest` -- all 15 tests pass
- `php artisan test --filter=AccountantFirmTest` -- all 10 tests pass
- `php artisan test --compact` -- full suite passes (no regressions)
</verification>

<success_criteria>
25 new tests covering cross-role authorization boundaries and firm workflows. All pass. No regressions in existing test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/08-accountant-document-collaboration/08-05-SUMMARY.md`
</output>
