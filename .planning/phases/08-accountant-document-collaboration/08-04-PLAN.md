---
phase: 08-accountant-document-collaboration
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - resources/js/Pages/Accountant/Dashboard.tsx
  - resources/js/Pages/Auth/FirmInvite.tsx
  - resources/js/Components/SpendifiAI/AnnotationThread.tsx
  - resources/js/Components/SpendifiAI/DocumentRequestCard.tsx
  - resources/js/Pages/Vault/Show.tsx
  - resources/js/Pages/Vault/Index.tsx
  - routes/web.php
autonomous: true
requirements:
  - ACCT-08
  - UI-03
  - UI-04b

must_haves:
  truths:
    - "Accountant sees a dashboard with stats bar, client list, deadline tracker, and invite link generator"
    - "Annotations displayed as threaded comments in a Comments tab on document detail page"
    - "Client sees document request alerts in their vault view with upload prompts"
    - "Firm invite page shows firm branding and directs to registration"
  artifacts:
    - path: "resources/js/Pages/Accountant/Dashboard.tsx"
      provides: "Full accountant dashboard page"
      min_lines: 100
    - path: "resources/js/Components/SpendifiAI/AnnotationThread.tsx"
      provides: "Threaded annotation component with reply support"
      min_lines: 60
    - path: "resources/js/Components/SpendifiAI/DocumentRequestCard.tsx"
      provides: "Document request alert card with upload prompt"
      min_lines: 40
    - path: "resources/js/Pages/Auth/FirmInvite.tsx"
      provides: "Branded firm invite landing page"
      min_lines: 30
  key_links:
    - from: "resources/js/Pages/Accountant/Dashboard.tsx"
      to: "/api/v1/accountant/dashboard"
      via: "useApi hook fetching dashboard stats"
      pattern: "useApi.*accountant/dashboard"
    - from: "resources/js/Pages/Vault/Show.tsx"
      to: "resources/js/Components/SpendifiAI/AnnotationThread.tsx"
      via: "Comments tab rendering AnnotationThread"
      pattern: "AnnotationThread"
    - from: "resources/js/Pages/Vault/Index.tsx"
      to: "resources/js/Components/SpendifiAI/DocumentRequestCard.tsx"
      via: "Rendering document request alerts"
      pattern: "DocumentRequestCard"
---

<objective>
Build the Accountant Dashboard page, FirmInvite landing page, AnnotationThread and DocumentRequestCard shared components, and integrate them into existing Vault pages.

Purpose: Completes the frontend for accountant collaboration -- dashboard for accountants, annotations on documents, request alerts for clients.
Output: 2 new pages, 2 new shared components, 2 existing pages extended.
</objective>

<execution_context>
@/home/jratz/.claude/get-shit-done/workflows/execute-plan.md
@/home/jratz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-accountant-document-collaboration/08-CONTEXT.md
@.planning/phases/08-accountant-document-collaboration/08-RESEARCH.md
@.planning/phases/08-accountant-document-collaboration/08-02-SUMMARY.md
@.planning/phases/08-accountant-document-collaboration/08-03-SUMMARY.md

@resources/js/Pages/Accountant/Clients.tsx
@resources/js/Pages/Vault/Show.tsx
@resources/js/Pages/Vault/Index.tsx
@resources/js/Layouts/AppLayout.tsx
@resources/js/hooks/useApi.ts
@resources/js/hooks/useApiPost.ts
@resources/js/types/spendifiai.d.ts
@resources/js/Components/SpendifiAI/MissingAlertBanner.tsx
@resources/js/Components/SpendifiAI/Badge.tsx

<interfaces>
From resources/js/types/spendifiai.d.ts (Plan 01):
```typescript
interface AccountingFirm { id, name, address?, phone?, logo_url?, primary_color?, invite_token?, created_at }
interface DocumentAnnotation { id, tax_document_id, user_id, parent_id, body, author: {id, name, user_type}, replies: DocumentAnnotation[], created_at }
interface DocumentRequest { id, accounting_firm_id, client_id, accountant_id, description, tax_year?, category?, category_label?, status, fulfilled_document_id?, accountant_name, created_at }
```

API endpoints (Plan 02 + 03):
- GET /api/v1/accountant/dashboard -> { total_clients, documents_pending_review, open_requests, upcoming_deadlines, clients[], firm }
- GET /api/v1/accountant/firm/invite-link -> { url }
- GET /api/v1/accountant/documents/{id}/annotations -> DocumentAnnotation[]
- POST /api/v1/accountant/documents/{id}/annotations -> { body, parent_id? }
- GET /api/v1/document-requests -> DocumentRequest[] (client-facing)
- GET /api/v1/tax-vault/documents/{id}/annotations -> DocumentAnnotation[]
- POST /api/v1/tax-vault/documents/{id}/annotations -> { body, parent_id? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Accountant Dashboard and FirmInvite pages</name>
  <files>
    resources/js/Pages/Accountant/Dashboard.tsx,
    resources/js/Pages/Auth/FirmInvite.tsx,
    routes/web.php
  </files>
  <action>
    **Accountant/Dashboard.tsx** — New Inertia page replacing/extending Clients.tsx functionality:
    - Uses AppLayout (existing authenticated layout)
    - Fetches data via `useApi('/api/v1/accountant/dashboard')`
    - **Stats bar** at top: 4 stat cards in a responsive grid (lg:grid-cols-4, sm:grid-cols-2):
      - Total Clients (Users icon), Documents Pending Review (FileText icon), Open Requests (AlertCircle icon), Upcoming Deadline (Calendar icon -- show nearest deadline date)
    - **Client list table**: Columns: Name, Documents, Completeness (progress bar or percentage), Last Activity (relative time), Open Requests, Status badge. Click row to navigate to impersonation (`/api/v1/accountant/impersonate/{clientId}` then redirect to vault).
    - **Deadline tracker section**: Simple card listing upcoming tax deadlines from API data with date and label. Highlight deadlines within 30 days in yellow/amber.
    - **Invite link generator**: Card with firm name, a "Copy Invite Link" button that copies the firm invite URL to clipboard (use navigator.clipboard.writeText). Show success toast.
    - If no firm exists yet, show a "Register Your Firm" CTA that links to a firm registration form (inline modal or separate section with name, address, phone, logo_url, primary_color fields, POSTs to /api/v1/accountant/firm).
    - Use sw-* Tailwind tokens throughout (sw-card, sw-text, sw-accent, sw-border, sw-muted, sw-bg).
    - Responsive: single column on mobile, multi-column on desktop.

    **Auth/FirmInvite.tsx** — Public landing page (no auth required):
    - Receives Inertia props: `firm` (name, logo_url, primary_color), `token`
    - Shows firm name prominently, firm logo if provided
    - Uses firm's primary_color as accent (or default teal #0D9488)
    - CTA: "Join {firm.name} on SpendifiAI" button that navigates to /register?firm_token={token}
    - If user is already logged in, show "Link to {firm.name}" button that POSTs to an API endpoint to link the existing user
    - Simple, clean layout using GuestLayout or a minimal custom layout

    **routes/web.php** — Add Inertia route:
    ```php
    Route::get('/accountant/dashboard', fn () => Inertia::render('Accountant/Dashboard'))
        ->middleware(['auth', 'verified', 'accountant'])
        ->name('accountant.dashboard');
    ```
    (The /invite/{token} route was added in Plan 02.)
  </action>
  <verify>
    <automated>cd /var/www/html/ledgeriq && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard page renders stats bar, client list, deadline tracker, invite link generator. FirmInvite page shows firm branding with registration CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Create AnnotationThread and DocumentRequestCard, integrate into Vault pages</name>
  <files>
    resources/js/Components/SpendifiAI/AnnotationThread.tsx,
    resources/js/Components/SpendifiAI/DocumentRequestCard.tsx,
    resources/js/Pages/Vault/Show.tsx,
    resources/js/Pages/Vault/Index.tsx
  </files>
  <action>
    **AnnotationThread.tsx** (`resources/js/Components/SpendifiAI/AnnotationThread.tsx`):
    - Props: `documentId: number`, `annotations: DocumentAnnotation[]`, `onAnnotationAdded: () => void`
    - Renders top-level annotations (parent_id === null) as cards with author name, role badge (Accountant/Client using Badge component or sw-accent styling), timestamp (relative), and body text
    - Replies rendered indented below parent (max depth 2 -- replies to replies rendered flat under parent)
    - Each annotation has a "Reply" button that opens an inline text input
    - New annotation form at bottom: textarea + Submit button, POSTs via useApiPost to the appropriate annotations endpoint (detect from page context whether accountant or client route)
    - Use sw-card for annotation cards, sw-border for separators, sw-muted for timestamps
    - Empty state: "No comments yet. Start the conversation."

    **DocumentRequestCard.tsx** (`resources/js/Components/SpendifiAI/DocumentRequestCard.tsx`):
    - Props: `request: DocumentRequest`, `onUpload?: () => void`
    - Card showing: accountant name, description, tax year and category (if set), created date
    - Status badge: pending (yellow), uploaded (green), dismissed (gray)
    - If status is 'pending': show "Upload Document" button that triggers onUpload callback (parent handles actual upload flow)
    - Use AlertCircle icon for pending requests, CheckCircle for uploaded, XCircle for dismissed
    - Use sw-card styling with left border accent color

    **Vault/Show.tsx** — Extend the existing document detail page:
    - Add a "Comments" tab alongside existing tabs (Extraction, Audit Log)
    - In Comments tab: Fetch annotations via `useApi(`/api/v1/tax-vault/documents/${documentId}/annotations`)` (for client) or the accountant equivalent
    - Render AnnotationThread component with fetched annotations
    - Refresh annotations after new annotation is added

    **Vault/Index.tsx** — Extend the existing vault index page:
    - Fetch client's document requests via `useApi('/api/v1/document-requests')`
    - If pending requests exist, render DocumentRequestCard components above or alongside the MissingAlertBanner
    - DocumentRequestCard's onUpload navigates to upload flow (or scrolls to upload zone) with pre-filled tax year/category from the request
  </action>
  <verify>
    <automated>cd /var/www/html/ledgeriq && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>AnnotationThread renders threaded comments with reply support. DocumentRequestCard shows request alerts. Both integrated into Vault Show and Index pages.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds
- Dashboard page component exists at Pages/Accountant/Dashboard.tsx
- AnnotationThread and DocumentRequestCard exist in Components/SpendifiAI/
- Vault/Show.tsx has a Comments tab
- Vault/Index.tsx renders document request cards
</verification>

<success_criteria>
Accountant dashboard shows stats, clients, deadlines, and invite link. Document detail page has Comments tab with threaded annotations. Vault index shows document request alerts for clients. All TypeScript compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/08-accountant-document-collaboration/08-04-SUMMARY.md`
</output>
