---
phase: 08-accountant-document-collaboration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/Http/Controllers/Api/AccountantFirmController.php
  - app/Http/Requests/StoreFirmRequest.php
  - app/Http/Requests/UpdateFirmRequest.php
  - app/Mail/FirmInviteMail.php
  - app/Mail/DocumentRequestMail.php
  - app/Mail/AnnotationNotifyMail.php
  - app/Mail/DocumentUploadedMail.php
  - app/Mail/RequestFulfilledMail.php
  - resources/views/emails/firm-invite.blade.php
  - resources/views/emails/document-request.blade.php
  - resources/views/emails/annotation-notify.blade.php
  - resources/views/emails/document-uploaded.blade.php
  - resources/views/emails/request-fulfilled.blade.php
  - routes/api.php
  - routes/web.php
  - config/spendifiai.php
autonomous: true
requirements:
  - ACCT-03
  - ACCT-09

must_haves:
  truths:
    - "Accountant can register a firm with name, address, phone, and branding"
    - "Firm generates a branded invite link with unique token"
    - "Client can visit /invite/{token} and see firm branding"
    - "5 Mail classes exist for all accountant notification workflows"
  artifacts:
    - path: "app/Http/Controllers/Api/AccountantFirmController.php"
      provides: "Firm CRUD, invite link generation, dashboard stats"
      contains: "class AccountantFirmController"
    - path: "app/Mail/FirmInviteMail.php"
      provides: "Branded firm invite email"
      contains: "class FirmInviteMail"
    - path: "app/Mail/DocumentRequestMail.php"
      provides: "Missing document request notification"
      contains: "class DocumentRequestMail"
    - path: "app/Mail/AnnotationNotifyMail.php"
      provides: "Annotation notification to other party"
      contains: "class AnnotationNotifyMail"
    - path: "app/Mail/DocumentUploadedMail.php"
      provides: "Client upload notification to accountant"
      contains: "class DocumentUploadedMail"
    - path: "app/Mail/RequestFulfilledMail.php"
      provides: "Request fulfilled notification to accountant"
      contains: "class RequestFulfilledMail"
  key_links:
    - from: "routes/api.php"
      to: "app/Http/Controllers/Api/AccountantFirmController.php"
      via: "accountant-only route group"
      pattern: "AccountantFirmController"
    - from: "routes/web.php"
      to: "app/Models/AccountingFirm.php"
      via: "public /invite/{token} route"
      pattern: "invite.*token"
---

<objective>
Create the AccountantFirmController for firm registration/management, the branded invite link flow (public web route), and all 5 Mail classes with Blade templates.

Purpose: Enables accountant firm creation, client onboarding via branded links, and email notifications for all collaboration workflows.
Output: 1 controller, 2 form requests, 5 Mail classes, 5 Blade email templates, updated routes.
</objective>

<execution_context>
@/home/jratz/.claude/get-shit-done/workflows/execute-plan.md
@/home/jratz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-accountant-document-collaboration/08-CONTEXT.md
@.planning/phases/08-accountant-document-collaboration/08-RESEARCH.md
@.planning/phases/08-accountant-document-collaboration/08-01-SUMMARY.md

@app/Http/Controllers/Api/AccountantController.php
@app/Mail/AccountantInviteMail.php
@app/Http/Middleware/EnsureAccountant.php
@routes/api.php
@routes/web.php

<interfaces>
From app/Models/AccountingFirm.php (created in Plan 01):
- AccountingFirm model with: name, address, phone, logo_url, primary_color, invite_token
- members() hasMany User
- invite_token auto-generated via Str::random(64)

From app/Models/DocumentRequest.php (created in Plan 01):
- DocumentRequest model with: accounting_firm_id, client_id, accountant_id, description, tax_year, category, status, fulfilled_document_id
- firm(), client(), accountant(), fulfilledDocument() relationships
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AccountantFirmController with firm CRUD and invite link routes</name>
  <files>
    app/Http/Controllers/Api/AccountantFirmController.php,
    app/Http/Requests/StoreFirmRequest.php,
    app/Http/Requests/UpdateFirmRequest.php,
    routes/api.php,
    routes/web.php,
    config/spendifiai.php
  </files>
  <action>
    **StoreFirmRequest** (`app/Http/Requests/StoreFirmRequest.php`):
    - Rules: name (required, string, max:255), address (nullable, string), phone (nullable, string, max:20), logo_url (nullable, url, max:500), primary_color (nullable, string, regex for hex color)
    - authorize(): return user is accountant type

    **UpdateFirmRequest** (`app/Http/Requests/UpdateFirmRequest.php`):
    - Same rules as StoreFirmRequest but all fields nullable (including name)
    - authorize(): return user is accountant and belongs to firm

    **AccountantFirmController** (`app/Http/Controllers/Api/AccountantFirmController.php`):
    - `store(StoreFirmRequest)`: Create AccountingFirm, set user's accounting_firm_id, return firm JSON (201)
    - `show(Request)`: Return user's firm (via $request->user()->accountingFirm) or 404
    - `update(UpdateFirmRequest)`: Update firm fields, return updated firm
    - `inviteLink(Request)`: GET returns branded invite URL. POST regenerates token with new Str::random(64)
    - `dashboard(Request)`: Return dashboard stats JSON with total_clients, documents_pending_review, open_requests, upcoming_deadlines (from config), clients array with document counts and completeness, firm object

    **Routes** -- Add to accountant-only group in api.php:
    - POST /firm, GET /firm, PATCH /firm, GET /firm/invite-link, POST /firm/invite-link/regenerate, GET /dashboard

    **Public invite route** -- Add to web.php:
    - GET /invite/{token} renders Auth/FirmInvite Inertia page with firm branding props

    **Config** -- Add tax_deadlines array to config/spendifiai.php with standard US filing dates (April 15 individual, March 15 corporate/partnership/S-corp)
  </action>
  <verify>
    <automated>cd /var/www/html/ledgeriq && php artisan route:list --path=firm 2>&1 | head -10</automated>
  </verify>
  <done>AccountantFirmController registered with 6 API routes and 1 public web route. Dashboard endpoint returns stats JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 Mail classes with Blade email templates</name>
  <files>
    app/Mail/FirmInviteMail.php,
    app/Mail/DocumentRequestMail.php,
    app/Mail/AnnotationNotifyMail.php,
    app/Mail/DocumentUploadedMail.php,
    app/Mail/RequestFulfilledMail.php,
    resources/views/emails/firm-invite.blade.php,
    resources/views/emails/document-request.blade.php,
    resources/views/emails/annotation-notify.blade.php,
    resources/views/emails/document-uploaded.blade.php,
    resources/views/emails/request-fulfilled.blade.php
  </files>
  <action>
    Follow the existing `AccountantInviteMail` pattern. All 5 use `Queueable, SerializesModels` traits.

    **FirmInviteMail** -- Constructor: AccountingFirm $firm, string $inviteUrl. Subject: "{firm name} invites you to SpendifiAI". Template shows firm name, logo (if set), and invite link button.

    **DocumentRequestMail** -- Constructor: DocumentRequest $request, User $accountant. Subject: "Your accountant requested a document". Template shows accountant name, request description, tax year/category if set, and link to vault.

    **AnnotationNotifyMail** -- Constructor: DocumentAnnotation $annotation, User $recipient. Subject: "New comment on {document original_name}". Template shows author name, annotation body preview (first 200 chars), and link to document detail page.

    **DocumentUploadedMail** -- Constructor: TaxDocument $document, User $client. Subject: "{client name} uploaded a document". Template shows client name, document name, category, and link to client's documents.

    **RequestFulfilledMail** -- Constructor: DocumentRequest $request, TaxDocument $document. Subject: "Document request fulfilled by {client name}". Template shows request description, uploaded document name, and link to document.

    **Blade templates** -- All in `resources/views/emails/`. Simple HTML email layout consistent with existing email templates. Use firm primary_color for FirmInviteMail accent (default #0D9488 teal). Each email has a CTA button. Use `config('app.url')` for links.
  </action>
  <verify>
    <automated>cd /var/www/html/ledgeriq && php -r "require 'vendor/autoload.php'; foreach(['FirmInviteMail','DocumentRequestMail','AnnotationNotifyMail','DocumentUploadedMail','RequestFulfilledMail'] as \$c) { \$cls = 'App\\\\Mail\\\\'.\$c; if(!class_exists(\$cls)) { echo \"MISSING: \$c\n\"; exit(1); } } echo 'All 5 Mail classes exist';"</automated>
  </verify>
  <done>All 5 Mail classes created with proper constructors, subjects, and Blade templates. Each follows Queueable pattern.</done>
</task>

</tasks>

<verification>
- `php artisan route:list --path=firm` shows firm CRUD and invite routes
- All 5 Mail classes exist and can be instantiated
- Blade templates exist in resources/views/emails/
- config('spendifiai.tax_deadlines') returns array of deadline objects
</verification>

<success_criteria>
AccountantFirmController handles firm registration, management, invite links, and dashboard stats. 5 Mail classes with Blade templates cover all notification workflows. Public invite route renders firm branding.
</success_criteria>

<output>
After completion, create `.planning/phases/08-accountant-document-collaboration/08-02-SUMMARY.md`
</output>
