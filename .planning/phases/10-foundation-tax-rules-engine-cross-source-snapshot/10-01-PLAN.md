---
phase: 10-foundation-tax-rules-engine-cross-source-snapshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/tax-rules.php
  - app/Services/TaxRulesEngineService.php
  - tests/Unit/Services/TaxRulesConfigTest.php
  - tests/Unit/Services/TaxRulesEngineServiceTest.php
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, TAX-07]
user_setup: []

must_haves:
  truths:
    - "TaxRulesEngineService makes ZERO Claude/HTTP calls — a Pest test with Http::preventStrayRequests() proves no outbound requests occur during any computation"
    - "Every dollar amount, rate, and threshold the engine returns traces to a key in config/tax-rules.php — no IRS figure is hardcoded in the service class"
    - "Bracket tax, marginal rate, effective rate, standard deduction, contribution headroom (with age catch-up tiers), Roth band, SE tax, and QBI eligibility are all computed correctly at boundary inputs for all four filing statuses"
    - "Changing a config value (e.g. 2027 limits) automatically flips the corresponding assertion — tests read Config::get(), never hardcoded numbers"
  artifacts:
    - config/tax-rules.php
    - app/Services/TaxRulesEngineService.php
    - tests/Unit/Services/TaxRulesConfigTest.php
    - tests/Unit/Services/TaxRulesEngineServiceTest.php
  key_links:
    - "TaxRulesEngineService reads config via config('tax-rules.{year}.*') — the year is a method parameter, defaulting to 2026"
    - "Tests override config with Config::set() so assertions verify the wiring, not memorized constants"
---

<objective>
Deliver the deterministic tax-math bedrock for the entire Optimize My Income milestone: a year-versioned `config/tax-rules.php` holding all 2026 IRS constants (TAX-01) and a pure-PHP `TaxRulesEngineService` that computes marginal + effective federal rate, standard-vs-itemized comparison, 401(k)/IRA/HSA headroom with age catch-up, the Traditional-vs-Roth band + SECURE 2.0 §603 flag, SE tax, and QBI eligibility (TAX-02–TAX-06) — reading exclusively from config with zero Claude calls. Pest tests assert exact matches to config values at bracket boundaries and prove no stray HTTP requests occur (TAX-07).

Purpose: Phase 11 detectors and Phase 12 report read every number from this engine. It must be correct and auditable before anything downstream relies on it.
Output: One config file, one service, two test files. No DB dependencies — pure PHP, runnable in isolation.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-RESEARCH.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-PATTERNS.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-VALIDATION.md
@config/spendifiai.php
@app/Services/IncomeDetectorService.php
@tests/Unit/Services/SubscriptionDetectorServiceTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create year-versioned config/tax-rules.php with all 2026 IRS constants (TAX-01)</name>
  <files>config/tax-rules.php, tests/Unit/Services/TaxRulesConfigTest.php</files>
  <read_first>
    - 10-RESEARCH.md "Pattern 1: config/tax-rules.php — Exact Structure" (the full 2026 value set with citations is provided verbatim there — transcribe it exactly)
    - 10-PATTERNS.md "config/tax-rules.php (config, static)" — analog is config/spendifiai.php; year-integer array keys are valid; no env() calls for static tax data
    - config/spendifiai.php (structure/comment-block conventions)
  </read_first>
  <action>
    Create config/tax-rules.php returning an array keyed by tax year (top-level key `2026`), transcribing every value and citation comment from 10-RESEARCH.md Pattern 1: `brackets` (single, married_joint, married_separate, head_of_household — each an ordered list of `['rate','from','to']` with the top bracket's `to` = null), `standard_deduction`, `standard_deduction_senior_addition`, `401k` (employee_deferral, catchup_age_50_plus, catchup_age_60_to_63, mandatory_roth_catchup_threshold, highly_compensated_threshold), `ira` (annual_limit, catchup_age_50_plus, roth_phaseout, traditional_deduction_phaseout_covered, traditional_deduction_phaseout_spouse_covered), `hsa`, `se_tax`, `qbi`, and `roth_optimization`. All dollar values are plain integers in DOLLARS (the service converts to cents). Preserve the source-citation comments verbatim. CRITICAL: keep the SECURE 2.0 §603 `mandatory_roth_catchup_threshold` comment flagging it as an assumed/needs-verification 2026 indexed value exactly as written in RESEARCH — do not remove or soften that caveat. Do NOT add env() calls; this is static reference data. Do NOT merge into config/spendifiai.php — this is a separate domain with an annual update cadence.
    Then create tests/Unit/Services/TaxRulesConfigTest.php asserting the 2026 config structure is complete: each of the four filing statuses exists in `brackets` and each bracket list starts at from=0 and ends with to=null; `standard_deduction`, `401k`, `ira`, `hsa`, `se_tax`, `qbi`, and `roth_optimization` sub-keys are all present and non-empty. Assert the assumed §603 threshold key exists (value read from config, not hardcoded).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TaxRulesConfigTest</automated>
  </verify>
  <acceptance_criteria>
    - config/tax-rules.php returns an array with a `2026` key containing brackets (4 statuses), standard_deduction, standard_deduction_senior_addition, 401k, ira, hsa, se_tax, qbi, roth_optimization.
    - Each bracket list is ordered, starts at from=0, and the final bracket has to=null.
    - The §603 mandatory_roth_catchup_threshold retains its assumed/needs-verification comment.
    - TaxRulesConfigTest passes; no env() calls in the config file.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TaxRulesEngineService — pure-PHP deterministic math, zero Claude (TAX-02–TAX-06)</name>
  <files>app/Services/TaxRulesEngineService.php</files>
  <read_first>
    - 10-RESEARCH.md "Pattern 2: TaxRulesEngineService — Method Contracts" (exact public signatures), the private bracket-iteration algorithm, the SE-tax / QBI / 401k-headroom algorithms, and "Code Examples" (computeTax, remaining401kRoomCents, selfEmploymentTax reference implementations)
    - 10-RESEARCH.md "Common Pitfalls" 1 (§603 threshold), 2 (cents-vs-dollars), 4 (QBI SSTB out of scope for Phase 10)
    - 10-PATTERNS.md "TaxRulesEngineService (service, transform)" — App\Services namespace, typed-array returns with @return PHPDoc shapes, round monetary outputs, (float) casts not Number()
    - 10-RESEARCH.md "Security Domain" V5 — validate income >= 0, filing_status in the allowed set, throw InvalidArgumentException on invalid input
  </read_first>
  <behavior>
    - Bracket boundary: single filer with taxable income at 12,399 dollars stays in the 10% band; the first dollar at 12,400 is taxed at 12%; the first dollar at 50,400 is taxed at 22%.
    - marginalRate() returns the rate of the top bracket the income reaches, for all four filing statuses; effectiveRate() = computeTax / income and is <= marginalRate.
    - standardDeductionCents('single') equals the config single value times 100; with age >= 65 it adds the senior addition.
    - remaining401kRoomCents: age 45 -> base limit; age 50–59 or 64+ -> base + 50-plus catch-up; age 60–63 -> base + super catch-up; never negative.
    - remainingIraRoomCents(age 55) -> (annual_limit + 50-plus catch-up); remainingHsaRoomCents('family', age 56) -> (family + 55-plus catch-up).
    - rothVsTraditionalBand: <= 0.12 -> 'roth'; >= 0.32 -> 'traditional'; 0.22/0.24 -> 'split'. requiresMandatoryRothCatchup: prior-year FICA wages at/above the config threshold -> true.
    - selfEmploymentTax(net profit): net earnings = profit * 0.9235; SS portion capped at the wage base; Medicare uncapped; returns se_tax_cents and deductible_half_cents.
    - qbiDeduction: taxable income below the phase-out start -> min(qbi, taxable_income) * 0.20; at/above start+window for SSTB -> deduction 0; non-SSTB above start -> ['eligible'=>true,'deduction_cents'=>null,'reason'=>'above_threshold_requires_professional_review'] (do NOT compute the W-2 wage limitation in Phase 10).
    - Invalid filing_status or negative income throws InvalidArgumentException.
  </behavior>
  <action>
    Create app/Services/TaxRulesEngineService.php in the App\Services namespace implementing every public method contract from 10-RESEARCH.md Pattern 2 exactly (computeTax, marginalRate, effectiveRate, standardDeductionCents, compareStandardVsItemized, remaining401kRoomCents, remainingIraRoomCents, remainingHsaRoomCents, rothVsTraditionalBand, rothIraEligibility, requiresMandatoryRothCatchup, traditionalIraDeductibility, selfEmploymentTax, qbiDeduction, taxSavingsFromDeductionCents). Every method takes `int $year = 2026` and reads its numbers via config("tax-rules.{$year}.*") — NO IRS dollar amount, rate, or threshold may be written as a literal in this file. Convert config dollars to cents internally (times 100) using the documented `(int) round(... )` pattern; the service works exclusively in integer cents. Age catch-up tiers for 401(k)/IRA/HSA are selected by comparing age to config-driven tiers (50-plus vs 60–63 super catch-up for 401k). For QBI, follow Phase 10 scope from Pitfall 4 (eligibility + below-threshold estimate only; return null deduction with the professional-review reason above the threshold for non-SSTB). Validate inputs per Security Domain V5 (filing_status in {single, married_joint, married_separate, head_of_household}; income >= 0; supported year) and throw InvalidArgumentException otherwise. Add @return array{...} PHPDoc shapes on methods returning arrays. Make ZERO Claude/HTTP calls anywhere in this class.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty app/Services/TaxRulesEngineService.php && grep -rvE '^\s*(//|\*|/\*)' app/Services/TaxRulesEngineService.php | grep -qE '24500|7500|4400|8750|32200|16100|184500' && echo "FAIL: hardcoded IRS figure in service" && exit 1 || echo "OK: no hardcoded IRS figures"</automated>
  </verify>
  <acceptance_criteria>
    - All 15 public methods from Pattern 2 exist with the documented signatures and default `year = 2026`.
    - No IRS dollar/threshold literal appears in a non-comment line of the service (grep gate passes).
    - QBI above-threshold non-SSTB path returns eligible=true, deduction_cents=null, reason='above_threshold_requires_professional_review'.
    - Invalid filing_status / negative income throws InvalidArgumentException.
    - Zero Claude/HTTP calls in the class; `vendor/bin/pint --dirty` reports clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Pest boundary + property + no-Claude tests for the engine (TAX-07)</name>
  <files>tests/Unit/Services/TaxRulesEngineServiceTest.php</files>
  <read_first>
    - 10-RESEARCH.md "Validation Architecture" → "Phase Requirements → Test Map" (the 19 boundary/property cases for TAX-02..06) and the "Critical test pattern (TAX-07)" showing Config::set() + config-derived expected values
    - 10-VALIDATION.md items 2–4 (boundary tests, property tests, no-Claude-in-numbers guard)
    - 10-PATTERNS.md "tests/Unit/Services/... (test, unit)" — Pest.php auto-extends Tests\TestCase in Unit/Services; TaxRulesEngineService is pure PHP so no Http::fake is needed
    - tests/Unit/Services/SubscriptionDetectorServiceTest.php (file header + it() style)
  </read_first>
  <action>
    Create tests/Unit/Services/TaxRulesEngineServiceTest.php covering every case in the RESEARCH Test Map: bracket tax at the 10/12/22 boundaries for single (and marginal rate at each bracket boundary across all four filing statuses); effectiveRate <= marginalRate; standardDeductionCents equals config-times-100; compareStandardVsItemized picks 'standard' when itemized is lower; 401k headroom at ages 45/50/61; IRA headroom at 55; HSA family headroom at 56; rothVsTraditionalBand at 0.12/0.22/0.32; requiresMandatoryRothCatchup above threshold; selfEmploymentTax against the 0.9235/0.153 computation; qbiDeduction below threshold = qbi*0.20. CRITICAL (TAX-07): every expected value is derived from Config::get('tax-rules.2026.*') or set via Config::set() at the top of the test — do NOT hardcode dollar amounts as expected values, so a future config edit surfaces the assertion instead of silently passing. Add a property test asserting marginal rate is monotonic non-decreasing across ascending incomes and that headroom is never negative. Add a guard test that calls Http::preventStrayRequests() then exercises computeTax/remaining401kRoomCents/selfEmploymentTax/qbiDeduction and asserts they complete with no outbound HTTP request (proves zero Claude calls — foundation for SAFE-03).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TaxRulesEngineServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - All boundary cases from the RESEARCH Test Map pass for all four filing statuses.
    - Expected values are read from Config, not hardcoded (a Config::set override changes the asserted result).
    - Property tests (monotonic marginal rate, non-negative headroom) pass.
    - The Http::preventStrayRequests() guard test passes, proving the engine makes zero outbound calls.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → TaxRulesEngineService | Untrusted method arguments (income, filing_status, year, age) cross into deterministic math |
| service → config/tax-rules.php | Read-only config; single source of truth for all numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | TaxRulesEngineService filing_status / income args | medium | mitigate | Validate filing_status against the allowed set and income >= 0; throw InvalidArgumentException (Security Domain V5) |
| T-10-02 | Tampering | Bracket math on very large income | low | accept | 64-bit int on PHP 8.3; PHP_INT_MAX (~9.2e18 cents) far exceeds any realistic income — no overflow risk |
| T-10-03 | Information Disclosure | Hardcoded/leaked IRS constants drift from config | low | mitigate | Grep gate asserts zero IRS literals in the service; all numbers flow from config |
| T-10-04 | Tampering | Silent AI dependency creeping into the "deterministic" engine | high | mitigate | Http::preventStrayRequests() guard test proves zero outbound calls (SAFE-03 foundation) |
</threat_model>

<verification>
- `php artisan test --compact --filter=TaxRulesConfig` and `--filter=TaxRulesEngineService` both green.
- Grep gate: no IRS dollar/threshold literal in a non-comment line of app/Services/TaxRulesEngineService.php.
- `php artisan test --compact` full suite stays green (225+ existing tests, zero regressions).
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- config/tax-rules.php holds every 2026 IRS constant with citations and the flagged §603 caveat (TAX-01).
- TaxRulesEngineService computes marginal/effective rate, standard-vs-itemized, 401k/IRA/HSA headroom with catch-up tiers, Roth band + §603 flag, SE tax, and QBI eligibility purely from config (TAX-02–TAX-06).
- Pest tests assert exact config-derived matches at bracket boundaries and prove zero Claude/HTTP calls (TAX-07).
</success_criteria>

<output>
Create `.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-01-SUMMARY.md` when done
</output>
