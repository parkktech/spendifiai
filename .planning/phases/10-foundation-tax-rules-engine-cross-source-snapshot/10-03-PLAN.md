---
phase: 10-foundation-tax-rules-engine-cross-source-snapshot
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - database/migrations/2026_07_01_110000_create_optimization_findings_table.php
  - app/Models/OptimizationFinding.php
  - database/factories/OptimizationFindingFactory.php
  - app/Services/CrossSourceReviewService.php
  - app/Jobs/BuildIncomeOptimizationProfile.php
  - app/Events/OptimizationProfileBuilt.php
  - tests/Feature/CrossSourceReviewServiceTest.php
autonomous: true
requirements: [CTX-03, CTX-04]
user_setup: []

must_haves:
  truths:
    - "CrossSourceReviewService flags a W-2-vs-bank-deposit gap ONLY when it exceeds the 15% tolerance, and an SE-income gap only above 20% — the decision is deterministic, with ZERO Claude calls in Phase 10"
    - "The BuildIncomeOptimizationProfile job assembles the snapshot, runs the deterministic cross-source review, upserts OptimizationFinding rows, and fires OptimizationProfileBuilt for Phase 11 listeners"
    - "Findings and the snapshot expose what is already answerable so downstream detectors/interview skip known facts (CTX-04)"
  artifacts:
    - database/migrations/2026_07_01_110000_create_optimization_findings_table.php
    - app/Models/OptimizationFinding.php
    - app/Services/CrossSourceReviewService.php
    - app/Jobs/BuildIncomeOptimizationProfile.php
    - app/Events/OptimizationProfileBuilt.php
    - tests/Feature/CrossSourceReviewServiceTest.php
  key_links:
    - "Job → IncomeOptimizerDataAssemblerService::buildProfile() (from 10-02) → CrossSourceReviewService::review() → OptimizationFinding::updateOrCreate() → event(OptimizationProfileBuilt)"
    - "Findings keyed on user_id + tax_year + finding_key for idempotent upsert"
---

<objective>
Complete the snapshot pipeline: `CrossSourceReviewService` deterministically compares the assembled snapshot's document-sourced income against bank deposits (and SE income) using fixed tolerances, emitting discrepancy findings with ZERO Claude calls in Phase 10 (Claude narration is deferred to Phase 11). Persist findings in a new `OptimizationFinding` model and wire the whole flow behind `BuildIncomeOptimizationProfile` (a queued job) which assembles → reviews → upserts findings → fires `OptimizationProfileBuilt` for Phase 11 listeners (CTX-03). Downstream consumers can tell what is already answerable from the snapshot + findings (CTX-04).

Purpose: This is the deterministic discrepancy detector and the async entry point every later phase triggers. Getting the tolerance logic and the event contract right here unblocks Phase 11 red-flag detection.
Output: One migration, one model, one factory, one service, one job, one event, one feature test. Depends on the IncomeOptimizationProfile + assembler from plan 10-02.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-RESEARCH.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-PATTERNS.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-02-SUMMARY.md
@app/Jobs/CategorizePendingTransactions.php
@app/Services/TaxDeductionFinderService.php
@database/migrations/2026_03_30_000001_create_tax_documents_table.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: OptimizationFinding migration + model + factory (CTX-03)</name>
  <files>database/migrations/2026_07_01_110000_create_optimization_findings_table.php, app/Models/OptimizationFinding.php, database/factories/OptimizationFindingFactory.php</files>
  <read_first>
    - 10-RESEARCH.md "Pattern 5" finding-creation pattern (updateOrCreate keyed on user_id + tax_year + finding_key) and the finding data shape (type, w2_cents, bank_cents, gap_cents, gap_pct)
    - 10-PATTERNS.md migration + model conventions (Schema::create() only; casts() method syntax; $hidden for any sensitive amounts)
    - database/migrations/2026_03_30_000001_create_tax_documents_table.php (migration analog)
    - CLAUDE.md safety rules — additive/forward-only; TEXT for any encrypted field
  </read_first>
  <action>
    Create the migration `create_optimization_findings_table` with Schema::create() ONLY: id, user_id (foreignId constrained cascadeOnDelete), tax_year (unsignedSmallInteger), finding_key (string — stable identifier e.g. 'w2_deposit_mismatch'), finding_type (string), severity (string, nullable), a jsonb `details` column for the deterministic comparison payload (gap_cents, gap_pct, w2_cents, bank_cents — cents integers), a nullable text `description` column reserved for the Phase 11 Claude narration (leave null in Phase 10), status (string default 'open'), and timestamps. Add unique(['user_id','tax_year','finding_key']) for idempotent upsert and index(['user_id','tax_year']).
    Create app/Models/OptimizationFinding.php: $fillable for all columns; casts() (Laravel 12 method syntax) mapping details → 'array'; a user() BelongsTo; a scopeForUser(Builder, int $userId) for V4 access-control scoping.
    Create database/factories/OptimizationFindingFactory.php for tests.
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | grep -iE 'optimization_findings' && vendor/bin/pint --dirty app/Models/OptimizationFinding.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration uses Schema::create() only; unique(['user_id','tax_year','finding_key']) + index present; `migrate --pretend` shows the new table without touching existing tables.
    - Model casts details as array and exposes scopeForUser; Pint clean.
    - Factory creates a persistable finding.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CrossSourceReviewService — deterministic tolerance comparison, zero Claude (CTX-03, CTX-04)</name>
  <files>app/Services/CrossSourceReviewService.php</files>
  <read_first>
    - 10-RESEARCH.md "Pattern 5: CrossSourceReviewService — Discrepancy Algorithm" (W2_DEPOSIT_TOLERANCE = 0.15, SE_INCOME_TOLERANCE = 0.20, compareW2VsDeposits / compareSEIncomeVsDeposits logic, return-array shape) and the CTX-04 "answerable field mapping" table
    - 10-PATTERNS.md "CrossSourceReviewService (service, compare)" — withinTolerance helper; deterministic, no Claude; typed-array returns
    - app/Services/TaxDeductionFinderService.php (service structure analog)
  </read_first>
  <behavior>
    - review() returns an array of finding-data arrays (not persisted here — the job persists them).
    - W-2 vs deposits: if w2_cents == 0 OR bank_deposit_total == 0 → no finding (insufficient data). Otherwise gap_pct = |w2 - bank| / max(w2, bank); emit a 'w2_deposit_mismatch' finding ONLY when gap_pct > 0.15.
    - SE income vs deposits: same logic against 0.20; emit 'se_income_deposit_mismatch' only when self_employment_income > 0 AND bank_deposit_total > 0 AND gap_pct > 0.20.
    - No Claude/HTTP calls — descriptions are left null for Phase 11.
  </behavior>
  <action>
    Create app/Services/CrossSourceReviewService.php (App\Services, NO Claude/HTTP). Define the two tolerance class constants (W2_DEPOSIT_TOLERANCE = 0.15, SE_INCOME_TOLERANCE = 0.20 — business tolerances, not IRS figures). Public `review(IncomeOptimizationProfile $profile, User $user, int $taxYear): array` reads the profile's encrypted cent values (decrypt via the model cast, cast to int), runs protected compareW2VsDeposits() and compareSEIncomeVsDeposits() per the Pattern 5 logic, and returns an array of finding-data arrays each carrying finding_key, finding_type, and a details payload (w2_cents/bank_cents/gap_cents/gap_pct). Leave any human-readable description null (Phase 11 fills it). Use a withinTolerance-style helper; guard against divide-by-zero when both values are 0. This service NEVER calls Claude in Phase 10.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty app/Services/CrossSourceReviewService.php && ! grep -qiE 'anthropic|Http::|Claude|->chat\(' app/Services/CrossSourceReviewService.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - review() emits a w2_deposit_mismatch finding when the gap exceeds 15% and none when it is within 15%.
    - SE mismatch emitted only above 20% and only when both SE income and deposits are > 0.
    - No Claude/HTTP references in the file (grep gate passes); descriptions left null.
    - Pint clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: BuildIncomeOptimizationProfile job + OptimizationProfileBuilt event + feature tests (CTX-03, CTX-04)</name>
  <files>app/Jobs/BuildIncomeOptimizationProfile.php, app/Events/OptimizationProfileBuilt.php, tests/Feature/CrossSourceReviewServiceTest.php</files>
  <read_first>
    - 10-RESEARCH.md "Pattern 6: BuildIncomeOptimizationProfile Job" (tries=3, timeout=180, primitive constructor args, handle() injects assembler + cross-source service, upsert findings, fire event, failed() logging) and the finding-creation pattern
    - 10-PATTERNS.md "BuildIncomeOptimizationSnapshot (job)" — pass primitive ids, set tries+timeout, inject services via handle(), Log at start/end
    - app/Jobs/CategorizePendingTransactions.php (job analog); existing events under app/Events (event class shape)
    - 10-02-SUMMARY.md (IncomeOptimizerDataAssemblerService::buildProfile signature + IncomeOptimizationProfile from plan 10-02)
  </read_first>
  <action>
    Create app/Jobs/BuildIncomeOptimizationProfile.php implementing ShouldQueue with `public int $tries = 3; public int $timeout = 180;`, a constructor taking primitive `int $userId, int $taxYear` (NOT models), and handle(IncomeOptimizerDataAssemblerService $assembler, CrossSourceReviewService $crossSource): resolve the User, call $assembler->buildProfile() to build/refresh the snapshot, call $crossSource->review() for deterministic findings, upsert each via OptimizationFinding::updateOrCreate(['user_id','tax_year','finding_key'], ...), then event(new OptimizationProfileBuilt($userId, $taxYear, count($findings))). Add a failed(Throwable) method logging user_id/tax_year/error. Dispatch onto the 'optimization' queue (the existing `queue:work redis` handle can include --queue=optimization,default — note this operationally; do NOT modify .env in this task). Log::info at start and end.
    Create app/Events/OptimizationProfileBuilt.php (Dispatchable, SerializesModels) carrying userId, taxYear, findingCount — the Phase 11 listener attachment point.
    Create tests/Feature/CrossSourceReviewServiceTest.php (RefreshDatabase): (CTX-03) build a profile with a W-2/deposit gap > 15% and assert the job creates a 'w2_deposit_mismatch' OptimizationFinding; build one with a gap <= 15% and assert NO finding is created. Assert OptimizationProfileBuilt is dispatched (Event::fake) with the correct finding count. (CTX-04) assert the built profile's answerableFields() reports known flags so Phase 11 can skip those questions. Use Bus/Event fakes and the factories from plan 10-02 + Task 1.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=CrossSourceReviewServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - Job sets tries=3/timeout=180, takes primitive constructor args, and chains assemble → review → upsert findings → fire event.
    - Gap > 15% creates a w2_deposit_mismatch finding; gap <= 15% creates none.
    - OptimizationProfileBuilt fires with the correct finding count (asserted via Event::fake).
    - CTX-04 answerable-fields assertion passes.
    - Full `php artisan test --compact` suite stays green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queued job → snapshot + findings | Async job reads a user's encrypted snapshot and writes findings; must stay scoped to that user |
| CrossSourceReviewService → decision | Deterministic tolerance decides discrepancy — no AI judgment in Phase 10 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Information Disclosure | OptimizationFinding leaking another user's amounts | high | mitigate | user_id scoping via scopeForUser; findings keyed and upserted per user_id + tax_year |
| T-10-10 | Tampering | AI judgment substituted for deterministic tolerance | high | mitigate | Tolerances are fixed class constants; grep gate proves zero Claude/HTTP references in CrossSourceReviewService (Phase 10) |
| T-10-11 | Tampering (data loss) | Non-additive findings migration | high | mitigate | Schema::create() on a NEW table only; no changes to existing tables (CLAUDE.md) |
| T-10-12 | Denial of Service | Job retry storm on transient failure | low | accept | tries=3 + failed() logging bounds retries; queue isolation on 'optimization' |
</threat_model>

<verification>
- `php artisan test --compact --filter=CrossSourceReviewServiceTest` green.
- `php artisan migrate --pretend` shows only the new optimization_findings CREATE TABLE.
- Grep gate: CrossSourceReviewService has zero Claude/HTTP references.
- Full `php artisan test --compact` suite green (225+ existing tests, zero regressions).
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- CrossSourceReviewService deterministically flags document-vs-deposit discrepancies at the 15%/20% tolerances with zero Claude calls (CTX-03).
- BuildIncomeOptimizationProfile job assembles the snapshot, runs the review, upserts findings, and fires OptimizationProfileBuilt (CTX-02/CTX-03 pipeline).
- Downstream consumers can determine what is already answerable from the snapshot + findings (CTX-04).
</success_criteria>

<output>
Create `.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-03-SUMMARY.md` when done
</output>
