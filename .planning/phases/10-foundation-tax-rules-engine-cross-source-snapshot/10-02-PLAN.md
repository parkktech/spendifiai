---
phase: 10-foundation-tax-rules-engine-cross-source-snapshot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_01_100000_create_income_optimization_profiles_table.php
  - app/Models/IncomeOptimizationProfile.php
  - database/factories/IncomeOptimizationProfileFactory.php
  - app/Services/IncomeOptimizerDataAssemblerService.php
  - tests/Feature/IncomeOptimizerDataAssemblerTest.php
autonomous: true
requirements: [CTX-01, CTX-02, CTX-04]
user_setup: []

must_haves:
  truths:
    - "A per-user IncomeOptimizationProfile row is assembled from existing sources (UserFinancialProfile, extracted TaxDocuments, bank transactions) with ZERO Claude calls"
    - "All money columns are encrypted TEXT (never plaintext integer) and hidden from API responses; the model round-trips encrypted cent-strings"
    - "Bank deposits are summed over the tax year's calendar range (Jan 1–Dec 31), NOT a rolling 3-month window — verified by a mid-year test returning the full-year total"
    - "The snapshot exposes the answerable-field flags (filing_status, has_hsa_eligible_plan, has_ira, ira_type, has_home_office, has_self_employment, 401k/HSA ytd) so downstream detectors can skip what is already known (CTX-04)"
    - "Rebuilding the profile after the document set changes produces a different profile_hash (staleness detection)"
  artifacts:
    - database/migrations/2026_07_01_100000_create_income_optimization_profiles_table.php
    - app/Models/IncomeOptimizationProfile.php
    - database/factories/IncomeOptimizationProfileFactory.php
    - app/Services/IncomeOptimizerDataAssemblerService.php
    - tests/Feature/IncomeOptimizerDataAssemblerTest.php
  key_links:
    - "Assembler reads TaxDocument::forUser()->byYear()->byStatus(DocumentStatus::Ready) grouped by TaxDocumentCategory and sums extracted_data fields (dollars → cents)"
    - "Assembler queries Transaction directly for the tax-year date range instead of IncomeDetectorService's rolling window"
    - "profile_hash = SHA-256 of user_id + tax_year + sorted extracted doc IDs; drives staleness detection via isStale()"
---

<objective>
Build the per-user financial snapshot: a new `IncomeOptimizationProfile` cache model (encrypted TEXT money columns, additive/forward-only migration) and `IncomeOptimizerDataAssemblerService` that aggregates the snapshot from existing v2.0 sources — UserFinancialProfile flags, TaxDocument.extracted_data grouped by category, and bank transactions over the tax-year calendar range — with zero Claude calls (CTX-01, CTX-02). The snapshot surfaces the "answerable" flags so Phase 11 detectors/interview can skip already-known facts (CTX-04).

Purpose: This snapshot is the single input the Phase 11 red-flag detectors and Phase 12 report read from — it must accurately reflect the vault + bank data and never leak plaintext income.
Output: One migration, one model, one factory, one assembler service, one feature test. No changes to any existing table, model, or API response.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-RESEARCH.md
@.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-PATTERNS.md
@app/Models/UserFinancialProfile.php
@app/Models/TaxDocument.php
@app/Enums/TaxDocumentCategory.php
@app/Enums/DocumentStatus.php
@app/Services/IncomeDetectorService.php
@app/Services/TaxDeductionFinderService.php
@database/migrations/2026_03_30_000001_create_tax_documents_table.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive migration + IncomeOptimizationProfile model + factory (CTX-02, CTX-04)</name>
  <files>database/migrations/2026_07_01_100000_create_income_optimization_profiles_table.php, app/Models/IncomeOptimizationProfile.php, database/factories/IncomeOptimizationProfileFactory.php</files>
  <read_first>
    - 10-RESEARCH.md "Pattern 3: IncomeOptimizationProfile Migration" (full column list, encrypted TEXT money columns, non-sensitive flag columns, jsonb data_sources, profile_hash, unique [user_id, tax_year]) and the model casts block
    - 10-PATTERNS.md "IncomeOptimizationProfile.php (model)" and "create_income_optimization_profiles_table (migration)" — encrypted fields use $table->text(), casts() method syntax, $hidden on all encrypted fields, float accessors
    - app/Models/UserFinancialProfile.php (encrypted cast + $hidden + float accessor convention)
    - database/migrations/2026_03_30_000001_create_tax_documents_table.php (migration analog)
    - CLAUDE.md safety rules: Schema::create() only; NEVER dropColumn/dropIfExists on existing tables; encrypted fields MUST be TEXT
  </read_first>
  <action>
    Create the migration `create_income_optimization_profiles_table` using Schema::create() ONLY (additive/forward-only — this is a brand-new table; the down() may Schema::dropIfExists the NEW table since it is created by this same migration). Define every column from RESEARCH Pattern 3: id, user_id (foreignId constrained cascadeOnDelete), tax_year (unsignedSmallInteger); the income signal columns (w2_wages, self_employment_income, interest_income, dividend_income, retirement_distributions, bank_deposit_total), deduction signals (mortgage_interest, property_tax_paid, student_loan_interest, charitable_contributions), and retirement contribution signals (traditional_401k_ytd, roth_401k_ytd, ira_ytd, hsa_ytd) — ALL as $table->text()->nullable() because they carry encrypted cent-strings (a plaintext integer column would leak income); the non-sensitive flag/string columns (filing_status, has_home_office, has_self_employment, has_hsa_eligible_plan, has_ira boolean, ira_type, employment_type, estimated_age); metadata (data_sources jsonb default '{}', doc_count, profile_hash char(64) nullable, built_at timestamp) and timestamps. Add unique(['user_id','tax_year']) and index(['user_id','tax_year']).
    Create app/Models/IncomeOptimizationProfile.php with $fillable for all columns, $hidden listing EVERY encrypted money column, the casts() method (Laravel 12 method syntax) mapping all 14 money columns to 'encrypted', the booleans to 'boolean', data_sources to 'array', built_at to 'datetime'; a user() BelongsTo; a scopeForUser(Builder, int $userId) mirroring TaxDocument::scopeForUser for V4 access-control scoping; and an `answerableFields(): array` helper returning the CTX-04 skip-logic map (filing_status, has_hsa_eligible_plan, has_ira, ira_type, has_home_office, has_self_employment, whether traditional_401k_ytd/hsa_ytd are > 0) so Phase 11 can query "what do we already know". Document in the class docblock that encrypted money columns store integer CENTS as strings (e.g. '750000' == $7,500.00).
    Create database/factories/IncomeOptimizationProfileFactory.php producing a valid profile for tests (cents-as-string money fields, a filing_status, a tax_year).
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | grep -iE 'create table|income_optimization_profiles' && vendor/bin/pint --dirty app/Models/IncomeOptimizationProfile.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration uses Schema::create() only; every money column is TEXT; unique + index present; `php artisan migrate --pretend` shows the CREATE TABLE without touching any existing table.
    - Model casts all 14 money columns as 'encrypted', hides them, and round-trips an encrypted cent-string.
    - scopeForUser and answerableFields() exist; docblock documents the cents-as-string convention.
    - Factory creates a persistable profile; Pint clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: IncomeOptimizerDataAssemblerService — multi-source aggregation, zero Claude (CTX-01)</name>
  <files>app/Services/IncomeOptimizerDataAssemblerService.php</files>
  <read_first>
    - 10-RESEARCH.md "Pattern 4: IncomeOptimizerDataAssemblerService — Source Reading Strategy" (the 4-source field mapping, TaxDocument field-access pattern, cents conversion rule, computeProfileHash) and "Pitfall 2" (cents vs dollars) and "Pitfall 3" (IncomeDetectorService rolling-window trap)
    - 10-PATTERNS.md "IncomeOptimizerDataAssemblerService (service)" — single public method + protected gather helpers, typed-array returns, App\Services namespace, (float) casts
    - app/Services/TaxDeductionFinderService.php (gather-helper-then-merge structure; householdUserIds pattern if needed)
    - app/Services/IncomeDetectorService.php (classifyType logic to replicate for the direct tax-year query — DO NOT call analyze()'s rolling window)
    - app/Enums/DocumentStatus.php and app/Enums/TaxDocumentCategory.php
  </read_first>
  <action>
    Create app/Services/IncomeOptimizerDataAssemblerService.php (App\Services, NO Claude/HTTP). Public `buildProfile(User $user, int $taxYear): IncomeOptimizationProfile` orchestrates protected gather helpers and upserts (updateOrCreate keyed on user_id + tax_year) the IncomeOptimizationProfile. Public `isStale(IncomeOptimizationProfile $profile): bool` recomputes the hash from current inputs and compares to the stored profile_hash.
    Source 1 — readProfileFlags(User): read UserFinancialProfile flags directly (employment_type, tax_filing_status → filing_status, has_hsa → has_hsa_eligible_plan, has_ira, ira_type, has_home_office; derive has_self_employment from employment_type === 'self_employed').
    Source 2 — sumFromDocuments(User, taxYear): query TaxDocument::forUser($user->id)->byYear($taxYear)->byStatus(DocumentStatus::Ready) [IMPORTANT: the completed-extraction status in this codebase's enum is `Ready`, NOT `Extracted` — RESEARCH's `DocumentStatus::Extracted` reference is outdated], grouped by TaxDocumentCategory, summing the mapped extracted_data fields exactly per RESEARCH Pattern 4 (W2→wages; NEC_1099/MISC_1099→self_employment_income; INT_1099→interest_income; DIV_1099→dividend_income; R_1099→retirement_distributions; Mortgage_1098→mortgage_interest; PropertyTax→property_tax_paid; E_1098→student_loan_interest; CharitableDonation→charitable_contributions; HSA_5498→hsa_ytd; IRA_5498→ira_ytd). extracted_data values are float DOLLAR strings — ALWAYS convert with `(int) round((float) $value * 100)` to cents before storing (Pitfall 2). Track the contributing doc IDs and doc_count.
    Source 3 — sumBankDeposits(User, taxYear): query Transaction DIRECTLY for the calendar-year range (Carbon startOfYear/endOfYear for $taxYear, i.e. Jan 1–Dec 31), applying the same income-type classification IncomeDetectorService uses, summing primary + extra income and excluding transfers/refunds. DO NOT use IncomeDetectorService::analyze()'s rolling monthsBack window (Pitfall 3). Store as encrypted cents in bank_deposit_total.
    Compute profile_hash = SHA-256 of user_id + tax_year + sorted contributing doc IDs (per the Profile Staleness Hash example). Persist data_sources jsonb (doc_ids, transaction_range), doc_count, built_at, and all answerable flags. Store every money field as a cents string.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty app/Services/IncomeOptimizerDataAssemblerService.php && grep -qE 'DocumentStatus::Ready' app/Services/IncomeOptimizerDataAssemblerService.php && ! grep -qE 'DocumentStatus::Extracted|->analyze\(' app/Services/IncomeOptimizerDataAssemblerService.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - buildProfile() upserts an IncomeOptimizationProfile keyed on user_id + tax_year, reading UserFinancialProfile flags, TaxDocument extracted_data (status Ready, grouped by category), and tax-year bank deposits.
    - Document reads use DocumentStatus::Ready; the service does NOT reference DocumentStatus::Extracted and does NOT call IncomeDetectorService::analyze() (grep gate passes).
    - All extracted_data dollar values are converted to cents via (int) round((float) value * 100).
    - Bank deposits are summed over Jan 1–Dec 31 of the tax year via a direct Transaction query.
    - profile_hash is a SHA-256 of user_id + tax_year + sorted doc IDs; isStale() detects doc-set changes.
    - Zero Claude/HTTP calls; Pint clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Feature tests — snapshot assembly, calendar-year window, answerable fields, staleness (CTX-01, CTX-02, CTX-04)</name>
  <files>tests/Feature/IncomeOptimizerDataAssemblerTest.php</files>
  <read_first>
    - 10-RESEARCH.md "Validation Architecture" → Test Map rows for CTX-01, CTX-02, CTX-04 and "Pitfall 3" (mid-year rolling-window failure signature)
    - 10-VALIDATION.md item 5 (calendar-year date-range guard) and item 6 (encrypted round-trip)
    - 10-PATTERNS.md test conventions; tests/Pest.php helpers (createAuthenticatedUser, createUserWithBank, createUserWithBankAndProfile)
    - app/Models/TaxDocument.php + database factories for TaxDocument / Transaction / UserFinancialProfile
  </read_first>
  <action>
    Create tests/Feature/IncomeOptimizerDataAssemblerTest.php using RefreshDatabase. Cases:
    (CTX-01) Seed a Ready W2 TaxDocument with extracted_data wages "72500.00"; assert buildProfile() persists w2_wages as the encrypted cent-string equal to 7,250,000 cents and round-trips via the model cast (proves dollars→cents and encryption).
    (CTX-02) Assert an IncomeOptimizationProfile row is created with the answerable flag columns populated from a seeded UserFinancialProfile (filing_status, has_hsa_eligible_plan, has_ira, has_home_office). Assert rebuilding after adding a new Ready doc changes profile_hash and that isStale() returns true when the stored hash no longer matches the current inputs.
    (Pitfall 3 guard) Seed transactions spread across the full tax year including months OUTSIDE a rolling 3-month window from "now"; run buildProfile() as-if mid-year (freeze time mid-year) and assert bank_deposit_total equals the FULL Jan 1–Dec 31 income sum, not a 3-month subset — this is the regression guard for the IncomeDetectorService rolling-window trap.
    (CTX-04) Assert answerableFields() on the built profile reports filing_status/has_ira/has_home_office as answerable when set, so Phase 11 can skip those interview questions.
    Use factories; freeze time with Carbon::setTestNow for the mid-year case.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=IncomeOptimizerDataAssemblerTest</automated>
  </verify>
  <acceptance_criteria>
    - CTX-01: seeded W2 wages "72500.00" → w2_wages round-trips as 7,250,000 cents (encrypted).
    - CTX-02: profile row created with answerable flags; rebuild after doc-set change alters profile_hash and isStale() flips true.
    - Pitfall 3 guard: mid-year run returns the full calendar-year deposit total, not a 3-month subset.
    - CTX-04: answerableFields() reports set flags as answerable.
    - Full `php artisan test --compact` suite stays green (no regressions).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembler → encrypted DB records | Reads decrypted TaxDocument.extracted_data + UserFinancialProfile; writes encrypted cents to the new profile |
| stored profile → future controllers/API | Snapshot must never leak plaintext income; $hidden + encrypted casts enforce this |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Information Disclosure | Requesting another user's IncomeOptimizationProfile | high | mitigate | scopeForUser($userId) on all queries; assembler only ever operates on the passed User; future controller adds policy check |
| T-10-06 | Information Disclosure | Income stored in plaintext integer column | high | mitigate | Migration review gate — every money column is TEXT with 'encrypted' cast + $hidden; `migrate --pretend` inspected |
| T-10-07 | Tampering (data loss) | Non-additive migration on existing tables | high | mitigate | Schema::create() on a NEW table only; no dropColumn/dropIfExists against existing tables (CLAUDE.md) |
| T-10-08 | Tampering | Silent AI/HTTP dependency in the "no-Claude" assembler | high | mitigate | Grep gate proves no analyze()/Claude usage; assembler is pure DB aggregation |
</threat_model>

<verification>
- `php artisan test --compact --filter=IncomeOptimizerDataAssemblerTest` green.
- `php artisan migrate --pretend` shows only the new CREATE TABLE; no existing table touched.
- Grep gate: assembler uses DocumentStatus::Ready and does not call analyze() or reference DocumentStatus::Extracted.
- Full `php artisan test --compact` suite green (225+ existing tests, zero regressions).
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
- IncomeOptimizationProfile table + model exist with encrypted TEXT money columns, additive migration, and answerable-field exposure (CTX-02, CTX-04).
- IncomeOptimizerDataAssemblerService assembles the snapshot from UserFinancialProfile, extracted TaxDocuments (status Ready), and tax-year bank deposits with zero Claude calls (CTX-01).
- Bank deposits are summed over the calendar year, not a rolling window; staleness detected via profile_hash.
</success_criteria>

<output>
Create `.planning/phases/10-foundation-tax-rules-engine-cross-source-snapshot/10-02-SUMMARY.md` when done
</output>