---
phase: 09-intelligence-layer-final-validation
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified: []
autonomous: true
requirements: [TEST-05, TEST-06]
must_haves:
  truths:
    - "npm run build succeeds with zero errors"
    - "vendor/bin/pint reports no formatting issues"
    - "All existing tests still pass after fixes"
  artifacts: []
  key_links: []
---

<objective>
Run build validation and code style checks as the v2.0 quality gate. Fix any TypeScript errors found by `npm run build` and any PHP formatting issues found by `vendor/bin/pint`. Ensure all tests pass.

Purpose: Final quality gate before v2.0 ships -- everything must be green.
Output: Clean build, clean pint, all tests passing.
</objective>

<execution_context>
@/home/jratz/.claude/get-shit-done/workflows/execute-plan.md
@/home/jratz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-intelligence-layer-final-validation/09-01-SUMMARY.md
@.planning/phases/09-intelligence-layer-final-validation/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript build errors and PHP formatting issues</name>
  <files></files>
  <action>
1. Run `npm run build` and capture output. If any TypeScript errors:
   - Read each error, identify the file and line
   - Fix the errors (common issues: missing type imports, incorrect prop types, unused variables, missing interface fields)
   - Re-run `npm run build` until it succeeds with zero errors
   - Do NOT change functionality -- only fix type errors

2. Run `vendor/bin/pint --dirty --format agent` and capture output. If any formatting issues:
   - Pint auto-fixes files, so just run it
   - If `--dirty` finds nothing but full run does, run `vendor/bin/pint` (full)
   - Re-run to confirm zero issues

3. Run `php artisan test --compact` to verify all tests still pass after any fixes.
   - If any test fails, investigate and fix the root cause
   - The fix should not change test expectations -- if a test broke from a pint/TS fix, the fix was wrong

4. Final verification: run all three commands in sequence:
   - `npm run build` -- must succeed
   - `vendor/bin/pint --test` -- must report no changes needed
   - `php artisan test --compact` -- all tests must pass
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && vendor/bin/pint --test 2>&1 | tail -3 && php artisan test --compact 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build exits 0 with no errors. vendor/bin/pint --test exits 0. All tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build` exits with code 0
- `vendor/bin/pint --test` exits with code 0 (no changes needed)
- `php artisan test --compact` shows all tests passing
</verification>

<success_criteria>
- Zero TypeScript errors in production build
- Zero PHP formatting issues
- All existing + new tests pass
- v2.0 milestone quality gate is green
</success_criteria>

<output>
After completion, create `.planning/phases/09-intelligence-layer-final-validation/09-03-SUMMARY.md`
</output>
