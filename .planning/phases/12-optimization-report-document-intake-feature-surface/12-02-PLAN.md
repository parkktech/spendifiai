---
phase: 12-optimization-report-document-intake-feature-surface
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - app/Events/TaxDocumentExtracted.php
  - app/Jobs/ExtractTaxDocument.php
  - app/Jobs/ExtractProfileFacts.php
  - app/Services/AI/PaystubFactExtractorService.php
  - app/Services/IncomeOptimizerDataAssemblerService.php
  - app/Services/HsaShoeboxService.php
  - app/Http/Controllers/Api/DocumentRequestController.php
  - tests/Feature/ExtractProfileFactsDispatchTest.php
  - tests/Feature/PaystubProposalFlowTest.php
  - tests/Feature/HsaShoeboxTest.php
autonomous: true
requirements: [DOC-03, DOC-05, DOC-07, STORE-03]
must_haves:
  truths:
    - "When a pay-stub or benefits-guide extraction completes, per-field UserTaxFact PROPOSALS are created (is_current=false) that are invisible to skip-logic until the user confirms — a user-entered value is never silently overwritten (D4)."
    - "When any document extraction completes, a TaxDocumentExtracted event fires exactly once on the ready path, carrying the user + tax year for staleness consumers."
    - "Extracted pay-stub YTD deductions feed the IncomeOptimizationProfile signal totals via the assembler."
    - "A user's out-of-pocket medical receipts accumulate as an HSA shoebox list on the optimization profile with post-establishment-only education copy (STORE-03)."
    - "A finding's docs_missing item can be fulfilled by an in-flow vault upload that updates docs_captured/docs_missing (DOC-05)."
  artifacts:
    - "app/Events/TaxDocumentExtracted.php"
    - "app/Jobs/ExtractProfileFacts.php + app/Services/AI/PaystubFactExtractorService.php"
    - "app/Services/HsaShoeboxService.php"
    - "tests/Feature/PaystubProposalFlowTest.php (D4 gate, both directions)"
  key_links:
    - "ExtractTaxDocument::handle() ready path → event(new TaxDocumentExtracted($document)) + ExtractProfileFacts::dispatch for PayStub/BenefitsGuide"
    - "PaystubFactExtractorService → UserTaxFact::recordFact(sourceType: 'document_extraction') → is_current=false proposal, confidence in metadata"
    - "DurableFactsController::confirm() (existing) promotes a proposal to current — the only write path"
    - "DocumentRequest auto-fulfillment (v2.0) → finding.docs_captured / docs_missing update"
---

<objective>
Build the two missing chains RESEARCH identified: (1) the staleness event `TaxDocumentExtracted` fired at the end of extraction, and (2) the document→profile PROPOSAL bridge that turns extracted pay-stub/benefits-guide fields into confirm-gated `UserTaxFact` proposals. Then feed extracted signals into the snapshot (assembler), stand up the STORE-03 HSA shoebox, and wire the DOC-05 in-flow vault-upload fulfillment onto findings.

Purpose: DOC-03 (extraction feeds snapshot + emits the staleness signal), DOC-07 (D4 confirm-before-write "AI onboarding" proposals with per-field confidence), STORE-03 (HSA shoebox), DOC-05 (interview docs_missing wired to real vault upload).
Output: one event, one job, one extractor service, one shoebox service, an assembler extension, a DocumentRequest fulfillment hook, and end-to-end tests proving the D4 gate in both directions.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-CONTEXT.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-01-SUMMARY.md

# Source files (read before editing)
@app/Jobs/ExtractTaxDocument.php
@app/Models/UserTaxFact.php
@app/Http/Controllers/Api/DurableFactsController.php
@app/Services/IncomeOptimizerDataAssemblerService.php
@app/Models/IncomeOptimizationProfile.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaxDocumentExtracted event + fire on ready path + dispatch fact extraction (DOC-03)</name>
  <read_first>
    - app/Jobs/ExtractTaxDocument.php (the ready-path block that sets status=Ready ~line 138-157; failed(); duplicate detection)
    - app/Events/OptimizationProfileBuilt.php (event shape/constructor convention to mirror)
    - app/Enums/TaxDocumentCategory.php (PayStub, BenefitsGuide from 12-01)
  </read_first>
  <files>app/Events/TaxDocumentExtracted.php, app/Jobs/ExtractTaxDocument.php, tests/Feature/ExtractProfileFactsDispatchTest.php</files>
  <behavior>
    - When ExtractTaxDocument reaches the Ready status path, exactly one TaxDocumentExtracted event fires carrying the document (exposing user_id, tax_year, category, document id).
    - The event does NOT fire on the Failed, splitter, or below-gate paths (early returns).
    - For category PayStub or BenefitsGuide only, ExtractProfileFacts is dispatched with the document id.
  </behavior>
  <action>
    Create `App\Events\TaxDocumentExtracted` mirroring OptimizationProfileBuilt: Dispatchable + SerializesModels, readonly constructor taking `public readonly TaxDocument $document` (or the id + eager fields — match the OptimizationProfileBuilt convention). In `ExtractTaxDocument::handle()`, AFTER the `$document->update([... status => Ready])` block and audit log (the successful terminal path only), add `event(new TaxDocumentExtracted($document));` then, guarded by `in_array($document->category, [TaxDocumentCategory::PayStub, TaxDocumentCategory::BenefitsGuide], true)`, `ExtractProfileFacts::dispatch($document->id);`. Do not fire the event on any early-return (Failed/splitter/below-gate). No existing behavior changes.
    Test (ExtractProfileFactsDispatchTest): Event::fake()/Queue::fake(); run the ready path for a PayStub doc → assert TaxDocumentExtracted dispatched once and ExtractProfileFacts dispatched once; run for a W2 doc → assert TaxDocumentExtracted dispatched, ExtractProfileFacts NOT dispatched.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ExtractProfileFactsDispatchTest</automated>
  </verify>
  <done>TaxDocumentExtracted fires once on the ready path; ExtractProfileFacts dispatches only for PayStub/BenefitsGuide; no event on failure paths; existing ExtractTaxDocument tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaystubFactExtractorService + ExtractProfileFacts job (D4 proposals) + assembler feed (DOC-07, DOC-03)</name>
  <read_first>
    - app/Models/UserTaxFact.php (recordFact() named-arg signature; sourceType='document_extraction' ⇒ is_current=false; confirmProposal(); currentFactKeys() excludes unconfirmed proposals)
    - app/Http/Controllers/Api/DurableFactsController.php (confirm endpoint = the only promotion path)
    - app/Services/IncomeOptimizerDataAssemblerService.php (sumFromDocuments() switch ~line 167-293; existing arms read $data['field'] flat — dollarsToCents())
    - app/Models/IncomeOptimizationProfile.php ($fillable has w2_wages, traditional_401k_ytd, roth_401k_ytd, hsa_ytd — no migration needed)
    - .planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md ("Proposal/Confirm Bridge" — PAYSTUB_FACT_MAP / BENEFITS_FACT_MAP; Pitfall 2/3/7)
  </read_first>
  <files>app/Services/AI/PaystubFactExtractorService.php, app/Jobs/ExtractProfileFacts.php, app/Services/IncomeOptimizerDataAssemblerService.php, tests/Feature/PaystubProposalFlowTest.php</files>
  <behavior>
    - PayStub extraction with a traditional_401k_deduction field creates a UserTaxFact with fact_key 'retirement.traditional_401k_ytd_cents', is_current=false, source_type='document_extraction', metadata.confidence set from the field's confidence.
    - A proposal is EXCLUDED from IncomeOptimizationProfile::answerableFields() / currentFactKeys() until confirmed.
    - After DurableFactsController::confirm(), the fact becomes current and answerable.
    - A pre-existing user_edit/interview_answer fact for the same key is NOT overwritten when a proposal is created (only superseded after explicit confirm).
    - BenefitsGuide boolean fields persist as string 'yes'/'no' (original boolean in metadata).
  </behavior>
  <action>
    Create `App\Services\AI\PaystubFactExtractorService::proposeFacts(TaxDocument $document): int` using the PAYSTUB_FACT_MAP and BENEFITS_FACT_MAP from 12-RESEARCH.md (field → fact_key/label/volatility). Read fields as `$document->extracted_data['fields'][$fieldName]['value'] ?? null` and confidence as `['confidence'] ?? 0.5` (Pitfall 2: nested-with-confidence shape for this service). For each present field call `UserTaxFact::recordFact(userId: $document->user_id, factKey: ..., value: (string) $value, sourceType: 'document_extraction', label: ..., volatility: ..., taxYear: $document->tax_year, sourceId: (string) $document->id, metadata: ['confidence' => $confidence, 'document_id' => $document->id])`. CRITICAL (Pitfall 3, D4): sourceType MUST be 'document_extraction' so recordFact() writes is_current=false and never supersedes user-entered values. Money fields store integer-cents-as-string; benefits booleans store 'yes'/'no' (Pitfall 7), original bool in metadata. Return the count.
    Create `App\Jobs\ExtractProfileFacts implements ShouldQueue` (tries=3, timeout=120, onQueue('optimization')): load TaxDocument, call PaystubFactExtractorService::proposeFacts(), log count, failed() handler. Constructor takes `public readonly int $documentId`.
    Assembler: add PayStub and BenefitsGuide arms to `sumFromDocuments()` after the IRA_5498 arm. Follow the EXISTING arm convention in that switch (this method's `$data` is the normalized flat field map — read `$data['field'] ?? null` like the W2/1099 arms, NOT the nested shape). Accumulate PayStub YTD signals into traditional_401k_ytd / roth_401k_ytd / hsa_ytd / w2_wages via dollarsToCents(). BenefitsGuide is availability metadata only (no dollar accumulation). Additive — do not alter existing arms.
    Test (PaystubProposalFlowTest): (a) extract PayStub → assert proposals exist with is_current=false + source_type=document_extraction + metadata.confidence; (b) assert the proposed fact_key is NOT in UserTaxFact::currentFactKeys(); (c) seed a user_edit fact for the same key, create a proposal, assert the user_edit fact stays current (not superseded); (d) call the confirm endpoint → assert proposal becomes current and the old user_edit fact is superseded.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=PaystubProposalFlowTest</automated>
  </verify>
  <done>Proposals created with is_current=false + confidence metadata; invisible to skip-logic pre-confirm; confirm promotes them; user-entered values never silently overwritten; assembler surfaces pay-stub YTD signals.</done>
</task>

<task type="auto">
  <name>Task 3: HSA shoebox (STORE-03) + DOC-05 in-flow vault-upload fulfillment</name>
  <read_first>
    - app/Models/UserTaxFact.php (recordFact for namespaced fact_key)
    - app/Models/OptimizationFinding.php (docs_captured / docs_missing arrays; scopeForUser)
    - app/Http/Controllers/Api/DocumentRequestController.php + the v2.0 DocumentRequest auto-fulfillment path (from Phase 8) — reuse, do not fork
  </read_first>
  <files>app/Services/HsaShoeboxService.php, app/Http/Controllers/Api/DocumentRequestController.php, tests/Feature/HsaShoeboxTest.php</files>
  <action>
    STORE-03: create `App\Services\HsaShoeboxService` that records an out-of-pocket medical receipt as a UserTaxFact with a namespaced key `hsa_shoebox.<vault_document_id>` (volatility 'permanent'), value = amount-cents-as-string, metadata = {vault_document_id, incurred_on, description}. Education copy is a static string constant: expenses incurred AFTER the HSA was opened may be reimbursable tax-free in any future year with receipts — the service NEVER implies pre-establishment expenses qualify. Provide a listByUser(int $userId) returning the shoebox facts (value stays $hidden; expose amount from metadata for display). The listing is read via the existing GET /api/v1/optimizer/facts (fact_key prefix filter) — do not add a new route.
    DOC-05: extend DocumentRequestController's fulfillment path so that when an in-flow vault upload fulfills a request tied to an OptimizationFinding, the finding's docs_captured gains the vault document id and docs_missing drops the matching entry (scopeForUser-guarded). Reuse the v2.0 DocumentRequest auto-fulfillment — only add the finding docs_captured/docs_missing update. Additive; no change to existing DocumentRequest response shape.
    Tests (HsaShoeboxTest): assert a shoebox fact is created with fact_key prefix hsa_shoebox., permanent volatility, value $hidden from toArray(); assert the education copy constant matches the approved wording; assert a DOC-05 fulfillment moves an entry from docs_missing to docs_captured on the finding.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=HsaShoeboxTest</automated>
  </verify>
  <done>HSA shoebox facts persist with correct namespaced key + hidden value + accurate education copy; DOC-05 in-flow upload updates the finding's docs_captured/docs_missing; existing DocumentRequest tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extracted document field → durable-fact store | Untrusted extracted values could overwrite user-entered profile facts |
| in-flow upload → OptimizationFinding | Cross-user finding mutation risk on fulfillment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02-01 | Integrity/Elevation | Proposal silently overwriting user-entered fact | high | mitigate | source_type='document_extraction' forces is_current=false; confirmProposal() is the ONLY promotion path; PaystubProposalFlowTest asserts both directions. |
| T-12-02-02 | Information Disclosure | Encrypted fact value / SSN in extracted fields | high | mitigate | UserTaxFact.value is `encrypted` + `$hidden`; store money-cents-as-string; confidence-only in non-PII metadata. SSN-last-4 handling stays within existing sanitizeExtraction rules (P13/SAFE-04 audits it). |
| T-12-02-03 | Tampering | Cross-user finding mutation on DOC-05 fulfillment | medium | mitigate | scopeForUser() on the finding lookup; reuse the authorized v2.0 DocumentRequest fulfillment path. |
| T-12-02-04 | DoS | ExtractProfileFacts fan-out | low | accept | One job per extracted PayStub/BenefitsGuide; report-generation debounce lives in 12-04. |
| T-12-02-SC | Tampering | npm/composer installs | low | accept | No packages installed (RESEARCH audit: none). |
</threat_model>

<verification>
- `php artisan test --compact --filter='ExtractProfileFacts|PaystubProposal|HsaShoebox'` green.
- Full suite zero NEW failures; `vendor/bin/pint --dirty` clean.
- Manual grep: no `estimated_value_cents` assignment introduced (SAFE-03 gate stays green).
</verification>

<success_criteria>
DOC-03, DOC-07, STORE-03, DOC-05 satisfied: staleness event fires; the confirm-gated proposal bridge works end to end in both directions; assembler consumes pay-stub signals; HSA shoebox and in-flow doc fulfillment operate additively with no interface changes.
</success_criteria>

<output>
Create `.planning/phases/12-optimization-report-document-intake-feature-surface/12-02-SUMMARY.md` when done.
</output>
