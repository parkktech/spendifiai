---
phase: 12-optimization-report-document-intake-feature-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Models/OptimizationReport.php
  - database/migrations/2026_07_02_200000_create_optimization_reports_table.php
  - app/Services/OptimizationReportGeneratorService.php
  - app/Services/OptimizationReportNarratorService.php
  - app/Services/OptimizationReportExportService.php
  - app/Jobs/GenerateOptimizationReport.php
  - config/optimization-report.php
  - resources/views/pdf/optimization-report.blade.php
  - tests/Feature/OptimizationReportGeneratorTest.php
  - tests/Feature/OptimizationReportDisclaimerTest.php
  - tests/Feature/OptimizationReportPdfSmokeTest.php
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, RPT-06, RPT-07, RPT-08]
must_haves:
  truths:
    - "A user's optimization report assembles four ranked topical sections (deductions / taxes / filings / 401k-retirement) plus three wrapper sections (documents missing / needs professional review / what we refused and why) where every dollar figure originates from OptimizationFinding/TaxRulesEngineService — Claude writes only prose."
    - "The report is produced by a background job, stored in an OptimizationReport model, and exportable to PDF via the existing dompdf pipeline."
    - "Every report section carries a persistent, non-globally-dismissable educational disclaimer, and a year-end awareness section surfaces deadline-metadata items with 'commonly reviewed before year end' framing (no deadline pressure)."
    - "The report glossary renders the owner-approved RIA-reframed educational lines exactly as approved."
  artifacts:
    - "app/Models/OptimizationReport.php + additive migration (unique user_id+tax_year, is_stale/stale_since/rebuilt_at)"
    - "app/Services/OptimizationReportGeneratorService.php + OptimizationReportNarratorService.php (3rd/final Claude call site)"
    - "app/Jobs/GenerateOptimizationReport.php (ShouldBeUnique)"
    - "resources/views/pdf/optimization-report.blade.php + config/optimization-report.php"
  key_links:
    - "GeneratorService reads finding.estimated_value_cents for ranking (READ only) — never assigns it (SAFE-03 grep-gate stays green)"
    - "NarratorService payload excludes every monetary column (mirrors NarrationService) — 3rd and FINAL Claude call site of the milestone"
    - "GenerateOptimizationReport::uniqueId() = user:tax_year prevents thundering-herd rebuilds"
---

<objective>
Build the report engine: the `OptimizationReport` model, the deterministic generator that ranks and sections existing `OptimizationFinding` records, the third-and-final permitted Claude call site (section narrator), the queued generation job, and the dompdf PDF export with per-section disclaimers, glossary, and year-end awareness. Numbers come only from findings/TaxRulesEngineService; Claude writes prose only.

Purpose: RPT-01 (ranked 4-section report), RPT-02 (queued job + model), RPT-03 (persistent per-section disclaimers), RPT-04 (dompdf export), RPT-06 (three wrapper sections incl. the refusal trust feature), RPT-07 (educational glossary), RPT-08 (year-end awareness section).
Output: model + migration, three services, one job, one config file, one Blade view, and generator/disclaimer/PDF tests. No dependency on the doc-intake or staleness wiring — the staleness listeners and report API live in 12-04.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-CONTEXT.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md

# Source patterns to mirror (read before writing)
@app/Models/OptimizationFinding.php
@app/Services/NarrationService.php
@app/Jobs/BuildIncomeOptimizationProfile.php
@tests/Unit/EstimatedValueGuardTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: OptimizationReport model + additive migration (RPT-01/02 storage)</name>
  <read_first>
    - app/Models/OptimizationFinding.php ($fillable, casts(), scopeForUser(), $hidden)
    - .planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md ("OptimizationReport Model" — migration + model; Pitfall 8 firstOrCreate)
    - CLAUDE.md safety rules (additive/forward-only migration; TEXT for encrypted; encrypted fields must be TEXT in Postgres)
  </read_first>
  <files>app/Models/OptimizationReport.php, database/migrations/2026_07_02_200000_create_optimization_reports_table.php</files>
  <action>
    Additive migration `create_optimization_reports_table`: id, foreignId user_id constrained cascadeOnDelete, integer tax_year, jsonb sections, text executive_summary nullable, boolean is_stale default true, timestamp stale_since nullable, timestamp rebuilt_at nullable, timestamps; unique(['user_id','tax_year']); index(['user_id','is_stale']). Forward-only, no drops. Model `App\Models\OptimizationReport`: $fillable for all writable columns, $hidden = [] (no PII columns — sections carry no raw dollar amounts, only finding ids + prose), casts sections=>array, is_stale=>boolean, stale_since/rebuilt_at=>datetime; belongsTo user; `scopeForUser(Builder, int $userId)` mirroring OptimizationFinding (T-10-09 access control). Provide a static/helper to fetch-or-init via `firstOrCreate(['user_id','tax_year'], ['is_stale'=>true,'sections'=>[]])` (Pitfall 8 — callers always get a model).
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | grep -qi 'create table .*optimization_reports' && php artisan test --compact --filter=OptimizationReportGeneratorTest</automated>
  </verify>
  <done>Migration is additive (CREATE TABLE only, no DROP); model exposes scopeForUser + firstOrCreate init; unique(user_id, tax_year) present.</done>
</task>

<task type="auto">
  <name>Task 2: Generator + Narrator (3rd Claude call site) + job (RPT-01/02/06/07/08)</name>
  <read_first>
    - app/Services/NarrationService.php (SYSTEM_PROMPT, callClaude() retry pattern, json_encode payload, EXCLUSION of estimated_value_cents/money — mirror EXACTLY)
    - app/Models/OptimizationFinding.php (severity, band, finding_type, legal_basis, docs_missing, deadline, estimated_value_cents READ-only for ranking)
    - app/Jobs/BuildIncomeOptimizationProfile.php (job shape: tries/timeout/onQueue/failed())
    - tests/Unit/EstimatedValueGuardTest.php (the SAFE-03 grep-gate that fails the build if any file outside TaxRulesEngineService assigns estimated_value_cents)
    - .planning/reference/INTEGRATION-MAP.md (RPT-06 refusal section = WHAT/WHY only never HOW; RPT-07 approved glossary lines under default ruling R1; Blocked list lines 308-331)
  </read_first>
  <files>app/Services/OptimizationReportGeneratorService.php, app/Services/OptimizationReportNarratorService.php, app/Jobs/GenerateOptimizationReport.php, config/optimization-report.php</files>
  <action>
    Create `config/optimization-report.php` holding STATIC content only: the four topical section definitions + finding_type→section mapping; the RPT-07 glossary lines (only the owner-approved reframes: 0% LTCG bracket-awareness line — no sell/rebuy language; tax-loss-harvesting facts $3K offset + wash-sale rule; account-type-taxation facts with no asset classes named; stepped-up-basis line; "MFS may be worth modeling with your preparer" ceiling line; plus the avoidance-vs-evasion + killer-doctrines explainer, PB-v1 §9); the RPT-06 refusal list rendered as WHAT was refused + WHY at a high level ONLY — never HOW any blocked scheme works (source the Blocked-list items by name from INTEGRATION-MAP without describing mechanics); and the per-section disclaimer text.
    `OptimizationReportGeneratorService::generate(User $user, int $taxYear): OptimizationReport`: load OptimizationFinding::forUser()->where tax_year; group findings into the 4 topical sections via the config map; rank each section by severity tier then estimated_value_cents DESC (READ only — never assign it; SAFE-03). Build the 3 RPT-06 wrapper sections: "documents missing" (aggregate every finding's docs_missing), "needs professional review" (specialist-band findings), "what we refused and why" (config refusal list). Add the RPT-08 year-end awareness section from findings carrying deadline metadata (Dec-31 items framed "commonly reviewed before year end"; Jan-15/April items; state-layer items suppressed) — educational surfacing, NO countdown/pressure. Attach the RPT-07 glossary. Every section object carries a disclaimer string (RPT-03). Call NarratorService once per section for the overview prose; upsert via OptimizationReport::updateOrCreate(['user_id','tax_year'], [...]); set is_stale=false, rebuilt_at=now().
    `OptimizationReportNarratorService::narrateSection(array $sectionContext): ?string` — mirror NarrationService EXACTLY: same callClaude() retry, max_tokens 256, SAFE-01 SYSTEM_PROMPT (educational may/could/consider; never dollar amounts; never "you qualify/owe/should"; never filing-status/securities advice; end with "Consider discussing these items with a tax professional."). Payload = json_encode of {section_title, findings:[{finding_type, severity, band, legal_basis}]} ONLY — deliberately EXCLUDE estimated_value_cents, net_cash_cost, tax_saved, cliff_bonus_value and every monetary column (this is the 3rd and FINAL permitted Claude call site of the milestone). The service WRITES only prose (section overview / executive_summary) — never any finding money column.
    `GenerateOptimizationReport implements ShouldQueue, ShouldBeUnique` (tries=3, timeout=300, onQueue('optimization'), constructor `public readonly int $userId, public readonly int $taxYear`): `uniqueId()` returns "{$this->userId}:{$this->taxYear}" (Pitfall 4 thundering-herd); handle() injects the generator and generates; failed() logs.
    Tests: OptimizationReportGeneratorTest — assert 4 topical + 3 wrapper + year-end sections; ranking order (severity then value DESC) using seeded findings with known estimated_value_cents; assert the narrator payload contains no monetary keys (mock/inspect the encoded payload); assert every number in the produced report traces to a finding's value (no Claude-sourced figure). Keep EstimatedValueGuardTest green (do not assign estimated_value_cents anywhere).
  </action>
  <verify>
    <automated>php artisan test --compact --filter='OptimizationReportGeneratorTest|EstimatedValueGuardTest'</automated>
  </verify>
  <done>Report has 4 topical + 3 wrapper + year-end sections ranked correctly; narrator payload excludes all money fields; SAFE-03 grep-gate green; job is ShouldBeUnique on user:tax_year; glossary + refusal content sourced from config.</done>
</task>

<task type="auto">
  <name>Task 3: PDF export + Blade view + per-section disclaimers (RPT-03/04)</name>
  <read_first>
    - app/Services/TaxExportService.php (dompdf pattern: Pdf::loadView() → setPaper('letter','portrait') → save(); memory handling)
    - resources/views/pdf/tax-summary.blade.php (inline-CSS-only structure to mirror)
    - .planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md ("PDF Export" — ini_set memory 512M; Pitfall 5)
  </read_first>
  <files>app/Services/OptimizationReportExportService.php, resources/views/pdf/optimization-report.blade.php, tests/Feature/OptimizationReportPdfSmokeTest.php, tests/Feature/OptimizationReportDisclaimerTest.php</files>
  <action>
    `OptimizationReportExportService::generatePdf(OptimizationReport $report): string` — mirror TaxExportService: `ini_set('memory_limit','512M')` before load (Pitfall 5); `Pdf::loadView('pdf.optimization-report', [...flattened data..., 'disclaimer' => <persistent educational disclaimer>])`; `->setPaper('letter','portrait')->save($path)`; throw RuntimeException if the file is missing. Flatten the sections array before passing (no nested loops over large collections in Blade).
    Create `resources/views/pdf/optimization-report.blade.php` with INLINE CSS ONLY (dompdf cannot fetch external resources in queued jobs) mirroring tax-summary.blade.php: render each section with its heading, narrator prose, finding rows, and a per-section educational disclaimer bar (RPT-03) plus a document-level disclaimer footer. Include the RPT-07 glossary and RPT-08 year-end sections.
    Tests: OptimizationReportPdfSmokeTest — generate a PDF for a seeded report, assert the file is non-empty and begins with the `%PDF-` byte signature. OptimizationReportDisclaimerTest — assert every section object produced by the generator carries a non-empty disclaimer string, and the rendered Blade output contains the disclaimer text for each section (RPT-03 persistent, not globally dismissable).
  </action>
  <verify>
    <automated>php artisan test --compact --filter='OptimizationReportPdfSmokeTest|OptimizationReportDisclaimerTest'</automated>
  </verify>
  <done>PDF renders non-empty and begins with %PDF-; memory guard present; Blade uses inline CSS only; every section renders a persistent educational disclaimer (RPT-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding data → Claude narrator | Dollar figures must never reach Claude or be echoed as claims |
| report query → user | Cross-user report disclosure risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03-01 | Integrity | Dollar figures echoed via report narration (SAFE-03) | high | mitigate | NarratorService payload excludes every monetary column; generator READS estimated_value_cents for ranking but never assigns it; EstimatedValueGuardTest grep-gate fails build on violation. |
| T-12-03-02 | Information Disclosure | Cross-user report access | high | mitigate | OptimizationReport::scopeForUser() on all queries; report API owner-check added in 12-04. |
| T-12-03-03 | Tampering | Prompt injection via finding-derived strings in narrator payload | medium | mitigate | All finding-derived content json_encode()'d into a structured object, never interpolated into the system prompt (mirror NarrationService). |
| T-12-03-04 | DoS | dompdf memory exhaustion on long reports | medium | mitigate | ini_set memory 512M; flattened Blade data; inline CSS only. |
| T-12-03-SC | Tampering | npm/composer installs | low | accept | No packages installed (RESEARCH audit: none — dompdf pre-installed). |
</threat_model>

<verification>
- `php artisan test --compact --filter='OptimizationReport|EstimatedValueGuard'` green.
- Full suite zero NEW failures; `vendor/bin/pint --dirty` clean.
- Migration additive (CREATE TABLE only) — never runs migrate:fresh/rollback.
</verification>

<success_criteria>
RPT-01/02/03/04/06/07/08 satisfied: ranked 4-topical + 3-wrapper + year-end report generated by a unique-keyed queued job, stored in the model, exported to PDF, with persistent per-section disclaimers, an approved glossary, and numbers proven to originate from findings/engine (SAFE-03 gate green).
</success_criteria>

<output>
Create `.planning/phases/12-optimization-report-document-intake-feature-surface/12-03-SUMMARY.md` when done.
</output>
