---
phase: 12-optimization-report-document-intake-feature-surface
plan: 05
type: execute
wave: 4
depends_on: [12-02, 12-04]
files_modified:
  - resources/js/Layouts/AuthenticatedLayout.tsx
  - routes/web.php
  - resources/js/Pages/Optimize/Index.tsx
  - resources/js/Components/SpendifiAI/OptimizationReportView.tsx
  - resources/js/Pages/UserProfile/Index.tsx
  - resources/js/Components/SpendifiAI/ProposalConfirmCard.tsx
  - resources/js/Components/SpendifiAI/AiOnboardingUploadSection.tsx
  - resources/js/Components/SpendifiAI/FamilyHouseholdSection.tsx
  - resources/js/Components/SpendifiAI/HsaShoeboxSection.tsx
  - resources/js/Components/SpendifiAI/DocumentUploadFlow.tsx
  - tests/Feature/OptimizeSurfaceRouteTest.php
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "A user sees a new 'Optimize My Income' nav item (badged with pending red-flag questions) and a new 'My Profile' nav item — both additive, nothing existing renamed or moved."
    - "A user navigates the Optimize My Income page through findings → guided interview (reused P11 InterviewCard/SuggestedConfirmCard) → the ranked report view with collapsible sections."
    - "A user completes the AI-onboarding flow on the User/Family Profile page: upload paystub → per-field proposal cards with confidence → confirm/edit/reject each → profile + facts update (D4 confirm gate through the existing DurableFactsController)."
    - "Every optimization surface uses educational 'may/could/consider' framing and renders an inline, non-globally-dismissable disclaimer."
  artifacts:
    - "resources/js/Pages/Optimize/Index.tsx + OptimizationReportView.tsx"
    - "resources/js/Pages/UserProfile/Index.tsx (composes existing EnhancedProfileSection + LearnedTaxFactsSection unchanged + new sections)"
    - "resources/js/Components/SpendifiAI/ProposalConfirmCard.tsx + AiOnboardingUploadSection.tsx + HsaShoeboxSection.tsx + FamilyHouseholdSection.tsx"
    - "/optimize and /user-profile web routes (no /profile collision)"
  key_links:
    - "AuthenticatedLayout badge ← auth.pendingOptimizationCount (12-04 prop)"
    - "ProposalConfirmCard → POST /api/v1/optimizer/facts/{fact}/confirm (existing DurableFactsController — D4 gate)"
    - "OptimizationReportView ← GET /api/v1/optimizer/report/{year} (12-04)"
    - "/user-profile route distinct from Breeze /profile (Pitfall 6)"
---

<objective>
Ship the user-facing Optimize My Income surface and the User/Family Profile page (owner Decision 5, option (a)), elevating the existing sw-* design system without replacing it. Reuse the shipped P11 interview components and the existing profile sections; the new work is composition, the report view, the proposal-confirm onboarding UX, and additive nav.

Purpose: UI-01 (two additive nav items + badge), UI-02 (Optimize page: findings → interview → report), UI-03 (educational framing + inline disclaimers on every surface), plus the D5 User/Family Profile page hosting the AI-onboarding upload flow, learned facts, family/household facts, HSA shoebox, and new-doc-type upload.
Output: nav + two routes, two Inertia pages, five new components, and route smoke tests — with the D6 taste-skill v2 blocking audits recorded in the SUMMARY.

## D6 FRONTEND MANDATE (BINDING — applies to EVERY task in this plan)
Before implementing each task: consult `python3 .claude/skills/ui-ux-pro-max/scripts/search.py "<relevant query>"` AND apply `/frontend-design:frontend-design` principles AND soft-skill (high-end visual design) AND taste-skill. ELEVATE DON'T REPLACE: the sw-* tokens in resources/css/app.css + self-hosted Inter + StatCard/Badge/card patterns are the baseline to polish — no palette swaps, no restyles of existing pages. Record in the task log / SUMMARY the taste-skill v2 BLOCKING AUDITS (any Fail blocks completion): (1) em-dash audit, (2) Pre-Flight Check §14, (3) Preservation audit — list every URL / nav label / form field / anchor changed; it MUST be empty EXCEPT the two plan-granted nav additions ("Optimize My Income", "My Profile") and the two new routes /optimize + /user-profile, (4) Brand fidelity audit (sw-* accent, Inter stack, logo treatment survived). Existing components imported unchanged (InterviewCard, SuggestedConfirmCard, EnhancedProfileSection, LearnedTaxFactsSection, FileDropZone, StatementUploadWizard, ConnectBankPrompt) MUST NOT be modified.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-CONTEXT.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-PATTERNS.md
@.planning/reference/enhanced-profile-integration-notes.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-02-SUMMARY.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-04-SUMMARY.md

# Frontend patterns to mirror / reuse UNCHANGED (read before writing)
@resources/js/Layouts/AuthenticatedLayout.tsx
@resources/js/Pages/Questions/Index.tsx
@resources/js/Pages/Settings/Index.tsx
@resources/js/Components/SpendifiAI/SuggestedConfirmCard.tsx
@resources/js/Components/SpendifiAI/StatementUploadWizard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive nav (Optimize My Income badged + My Profile) + web routes (UI-01)</name>
  <read_first>
    - resources/js/Layouts/AuthenticatedLayout.tsx (NavItemDef badge field line 32; NavItem badge render lines 55-59; navItems array lines 96-113; lucide imports lines 4-25)
    - routes/web.php (auth:sanctum+verified Inertia group lines 56-72; the Breeze /profile route line 93 — DO NOT collide)
  </read_first>
  <files>resources/js/Layouts/AuthenticatedLayout.tsx, routes/web.php, tests/Feature/OptimizeSurfaceRouteTest.php</files>
  <action>
    Apply the D6 FRONTEND MANDATE (see objective). Add TWO nav items to the navItems array (additive; nothing existing renamed/reordered): "Optimize My Income" → href /optimize, routeName 'optimize', icon TrendingUp, badge `(auth.pendingOptimizationCount as number) || undefined` (prop from 12-04); "My Profile" → href /user-profile, routeName 'user-profile', icon User. Import TrendingUp + User from lucide-react (both installed). The existing NavItem badge render already handles the pill — reuse it, no change.
    routes/web.php (inside the existing auth:sanctum+verified group): `Route::get('/optimize', fn () => Inertia::render('Optimize/Index'))->name('optimize');` and `Route::get('/user-profile', fn () => Inertia::render('UserProfile/Index'))->name('user-profile');`. Use /user-profile NOT /profile (Breeze collision, Pitfall 6).
    Test (OptimizeSurfaceRouteTest): an authenticated+verified user GET /optimize returns 200 and the Inertia component 'Optimize/Index'; GET /user-profile returns 200 and 'UserProfile/Index'; assert /profile (Breeze) still resolves to profile.edit (no regression).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=OptimizeSurfaceRouteTest && npm run build</automated>
  </verify>
  <done>Two additive nav items render (Optimize badged); /optimize + /user-profile resolve to the new Inertia pages; Breeze /profile intact; npm build clean; taste-skill v2 audits recorded (preservation audit lists only the two granted nav items + two routes).</done>
</task>

<task type="auto">
  <name>Task 2: Optimize My Income page — findings → interview → report view (UI-02, UI-03)</name>
  <read_first>
    - resources/js/Pages/Questions/Index.tsx (page shell: useApi enabled+hasBankConnected guard, ConnectBankPrompt, AuthenticatedLayout header, loading state)
    - resources/js/Components/SpendifiAI/SuggestedConfirmCard.tsx + the P11 InterviewCard (reuse UNCHANGED for the interview stage)
    - 12-04-SUMMARY.md (report API shape: GET /api/v1/optimizer/report/{year}, regenerate, generating state; findings API)
  </read_first>
  <files>resources/js/Pages/Optimize/Index.tsx, resources/js/Components/SpendifiAI/OptimizationReportView.tsx</files>
  <action>
    Apply the D6 FRONTEND MANDATE. Build `Pages/Optimize/Index.tsx` mirroring Questions/Index.tsx: guard with auth.hasBankConnected (ConnectBankPrompt when false); a three-stage view (findings list → guided interview → report) using a viewMode state. Findings stage: list OptimizationFinding records (existing findings API) ranked by severity with "what we noticed → why it may matter → what to bring to a professional → source" framing (UI-03 educational 'may/could/consider'). Interview stage: REUSE the P11 InterviewCard + SuggestedConfirmCard unchanged (one-question-at-a-time). Report stage: render the new `OptimizationReportView` component fed by GET /api/v1/optimizer/report/{year} — collapsible sections (the 4 topical + 3 wrapper + year-end), each with an inline non-globally-dismissable educational disclaimer (UI-03), a "generating" state while is_stale/empty, and a regenerate + download-PDF action. DOC-05 affordance: where a finding lists docs_missing, surface an in-flow upload control (reuse FileDropZone) that posts to the existing vault upload / DocumentRequest fulfillment (12-02) without leaving the page.
    The "Tax Savings Control Center" 12-surface concept is the FRAMING north star (organize by surfaces the user can act on) — not a literal 12-module build. Use ui-ux-pro-max for the findings-list + collapsible-report layout and any chart choice; harmonize with recharts/sw-* only.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Optimize page renders findings → interview (reused components) → collapsible report with per-section disclaimers, generating/regenerate/download states, and an in-flow docs_missing upload; educational framing throughout; npm build clean; taste-skill v2 audits recorded (preservation audit empty).</done>
</task>

<task type="auto">
  <name>Task 3: User/Family Profile page — AI onboarding, learned facts, family, HSA shoebox, new-doc upload (D5, UI-03)</name>
  <read_first>
    - resources/js/Pages/Settings/Index.tsx (multi-section page layout; useApi/useApiPost profile pattern)
    - resources/js/Components/SpendifiAI/EnhancedProfileSection.tsx + LearnedTaxFactsSection.tsx (import UNCHANGED)
    - resources/js/Components/SpendifiAI/StatementUploadWizard.tsx + FileDropZone.tsx (reuse pattern for DocumentUploadFlow)
    - 12-02-SUMMARY.md (proposal facts: is_current=false + metadata.confidence; confirm via POST /api/v1/optimizer/facts/{fact}/confirm; HSA shoebox facts hsa_shoebox.* via GET /api/v1/optimizer/facts)
  </read_first>
  <files>resources/js/Pages/UserProfile/Index.tsx, resources/js/Components/SpendifiAI/ProposalConfirmCard.tsx, resources/js/Components/SpendifiAI/AiOnboardingUploadSection.tsx, resources/js/Components/SpendifiAI/FamilyHouseholdSection.tsx, resources/js/Components/SpendifiAI/HsaShoeboxSection.tsx, resources/js/Components/SpendifiAI/DocumentUploadFlow.tsx</files>
  <action>
    Apply the D6 FRONTEND MANDATE. Build `Pages/UserProfile/Index.tsx` (owner Decision 5, option (a)) composing, in one page: the existing `EnhancedProfileSection` and `LearnedTaxFactsSection` imported UNCHANGED (do not modify them); a new `FamilyHouseholdSection` (spouse/dependents/household facts — read-only surfacing of existing household/dependent data, additive); the `AiOnboardingUploadSection`; and the `HsaShoeboxSection`. Settings keeps account/security — do NOT move anything off Settings; this is an additive page. UI-03: inline educational disclaimer on the page and each new section.
    `AiOnboardingUploadSection`: "fastest path to a complete profile is uploading a paystub" — embed `DocumentUploadFlow` (reuse StatementUploadWizard/FileDropZone pattern; accept the new doc-type categories from 12-01, incl. images) → on extraction, poll GET /api/v1/optimizer/facts and render a `ProposalConfirmCard` per proposed field.
    `ProposalConfirmCard`: shows the proposed fact label + value + per-field confidence (from metadata.confidence) with confirm / edit / reject — confirm calls POST /api/v1/optimizer/facts/{fact}/confirm (existing DurableFactsController, D4 gate); reject discards; edit routes through supersede. Make explicit in copy that nothing is written until the user confirms (never silently overwrite).
    `HsaShoeboxSection`: list hsa_shoebox.* facts (GET /api/v1/optimizer/facts prefix) with the accurate post-establishment-only education copy from 12-02; allow adding a receipt (upload → shoebox).
    `DocumentUploadFlow`: multi-step wizard (type → upload → processing → review) reusing FileDropZone; accept pdf/png/jpg/jpeg for the new financial + substantiation categories.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User/Family Profile page composes existing sections unchanged + AI-onboarding proposal-confirm flow (D4 gate via existing endpoint) + family + HSA shoebox + new-doc upload; nothing removed from Settings; npm build clean; taste-skill v2 audits recorded (preservation audit empty except the granted additions; EnhancedProfileSection/LearnedTaxFactsSection unmodified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client confirm action → durable-fact store | User must be the only actor promoting a proposal |
| in-flow / onboarding upload → vault | File upload from the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05-01 | Elevation | Proposal auto-confirmed without explicit user action | high | mitigate | Confirm only via the existing DurableFactsController::confirm() (D4 gate, owner-scoped); UI never auto-confirms; copy states nothing writes until confirmed. |
| T-12-05-02 | Information Disclosure | Educational surface implying tax advice / dollar guarantees | high | mitigate | UI-03 'may/could/consider' framing + inline persistent disclaimer on every surface; no computed dollar guarantees rendered; report numbers come from the API only. |
| T-12-05-03 | Tampering | Modifying existing shared components (brand/behavior drift) | medium | mitigate | Preservation + brand-fidelity taste-skill audits; imported P11/profile components unchanged; routes/nav labels unchanged except the two granted additions. |
| T-12-05-04 | Tampering | Screenshot/vision upload injection | medium | mitigate | Uploads flow through the existing vault extraction path (12-01/02); prompt-injection pen test deferred to P13/SAFE-07 (documented). |
| T-12-05-SC | Tampering | npm installs | low | accept | No packages installed (RESEARCH audit: none; TrendingUp/User already in lucide-react). |
</threat_model>

<verification>
- `php artisan test --compact --filter=OptimizeSurfaceRouteTest` green; `npm run build` clean (zero TS errors).
- Full suite zero NEW failures; `vendor/bin/pint --dirty` clean.
- taste-skill v2 blocking audits recorded IN WRITING in 12-05-SUMMARY (em-dash, Pre-Flight §14, Preservation empty-except-granted, Brand fidelity) — any Fail blocks completion.
- OWNER REVIEW of the full UI is a standing order: P12 HOLDS unshipped/unpushed until the owner reviews.
</verification>

<success_criteria>
UI-01/02/03 + D5 satisfied: two additive nav items (Optimize badged, My Profile); Optimize page runs findings → reused interview → collapsible disclaimer-bearing report; User/Family Profile page hosts the D4-gated AI-onboarding proposal-confirm flow, learned facts, family, HSA shoebox, and new-doc upload — all elevating (not replacing) the sw-* system, with npm build clean and the taste-skill audits passing.
</success_criteria>

<output>
Create `.planning/phases/12-optimization-report-document-intake-feature-surface/12-05-SUMMARY.md` when done.
</output>
