---
phase: 12-optimization-report-document-intake-feature-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/TaxDocumentCategory.php
  - app/Services/AI/TaxDocumentExtractorService.php
  - tests/Feature/TaxDocumentCategoryAdditivityTest.php
  - tests/Feature/TaxDocumentExtractorServiceTest.php
autonomous: true
requirements: [DOC-01, DOC-02, DOC-06]
must_haves:
  truths:
    - "A user can classify and extract six new financial document types (pay stub, offer letter, retirement statement, stock statement, insurance doc, benefits guide) through the existing two-pass vault pipeline."
    - "A user can upload substantiation documents (sponsorship agreement, physician letter, appraisal, gallons log, mileage log, daycare license, etc.) as recognized vault categories."
    - "A user's screenshot/image upload (JPEG/PNG) of any new doc type is extracted through the existing Claude vision branch with no new library."
  artifacts:
    - "app/Enums/TaxDocumentCategory.php with 18 additive cases + label() arms"
    - "app/Services/AI/TaxDocumentExtractorService.php with 6 new FIELDS const arrays + getFieldSchema() arms"
    - "tests/Feature/TaxDocumentCategoryAdditivityTest.php (enum-additivity grep-gate)"
  key_links:
    - "TaxDocumentCategory::tryFrom() in ExtractTaxDocument::handle() resolves the new cases → classification succeeds"
    - "getFieldSchema() returns a non-empty field list for every new financial case; substantiation cases fall through to TIER2_FIELDS default"
---

<objective>
Extend the shipped v2.0 Tax Document Vault two-pass classify→extract pipeline with the new Phase 12 document types — additively, changing zero existing behavior. This is the data-plane foundation the proposal bridge (12-02) and report engine consume.

Purpose: DOC-01 (six financial statement types), DOC-06 (substantiation categories linkable from a finding's docs checklist), and DOC-02 (image/screenshot extraction via the already-present vision branch) require only additive enum cases and extraction schemas — no new pipeline, no new library.
Output: 18 new `TaxDocumentCategory` cases, 6 new extraction field schemas, and additivity/regression tests proving the existing 25 form types are untouched.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-CONTEXT.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md

# Source files to extend (read before editing)
@app/Enums/TaxDocumentCategory.php
@app/Services/AI/TaxDocumentExtractorService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 financial + 12 substantiation TaxDocumentCategory cases (DOC-01, DOC-06)</name>
  <read_first>
    - app/Enums/TaxDocumentCategory.php (all 25 existing cases + label() match + forGrid())
    - app/Jobs/ExtractTaxDocument.php (how TaxDocumentCategory::tryFrom() consumes classification['category'])
  </read_first>
  <files>app/Enums/TaxDocumentCategory.php, tests/Feature/TaxDocumentCategoryAdditivityTest.php</files>
  <action>
    Append (never reorder or remove existing cases) after `CharitableDonation`. DOC-01 financial data plane: PayStub='pay_stub', OfferLetter='offer_letter', RetirementStatement='retirement_statement', StockStatement='stock_statement', InsuranceDoc='insurance_doc'. DOC-07 data plane: BenefitsGuide='benefits_guide'. DOC-06 substantiation: SponsorshipAgreement='sponsorship_agreement', MarketCompMemo='market_comp_memo', PhysicianLetter='physician_letter', Appraisal='appraisal', GallonsLog='gallons_log', RescueOrgLetter='rescue_org_letter', SecurityMemo='security_memo', LoanDoc='loan_doc', ContractorInvoice='contractor_invoice', MileageLog='mileage_log', DaycareLicense='daycare_license', SponsorshipVendorEvidence='sponsorship_vendor_evidence'. Add a human-readable arm for every new case to the `label()` match. Because `forGrid()` iterates `self::cases()` the new types surface in the vault UI automatically. Per D7: additive only — no case renamed, no existing string value changed.
    Create the additivity test: assert all 25 pre-existing case values still resolve via `tryFrom()`, that the new 18 values resolve, and that `label()` throws no UnhandledMatchError for any `self::cases()` member (iterate every case and call label()).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TaxDocumentCategoryAdditivityTest</automated>
  </verify>
  <done>All 43 cases resolve via tryFrom(); label() returns a non-empty string for every case; the 25 original values are byte-identical to their pre-change strings (regression assertion passes).</done>
</task>

<task type="auto">
  <name>Task 2: Add extraction field schemas + confirm vision branch (DOC-01, DOC-07 schema, DOC-02)</name>
  <read_first>
    - app/Services/AI/TaxDocumentExtractorService.php (TIER2_FIELDS ~line 95, getFieldSchema() ~line 378, buildDocumentContent() image branch ~line 399-430, extract()/classify())
    - .planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md ("Two-Pass Pipeline Extension" — exact FIELDS arrays)
  </read_first>
  <files>app/Services/AI/TaxDocumentExtractorService.php, tests/Feature/TaxDocumentExtractorServiceTest.php</files>
  <action>
    Add six new `const *_FIELDS` arrays after TIER2_FIELDS using the exact field lists in 12-RESEARCH.md: PAY_STUB_FIELDS (employer_name, employer_ein, employee_name, ssn_last4, pay_period_start/end, pay_date, gross_pay, federal/state/fica/medicare withheld, hsa_deduction, traditional_401k_deduction, roth_401k_deduction, fsa_deduction, health/dental_vision premiums, ytd_gross, ytd_federal_tax, tax_year), BENEFITS_GUIDE_FIELDS (employer_name, plan_year, has_401k, employer_match_formula, after_tax_401k_available, in_plan_roth_conversion_available, hdhp_hsa_available, fsa_available, dependent_care_fsa_available, espp_available, espp_terms, nqdc_available, section_127_available, commuter_benefits_available, group_legal_available, trump_account_available, employer_trump_account_contribution), OFFER_LETTER_FIELDS, RETIREMENT_STATEMENT_FIELDS, STOCK_STATEMENT_FIELDS, INSURANCE_FIELDS. Add one arm per new financial case to the `getFieldSchema()` match, mapping each case to its FIELDS const. DOC-06 substantiation cases intentionally have NO arm — they fall through to the existing `default => self::TIER2_FIELDS` (freeform substantiation extraction). Do not touch existing arms.
    DOC-02: `buildDocumentContent()` already branches to `type: image` with base64 for JPEG/PNG — do NOT add a library. Extend TaxDocumentExtractorServiceTest to assert buildDocumentContent() returns a content block of type image for a png/jpeg mime_type fixture, and that getFieldSchema() returns a non-empty array for each of the six new financial cases and TIER2_FIELDS for a substantiation case.
    SAFE-note (carried, hardened in P13): keep all document content inside the existing `<document_content>` delimiter pattern already used by extract(); do not interpolate extracted text into any system prompt.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TaxDocumentExtractorServiceTest</automated>
  </verify>
  <done>getFieldSchema() returns the correct non-empty schema for every new financial case; substantiation cases return TIER2_FIELDS; the image branch test passes for JPEG and PNG; existing extractor tests still pass (zero new failures).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded document → Claude classifier/extractor | Untrusted file text/image crosses into the LLM call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01-01 | Tampering | DOC-02 image/screenshot text passed to Claude | high | mitigate | Reuse the existing `<document_content>`-delimited buildDocumentContent() path; add no new prompt surface. Full prompt-injection pen test deferred to P13/SAFE-07 (documented, not weakened here). |
| T-12-01-02 | Tampering | New enum arm coverage | low | mitigate | Additivity test proves no existing case is altered; a `default` arm in getFieldSchema() prevents UnhandledMatchError on any future case. |
| T-12-01-SC | Tampering | npm/composer installs | low | accept | No packages installed this plan (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `php artisan test --compact --filter=TaxDocument` green.
- Full suite has zero NEW failures vs baseline (known pre-existing DashboardFinancialBlocksTest excluded).
- `vendor/bin/pint --dirty` clean.
</verification>

<success_criteria>
DOC-01, DOC-06, DOC-02 satisfied: 18 additive categories classify/extract through the unchanged two-pass pipeline; six financial schemas return correct fields; substantiation types extract via TIER2_FIELDS; image uploads use the existing vision branch; all existing vault/extraction tests pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-optimization-report-document-intake-feature-surface/12-01-SUMMARY.md` when done.
</output>
