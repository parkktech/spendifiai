---
phase: 12-optimization-report-document-intake-feature-surface
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - app/Listeners/MarkOptimizationReportStale.php
  - app/Listeners/DispatchReportGeneration.php
  - app/Providers/AppServiceProvider.php
  - app/Http/Controllers/Api/UserProfileController.php
  - app/Http/Controllers/Api/OptimizationReportController.php
  - app/Services/OptimizationProReviewExportService.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - routes/api.php
  - tests/Feature/ReportStalenessTest.php
  - tests/Feature/OptimizationReportControllerTest.php
  - tests/Feature/HandleInertiaRequestsTest.php
autonomous: true
requirements: [RPT-05]
must_haves:
  truths:
    - "A user's report is marked stale (flag flip only, no thundering-herd) on document extraction, interview answer, profile change, and bank sync; regeneration happens on demand or via a single debounced unique job."
    - "A user can GET their optimization report for a year (auto-initialized), request regeneration, and download the PDF."
    - "A user can export a single finding as a pro-review packet (fact-pattern, timestamped assertions, captured docs, static legal basis + citations, static defensibility rating, the question for the professional) — blocked while required docs are missing, every PDF carrying the persistent educational + 'prepared from user-asserted, unverified facts' disclaimer (RPT-05)."
    - "The 'Optimize My Income' nav badge count is available as a guarded Inertia shared prop."
  artifacts:
    - "app/Listeners/MarkOptimizationReportStale.php + DispatchReportGeneration.php + AppServiceProvider wiring"
    - "app/Http/Controllers/Api/OptimizationReportController.php + /api/v1/optimizer/report routes"
    - "app/Services/OptimizationProReviewExportService.php"
    - "auth.pendingOptimizationCount in HandleInertiaRequests::share()"
  key_links:
    - "TaxDocumentExtracted / OptimizationProfileBuilt / UserAnsweredQuestion → MarkOptimizationReportStale (flag) + DispatchReportGeneration (delay 30s, ShouldBeUnique job)"
    - "UserProfileController::updateFinancial() → BuildIncomeOptimizationProfile::dispatch (profile-change staleness)"
    - "OptimizationReportController → firstOrCreate + GenerateOptimizationReport::dispatch; pro-review export blocked while docs_missing"
    - "HandleInertiaRequests pendingOptimizationCount guarded by hasBankConnected → AuthenticatedLayout badge (12-05)"
---

<objective>
Close the staleness loop and expose the report to the client: wire the `TaxDocumentExtracted` event (from 12-02) and the existing `OptimizationProfileBuilt` / `UserAnsweredQuestion` events to stale-flag + debounced regeneration listeners; add profile-change staleness; build the report API (GET/regenerate/download), the RPT-05 pro-review export packet, and the nav badge shared prop.

Purpose: completes the RPT-02 staleness behavior, ships RPT-05 (pro-review export), and provides the UI-01 badge count consumed by 12-05.
Output: two listeners + wiring, a profile-controller dispatch, a report controller + routes, a pro-review export service, a HandleInertiaRequests prop, and staleness/API/badge tests.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-CONTEXT.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-RESEARCH.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-02-SUMMARY.md
@.planning/phases/12-optimization-report-document-intake-feature-surface/12-03-SUMMARY.md

# Source patterns (read before editing)
@app/Providers/AppServiceProvider.php
@app/Http/Middleware/HandleInertiaRequests.php
@app/Http/Controllers/Api/DurableFactsController.php
@app/Http/Controllers/Api/UserProfileController.php
@app/Services/TaxExportService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Staleness listeners + event wiring + profile-change staleness (completes RPT-02 behavior)</name>
  <read_first>
    - app/Providers/AppServiceProvider.php (Event::listen block lines 112-132 — the manual registration convention to extend)
    - app/Events/TaxDocumentExtracted.php (from 12-02), app/Events/OptimizationProfileBuilt.php, app/Events/UserAnsweredQuestion.php
    - app/Jobs/GenerateOptimizationReport.php (ShouldBeUnique, uniqueId user:tax_year — from 12-03)
    - app/Http/Controllers/Api/UserProfileController.php (updateFinancial() ~line 19; it currently dispatches CategorizePendingTransactions, NOT BuildIncomeOptimizationProfile — Open Question A3)
    - app/Jobs/BuildIncomeOptimizationProfile.php (dispatch signature; fires OptimizationProfileBuilt)
  </read_first>
  <files>app/Listeners/MarkOptimizationReportStale.php, app/Listeners/DispatchReportGeneration.php, app/Providers/AppServiceProvider.php, app/Http/Controllers/Api/UserProfileController.php, tests/Feature/ReportStalenessTest.php</files>
  <action>
    Create `MarkOptimizationReportStale implements ShouldQueue` handling TaxDocumentExtracted, UserAnsweredQuestion (optimization-type only), and OptimizationProfileBuilt: resolve user id + tax year from the event and `OptimizationReport::forUser($userId)->where('tax_year',$year)->update(['is_stale'=>true,'stale_since'=>now()])`. This is a FLAG FLIP only — never generates inline (no thundering-herd). For UserAnsweredQuestion, guard to optimization questions only (mirror the FEED-04 guard style) so transaction-categorization answers do not mark reports stale.
    Create `DispatchReportGeneration implements ShouldQueue` handling TaxDocumentExtracted and OptimizationProfileBuilt: `GenerateOptimizationReport::dispatch($userId,$year)->delay(now()->addSeconds(30))` — the 30s debounce + the job's ShouldBeUnique(user:tax_year) coalesce a 20-fact paystub upload into one report job (Pitfall 4).
    Register all listeners in AppServiceProvider inside the existing Phase-11 Event::listen block (same manual style). Do NOT convert to auto-discovery.
    Profile-change staleness: in `UserProfileController::updateFinancial()`, after the existing profile save + CategorizePendingTransactions dispatch, ALSO dispatch `BuildIncomeOptimizationProfile` for the user + current tax year (which fires OptimizationProfileBuilt → the chain above). Verify it is not already dispatched before adding (A3) — additive, keep existing dispatches.
    Test (ReportStalenessTest): seed a report is_stale=false; fire TaxDocumentExtracted → assert is_stale=true + stale_since set; assert no GenerateOptimizationReport ran inline (Queue::fake — only a delayed unique job queued); fire 5 UserAnsweredQuestion(optimization) events → assert only ONE unique report job effectively queues (ShouldBeUnique); fire a non-optimization UserAnsweredQuestion → assert report NOT marked stale.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ReportStalenessTest</automated>
  </verify>
  <done>Report stale-flags on doc extraction / interview / profile / bank-sync events; regeneration is debounced + unique (no herd); non-optimization answers do not affect the report; existing profile-save behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Report API (GET/regenerate/download) + RPT-05 pro-review export</name>
  <read_first>
    - routes/api.php (the optimizer group ~lines 332-346 where interview + facts routes live — add optimizer/report here)
    - app/Http/Controllers/Api/DurableFactsController.php (owner-check + scopeForUser response pattern to mirror)
    - app/Services/TaxExportService.php + app/Http/Controllers/Api/TaxController.php sendToAccountant() + the TaxPackageMail pattern (RPT-05 mirrors this)
    - app/Models/OptimizationReport.php (firstOrCreate init — 12-03) + app/Models/OptimizationFinding.php (docs_missing, pro_export_ready, legal_basis, assumptions, user_assertions, docs_captured)
  </read_first>
  <files>app/Http/Controllers/Api/OptimizationReportController.php, app/Services/OptimizationProReviewExportService.php, routes/api.php, tests/Feature/OptimizationReportControllerTest.php</files>
  <action>
    `OptimizationReportController` (auth:sanctum, all queries scopeForUser + owner-check like DurableFactsController): `show(year)` → firstOrCreate the report; if is_stale/empty, dispatch GenerateOptimizationReport and return a "generating" state (Pitfall 8 — never null); return sections + rebuilt_at + is_stale. `regenerate(year)` → dispatch GenerateOptimizationReport (rate-limit like other sensitive optimizer endpoints); `download(year)` → OptimizationReportExportService::generatePdf and stream/served via the existing signed-URL/download pattern.
    RPT-05 pro-review export: `OptimizationProReviewExportService::generatePacket(OptimizationFinding $finding): string` — mirror TaxExportService dompdf + the sendToAccountant/TaxPackageMail send path. Packet contents per finding: fact-pattern narrative (from description), timestamped user_assertions[] log, docs_captured references, STATIC config-sourced legal_basis + citations + assumptions (never Claude output), a static defensibility rating (solid / fact-dependent / frequently-abused) sourced from the finding band/config, and the specific question for the professional. BLOCK the export when the finding has any docs_missing or pro_export_ready=false (return 422). Every packet PDF carries the persistent educational disclaimer PLUS "prepared from user-asserted, unverified facts; educational material, not tax advice." Add controller endpoint + route for the packet (and, mirroring TaxController, an email-to-professional path via the existing mail pattern).
    Routes: add an `optimizer/report` prefix group in api.php next to optimizer/interview + optimizer/facts: GET /report/{year}, POST /report/{year}/regenerate, GET /report/{year}/download, POST /report/finding/{finding}/pro-review-export (+ email). Do not touch existing optimizer routes.
    Test (OptimizationReportControllerTest): GET returns a report/generating state and never 500s for a user with no report; cross-user GET is 403; pro-review export is 422 when docs_missing is non-empty and succeeds (PDF begins %PDF-) when docs complete; exported PDF contains the unverified-facts disclaimer string.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=OptimizationReportControllerTest</automated>
  </verify>
  <done>Report GET/regenerate/download work and are owner-scoped; pro-review packet builds with static legal basis + defensibility rating + disclaimers and is blocked while docs are missing (RPT-05).</done>
</task>

<task type="auto">
  <name>Task 3: Nav badge shared prop (UI-01 backend support)</name>
  <read_first>
    - app/Http/Middleware/HandleInertiaRequests.php (share() auth block — additive prop)
    - app/Models/OptimizationFinding.php (status values + description — confirm the "pending" status via grep before writing the count; Open Question A1)
  </read_first>
  <files>app/Http/Middleware/HandleInertiaRequests.php, tests/Feature/HandleInertiaRequestsTest.php</files>
  <action>
    Add `pendingOptimizationCount` to the `auth` array in HandleInertiaRequests::share(): a single guarded query — when the user has a bank connected, count OptimizationFinding::forUser(user id) that are open/pending with a non-null description (verify the exact pending status string against the OptimizationFinding model/detector code first, A1); otherwise 0. Additive prop only — do not alter any existing shared prop shape. This feeds the AuthenticatedLayout badge added in 12-05 (UI-01).
    Test (HandleInertiaRequestsTest): assert auth.pendingOptimizationCount is an integer; equals 0 when no bank connected; reflects the count of open findings for a bank-connected user.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=HandleInertiaRequestsTest</automated>
  </verify>
  <done>pendingOptimizationCount is an integer Inertia prop, guarded by hasBankConnected, with no change to existing shared props.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report/packet request → user | Cross-user report/finding disclosure |
| pro-review packet → external professional | Unverified facts leaving the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | Cross-user report / pro-review access | high | mitigate | scopeForUser() + explicit owner-check (403) on every controller action; mirror DurableFactsController. |
| T-12-04-02 | Repudiation | Pro-review packet presented as verified advice | high | mitigate | Every packet PDF carries "prepared from user-asserted, unverified facts; educational material, not tax advice"; blocked while docs_missing (422); legal_basis/citations are static config, never Claude. |
| T-12-04-03 | DoS | Thundering-herd report rebuilds | medium | mitigate | Stale = flag flip only; DispatchReportGeneration debounces 30s; GenerateOptimizationReport is ShouldBeUnique. |
| T-12-04-04 | Tampering | Badge count leaking cross-user findings | low | mitigate | Count scoped via forUser() + guarded by hasBankConnected. |
| T-12-04-SC | Tampering | npm/composer installs | low | accept | No packages installed (RESEARCH audit: none). |
</threat_model>

<verification>
- `php artisan test --compact --filter='ReportStaleness|OptimizationReportController|HandleInertiaRequests'` green.
- Full suite zero NEW failures; `vendor/bin/pint --dirty` clean.
- No inline report generation on any event (Queue::fake assertion); SAFE-03 gate green.
</verification>

<success_criteria>
RPT-05 satisfied and the RPT-02 staleness behavior + UI-01 badge count completed: report is stale-flagged and debounce-regenerated across all four triggers, exposed via owner-scoped GET/regenerate/download, exportable as a disclaimer-bearing pro-review packet blocked while docs are missing, with a guarded badge count prop for the nav.
</success_criteria>

<output>
Create `.planning/phases/12-optimization-report-document-intake-feature-surface/12-04-SUMMARY.md` when done.
</output>
