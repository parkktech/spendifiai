---
phase: 13-safety-validation-hardening
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - tests/Unit/Safe03ConsolidationTest.php
  - .planning/phases/13-safety-validation-hardening/SAFE-HARDENING-REPORT.md
autonomous: true
requirements: [SAFE-03, SAFE-05]
must_haves:
  truths:
    - "A single consolidation test asserts that no Claude call site constructs a payload carrying a report/finding dollar field — all numbers trace to TaxRulesEngineService/config."
    - "The SAFE-05 hardening report certifies every SAFE-01..07 requirement CERTIFIED / SCOPED-OUT / DEFERRED with concrete evidence pointers to tests, config keys, and service methods."
    - "The report delineates the v1.0 liability scope (Claude-computed dollars in savings services) as a documented owner recommendation, not a silent gap."
  artifacts:
    - tests/Unit/Safe03ConsolidationTest.php
    - .planning/phases/13-safety-validation-hardening/SAFE-HARDENING-REPORT.md
  key_links:
    - "The report binds the artifacts from 13-01/02/03 by exact test name + config key so future audits are one green run."
    - "Safe03ConsolidationTest complements EstimatedValueGuardTest (write-site) and NoLiteralGuardTest (literals) on the payload-exclusion axis."
---

<objective>
Consolidate the SAFE-03 payload guard into one authoritative test and write the SAFE-05 security + legal hardening report that certifies the whole feature within the educational-only liability boundary (SAFE-03, SAFE-05).

Purpose: SAFE-03 evidence is currently spread across four separate tests; a single consolidation test makes the "Claude never computes dollar amounts" guarantee auditable in one place. SAFE-05 requires a hardening artifact — but per research Pitfall 6 the artifact is a green test suite plus a bound report, not a decaying document. This plan runs last so the report reflects the completed 13-01/02/03 work accurately.

Output: One consolidation Pest test over all Claude call sites, and SAFE-HARDENING-REPORT.md binding every SAFE requirement to its evidence. Additive only — no service edits (the report references existing exclusion docblocks rather than adding comment noise to 12 files, honoring CLAUDE.md scope discipline).
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-safety-validation-hardening/13-RESEARCH.md

# Existing SAFE-03 guards this test consolidates alongside
@tests/Unit/EstimatedValueGuardTest.php
# Payload-exclusion docblocks to cite (dollar fields deliberately excluded from Claude payloads)
@app/Services/NarrationService.php
@app/Services/OptimizationReportNarratorService.php
# Prior-plan SUMMARYs (evidence to bind) — read whichever exist at execution time
@.planning/phases/13-safety-validation-hardening/13-01-SUMMARY.md
@.planning/phases/13-safety-validation-hardening/13-02-SUMMARY.md
@.planning/phases/13-safety-validation-hardening/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SAFE-03 payload-exclusion consolidation test across all Claude call sites</name>
  <files>tests/Unit/Safe03ConsolidationTest.php</files>
  <action>
Create tests/Unit/Safe03ConsolidationTest.php — one authoritative SAFE-03 gate on the payload-exclusion axis (complements EstimatedValueGuardTest which guards the write-site, and NoLiteralGuardTest which guards raw literals). Static-scan the Claude call-site service files enumerated in the RESEARCH.md "Complete Claude Call Site Inventory" that build an optimization/finding/report payload (NarrationService, OptimizationReportNarratorService, InterviewOrchestratorService) and assert that the request payload construction does not include any report/finding dollar field name — the set the exclusion docblocks name: estimated_value_cents, net_cash_cost, tax_saved, cliff_bonus_value (read NarrationService line ~108 / OptimizationReportNarratorService line ~96 for the authoritative excluded-field list and use exactly that set). Scan the Http::post 'messages'/'system' payload region; skip comment lines and the docblock lines that merely NAME the excluded fields (same comment-skip discipline as EstimatedValueGuardTest, so the exclusion docblock does not self-trip). A dollar field appearing in an actual payload array is a violation.

Add two anchor assertions that the existing SAFE-03 guards are present and wired: assert tests/Unit/EstimatedValueGuardTest.php and tests/Unit/NoLiteralGuardTest.php exist, and that app/Services/TaxRulesEngineService.php exists (the sole permitted dollar-math source). Write the header to state SAFE-03's claim: all report/finding dollar amounts originate from TaxRulesEngineService/config; Claude narrates only.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=Safe03Consolidation</automated>
  </verify>
  <done>The consolidation test is GREEN; adding a dollar field to any narration/report Claude payload makes it RED; the existing EstimatedValueGuard + NoLiteral guards and TaxRulesEngineService are asserted present.</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-05 hardening report binding all SAFE evidence</name>
  <files>.planning/phases/13-safety-validation-hardening/SAFE-HARDENING-REPORT.md</files>
  <action>
Write .planning/phases/13-safety-validation-hardening/SAFE-HARDENING-REPORT.md — the SAFE-05 "security + legal hardening pass completed" artifact. Read the 13-01/13-02/13-03 SUMMARYs (and the 13-01 framing audit worksheet) to bind accurate evidence. Structure:

1. Certification table — every requirement SAFE-01..SAFE-07 marked CERTIFIED / SCOPED-OUT / DEFERRED, each with a one-line status.
2. Evidence pointers — per requirement, the exact test name(s), config key(s), and service method(s) that constitute the proof (e.g. SAFE-01 → BannedPhraseSystemPromptsTest + BannedPhraseTemplatesTest + FramingReviewPinTest; SAFE-02/07 → InjectionPenTest + the schema-whitelist in TaxDocumentExtractorService + <document_content> wrapping in EmailParserService/BankStatementParserService; SAFE-03 → Safe03ConsolidationTest + EstimatedValueGuardTest + NoLiteralGuardTest + TaxRulesEngineService; SAFE-04 → SsnMaskingAuditTest + sanitizeExtraction + encrypted casts; SAFE-05 → this report + green suite; SAFE-06 → HardBlockRefusalService + config/safe-refusal.php + the two wired call sites + HardBlockRefusalTest).
3. Injection pen-test results — the content paths exercised (DC-01/02/05/06/08/09), what defense each relies on (binary type, schema whitelist, delimiter wrapping), and the outcome.
4. SSN-masking audit results — the five-link chain and its status.
5. v1.0 liability delineation (binding constraint 1) — SavingsAnalyzerService, SavingsTargetPlannerService, AlternativeSuggestionService use assertive framing and compute dollar amounts via Claude; these are EXISTING pre-v2.1 features, out of scope for rewrite in this phase, recorded here as a documented recommendation for the owner to schedule as a separate hardening pass (with the specific risk: assertive-language bleed when users arrive from the v2.1 optimization surface, and Claude-computed savings dollars outside the TaxRulesEngineService boundary).
6. Gray-area module sign-off status — the reframed ceilings pinned by FramingReviewPinTest, each with owner-review status (note items still marked "[default ruling — owner review pending]" from config, e.g. the MFS/entity rulings).
7. Known limitations — vision injection has a schema-constraint defense but not a semantic defense (research Assumption A2/tertiary source); the best-effort abusive-scheme disclaimer is not a detection guarantee (SAFE-06); text-path delimiters are defense-in-depth atop the JSON-output constraint.

Keep it factual and evidence-bound; do not restate implementation. This report + the green SAFE suite is the milestone certification.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-safety-validation-hardening/SAFE-HARDENING-REPORT.md && grep -q "SAFE-07" .planning/phases/13-safety-validation-hardening/SAFE-HARDENING-REPORT.md && echo REPORT_OK</automated>
  </verify>
  <done>SAFE-HARDENING-REPORT.md exists; certifies SAFE-01..07 with evidence pointers; delineates v1.0 scope as a documented owner recommendation; lists known limitations and gray-area sign-off status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude narration payload → report/finding dollars | Claude must never be the source of a report/finding dollar amount |
| milestone certification → liability boundary | the milestone is declared done only when every SAFE requirement is evidenced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04-01 | Tampering | narration/report Claude payloads | high | mitigate | Safe03ConsolidationTest asserts no dollar field in any Claude payload |
| T-13-04-02 | Elevation (legal) | milestone certification completeness | high | mitigate | SAFE-HARDENING-REPORT binds every SAFE req to evidence; v1.0 gap documented not hidden |
| T-13-04-03 | Repudiation | undocumented residual risk | medium | accept | Known-limitations section records vision-injection semantic gap + best-effort disclaimer scope |
| T-13-04-SC | Tampering | package installs | low | accept | No npm/composer installs (test + doc only) |
</threat_model>

<verification>
- `php artisan test --compact --filter=Safe03Consolidation` — green
- `php artisan test --compact --filter=SAFE` — the complete SAFE suite green (SAFE-01..07 evidence tests)
- `php artisan test --compact` — full suite green at baseline or higher (binding constraint 6)
- `npm run build` — clean (phase gate before /gsd-verify-work)
- `vendor/bin/pint --dirty` — clean
- SAFE-HARDENING-REPORT.md present and certifies all seven SAFE requirements
</verification>

<success_criteria>
- One consolidation test certifies SAFE-03 payload exclusion across all Claude call sites.
- SAFE-HARDENING-REPORT.md certifies SAFE-01..07 with evidence pointers, v1.0 delineation, gray-area sign-off, and known limitations.
- Full SAFE suite + full suite green; npm build + pint clean — the milestone certification gate.
</success_criteria>

<output>
Create `.planning/phases/13-safety-validation-hardening/13-04-SUMMARY.md` when done, plus the SAFE-HARDENING-REPORT.md artifact. The SUMMARY records the final SAFE-01..07 certification status and the full-suite/build/pint gate results.
</output>
