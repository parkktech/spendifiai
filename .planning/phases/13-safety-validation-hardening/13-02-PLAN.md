---
phase: 13-safety-validation-hardening
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - config/safe-refusal.php
  - app/Services/HardBlockRefusalService.php
  - app/Exceptions/HardBlockRefusalException.php
  - app/Services/InterviewOrchestratorService.php
  - app/Http/Controllers/Api/AIQuestionController.php
  - tests/Unit/HardBlockRefusalServiceTest.php
  - tests/Feature/HardBlockRefusalTest.php
autonomous: true
requirements: [SAFE-06]
must_haves:
  truths:
    - "A user whose escape-hatch or chat free text names a hard-block abusive scheme receives a structured refuse-and-educate response (what/why, never how) with zero Claude calls made."
    - "Legitimate financial free text (e.g. mentioning a trust account or a captive customer) passes through unblocked."
    - "The refusal path adds no Anthropic HTTP call — the D17 zero-Claude assertions still pass."
  artifacts:
    - config/safe-refusal.php
    - app/Services/HardBlockRefusalService.php
    - app/Exceptions/HardBlockRefusalException.php
    - tests/Unit/HardBlockRefusalServiceTest.php
    - tests/Feature/HardBlockRefusalTest.php
  key_links:
    - "HardBlockRefusalService::check() runs in InterviewOrchestratorService::recordAnswer() on the extracted escape-hatch free text BEFORE interpretEscapeAnswer()'s Claude call."
    - "HardBlockRefusalService::check() runs in AIQuestionController::chat() on the validated message BEFORE categorizer->interpretUserResponse()'s Claude call."
    - "Education copy is sourced from config (the refused_recommendations why-text lineage) — never generated by Claude."
---

<objective>
Enforce the SAFE-06 hard-block refusal list in code: detect abusive-scheme signals in user free text, refuse-and-educate (what/why only, never how), never monetize, and never call Claude to do it (SAFE-06).

Purpose: Today the escape-hatch (InterviewOrchestratorService) and chat (AIQuestionController) routes send user free text straight to Claude with no pre-filter — the system prompt is the only defense. SAFE-06 requires a code-level detect/refuse gate for the full Dirty-Dozen list (831(b) micro-captives, syndicated conservation easements, offshore/FBAR-FATCA concealment, Malta pension / abusive foreign trusts, nonprofit-as-personal-shelter, corporation sole / pure trust, "start a ministry", crypto non-reporting, cash structuring, PPLI / offshore-crypto-IRA, Hess-style body-mod probes).

Output: A config-driven, zero-AI keyword/phrase detector wired before both Claude call sites, returning a D18/D19-shaped structured refusal, feeding the RPT-06 refusal lineage. Additive only.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-safety-validation-hardening/13-RESEARCH.md

# Verbatim SAFE-06 hard-block list + never-surface list + anti-waste principle
@.planning/REQUIREMENTS.md
# Education copy source-of-truth (refused_recommendations what/why) — copy the WHY text, never invent
@config/optimization-report.php
# Wiring point A — escape-hatch free text is extracted here (recordAnswer) then handed to interpretEscapeAnswer's Claude call
@app/Services/InterviewOrchestratorService.php
# Wiring point B — chat() forwards message to categorizer's Claude call with no pre-filter
@app/Http/Controllers/Api/AIQuestionController.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: safe-refusal config + HardBlockRefusalService + refusal exception (zero AI)</name>
  <files>config/safe-refusal.php, app/Services/HardBlockRefusalService.php, app/Exceptions/HardBlockRefusalException.php, tests/Unit/HardBlockRefusalServiceTest.php</files>
  <behavior>
    - check() returns a structured refusal array {refused:true, category, education, blocked_reason:'hard_block_safe06'} when text contains a trigger phrase from any cluster; returns null otherwise.
    - Detection is case-insensitive and multi-word / n-gram based (research Pitfall 5): "abusive foreign trust" blocks; the bare word "trust" does NOT (so "I have a trust account" passes). "captive insurance" / "micro-captive" / "831b" block; "captive customer" passes.
    - Every SAFE-06 cluster is covered: 831(b) micro-captive, syndicated/façade conservation easement, offshore concealment / FBAR-FATCA, Malta pension / abusive foreign trust, nonprofit-as-personal-shelter / §4958, corporation sole / pure trust, "start a ministry", crypto non-reporting, cash structuring, PPLI / offshore-crypto-IRA, Hess-style body-mod probe.
    - education text is pulled from config (never Claude), describes WHAT the scheme is and WHY the IRS challenges it, and never reveals HOW to implement it.
    - check() makes zero HTTP/Anthropic calls and has zero dependency on any Claude-calling service (research Pitfall 1).
  </behavior>
  <action>
Create config/safe-refusal.php with: (1) a `clusters` array — one entry per SAFE-06 category, each { category (human label), phrases (array of high-signal multi-word triggers, lowercase), education (the what/why refuse-and-educate copy) }. Draw the trigger phrases from the RESEARCH.md SAFE-06 keyword table and the REQUIREMENTS.md SAFE-06 line (read both — the list is long and specific; every named scheme goes in the config). Draw the education copy from the corresponding config/optimization-report.php refused_recommendations `why` text (the RPT-06 lineage) so the messaging is consistent; keep it what/why only, never how. (2) a `best_effort_disclaimer` string (verbatim per SAFE-06): that SpendifiAI monitors for abusive-scheme signals as a best-effort safeguard and this does not guarantee detection of every harmful prompt or scheme — rendered once per session, not per detection. (3) an `anti_waste_principle` string capturing that no output may present spending solely to create a deduction as savings (the pinning of live copy is owned by 13-01 FramingReviewPinTest; this key is the canonical statement config).

Keep phrases at phrase/n-gram granularity, not bare words (Pitfall 5). Use ASCII fallbacks alongside accented forms where relevant (e.g. both "facade easement" and the accented form).

Create app/Services/HardBlockRefusalService.php with `check(string $userText): ?array` implementing the behavior above — mb_strtolower the input once, iterate config('safe-refusal.clusters'), str_contains each phrase, return the structured array on first match else null. No constructor dependencies on Claude-calling services.

Create app/Exceptions/HardBlockRefusalException.php carrying the refusal array and a render() method returning response()->json($refusal, 200) — so the interview path self-renders the refusal without any controller-flow change (additive, scope-safe). Constructor accepts the array from check().

Write tests/Unit/HardBlockRefusalServiceTest.php with two fixture corpora: (a) an abusive-phrase corpus (at least one phrase per cluster) that MUST each return refused:true with the right category; (b) a legitimate-financial corpus (trust account, captive customer, easement on my property survey, ministry donation, my HSA, etc.) that MUST each return null. Assert education never contains implementation verbs ("how to", "set up a", "form a") — what/why only.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=HardBlockRefusalServiceTest</automated>
  </verify>
  <done>Every SAFE-06 cluster is detected by at least one fixture; every legitimate-financial fixture passes unblocked; check() makes zero AI calls; config carries the best-effort disclaimer and anti-waste principle strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wire refusal gate before both Claude call sites + feature test</name>
  <files>app/Services/InterviewOrchestratorService.php, app/Http/Controllers/Api/AIQuestionController.php, tests/Feature/HardBlockRefusalTest.php</files>
  <behavior>
    - Escape-hatch: POSTing an abusive-scheme free text through the interview answer route returns the structured refusal and dispatches NO Anthropic request.
    - Chat: POSTing an abusive-scheme message to /questions/{question}/chat returns the structured refusal (200) and dispatches NO Anthropic request.
    - Legitimate free text on both routes proceeds to the existing behavior unchanged.
    - NoClaudeScenarioTest and ObjectiveEnqueueTest (D17 zero-call assertions) still pass after wiring.
  </behavior>
  <action>
Wire HardBlockRefusalService::check() at the two enforcement points, BEFORE the existing Claude call in each — additive, no change to existing response shapes for the non-refused path (CLAUDE.md scope rules).

InterviewOrchestratorService::recordAnswer(): after the escape-hatch free text is extracted (the branch that strips the __other__ sentinel into $freeText, immediately before interpretEscapeAnswer() is called), run check($freeText); if it returns a refusal, throw new HardBlockRefusalException($refusal). The exception's render() surfaces the structured refusal to the client; no InterviewController change is required. Resolve HardBlockRefusalService via the container (constructor-inject or app()) without importing any Claude-calling service into it.

AIQuestionController::chat(): before calling $this->categorizer->interpretUserResponse(...), run check($request->validated('message')); if refused, return response()->json($refusal, 200) and do not call the categorizer. Inject HardBlockRefusalService into the controller (additive constructor param or method resolve).

Write tests/Feature/HardBlockRefusalTest.php using Http::preventStrayRequests() (and assertNothingSent) so any Anthropic call fails the test. Cover: (1) an authenticated user posting an abusive escape-hatch answer to the interview answer route → refusal JSON, no Anthropic request; (2) posting an abusive message to /questions/{question}/chat → refusal JSON (200), no Anthropic request; (3) a legitimate escape-hatch answer and a legitimate chat message reach their existing code paths (fake the Anthropic response for these) and return the normal shape. Use the existing Pest auth helpers (createUserWithBank for the chat route which sits behind bank.connected).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=HardBlockRefusal</automated>
  </verify>
  <done>Both routes refuse-and-educate on abusive free text with zero Anthropic calls; legitimate text is unaffected; the refusal payload matches the check() contract; wiring is additive (no existing response shape changed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user free text → Claude | escape-hatch answer + chat message are untrusted free text sent to the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02-01 | Tampering | escape-hatch / chat free-text → Claude | high | mitigate | HardBlockRefusalService keyword gate runs before both Claude calls; refuse-and-educate on match |
| T-13-02-02 | Information Disclosure | education copy | medium | mitigate | education text is what/why only, sourced from config; unit test forbids implementation verbs |
| T-13-02-03 | Denial of Service (cost) | added AI call on hot path | high | mitigate | detector is pure phrase-match, zero Claude calls; D17 zero-call tests re-run as a gate |
| T-13-02-04 | Tampering | over-broad keyword false-block | medium | mitigate | multi-word n-gram triggers only (Pitfall 5); legitimate-financial corpus proves no false block |
| T-13-02-SC | Tampering | package installs | low | accept | No npm/composer installs (existing Pest/PHP + config only) |
</threat_model>

<verification>
- `php artisan test --compact --filter=HardBlockRefusal` — green (unit + feature)
- `php artisan test --compact --filter=NoClaudeScenario` and `--filter=ObjectiveEnqueue` — D17 zero-call assertions still green after wiring (research Pitfall 1)
- `php artisan test --compact --filter=SAFE` — green
- `php artisan test --compact` — full suite green at baseline or higher (binding constraint 6)
- `vendor/bin/pint --dirty` — clean
</verification>

<success_criteria>
- HardBlockRefusalService detects every SAFE-06 cluster and refuses-and-educates with zero Claude calls.
- Both the escape-hatch and chat routes enforce the gate before their Claude call; legitimate text is unaffected.
- config/safe-refusal.php carries the best-effort disclaimer and anti-waste principle.
- D17 zero-call tests and the full suite stay green; pint clean.
</success_criteria>

<output>
Create `.planning/phases/13-safety-validation-hardening/13-02-SUMMARY.md` when done, listing the SAFE-06 clusters covered, the two enforcement points, and confirmation the D17 zero-call tests remained green.
</output>
