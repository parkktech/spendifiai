---
phase: 14-action-center-scenarios-design-elevation
plan: 10
type: execute
wave: 6
depends_on: ["14-08", "14-09", "14-07", "14-04"]
files_modified:
  - resources/js/Components/SpendifiAI/ActionCenterWidget.tsx
  - resources/js/Components/SpendifiAI/ObjectiveReadinessPanel.tsx
  - resources/js/Components/SpendifiAI/ScenarioComparisonCards.tsx
  - resources/js/Components/SpendifiAI/ScenarioMixPanel.tsx
  - resources/js/Components/SpendifiAI/OptimizationChecklistView.tsx
  - resources/js/Components/SpendifiAI/InterviewCard.tsx
  - resources/js/Components/SpendifiAI/OptimizationReportView.tsx
  - resources/js/Pages/Optimize/Index.tsx
  - resources/js/Pages/Dashboard.tsx
  - resources/js/Layouts/AuthenticatedLayout.tsx
  - resources/js/types/spendifiai.d.ts
autonomous: true
requirements: [ACT-01, ACT-05, ELEV-03]
must_haves:
  truths:
    - "A persistent Action Center surface renders on the Dashboard (widget) and the Optimize page, with big-checkmark to-do items carrying benefit lines and due dates; the nav badge shows the combined open count (ACT-01)"
    - "First-run users see Stage-0 items (link bank, cards, emails, interview, paystub) that disappear as completed; the fully-optimized empty state renders as an achievement moment, not a blank view (ACT-02 display, ACT-05)"
    - "For diverging objectives the Optimize 'scenarios' stage shows a side-by-side A/B/Balanced comparison with knob-diff highlights, trade-off one-liners, guard chips, and an Illustration badge on long-horizon figures; agreement shows one merged plan; choosing persists and re-materializes the checklist (SCN-06/08 display)"
    - "Every materialized checklist step shows its engine-computed benefit line; the header aggregates total unlocked benefit; long-horizon projections are labeled illustrations with stated assumptions (SCN-08/D9.7 display)"
    - "All new Phase-14 components are born to the DESIGN-ELEVATION-SPEC canonical recipes (§3.11 action card, §3.9 empty state, §3.10 skeleton) from their first commit — no retrofitting (ELEV-03); Wave-3 preservation audit passes"
  artifacts:
    - "ActionCenterWidget, ObjectiveReadinessPanel, ScenarioComparisonCards, ScenarioMixPanel, OptimizationChecklistView components + Optimize/Index 'scenarios' ViewMode + Dashboard widget mount + InterviewCard/OptimizationReportView born-premium upgrades + types"
  key_links:
    - "Money fields arrive as integer _cents from the API — divide by 100 for display (no decimal-string pitfalls); apply font-tabular to all figures"
    - "All motion is CSS/Tailwind only (framer-motion absent, Pitfall 9); nav badge combines pendingOptimizationCount + pendingActionCount"
---

<objective>
Deliver the Phase-14 frontend (ACT-01, ACT-05, ELEV-03): the Action Center UI, the scenario comparison + choice + checklist stage, and the born-premium new components. Build the Action Center surface (Dashboard `ActionCenterWidget` + Optimize-page surface) with big-checkmark items, benefit lines, due dates, Stage-0 onboarding items that disappear as completed, and the achievement-moment empty state; add the `scenarios` ViewMode to `Optimize/Index.tsx` with the `ObjectiveReadinessPanel` (enqueue tick-down loop), `ScenarioComparisonCards` (three-metric block, knob-diff highlights, trade-off lines, guard chips, Illustration badge + assumptions tooltip, disclaimers, citation line), `ScenarioMixPanel` (compute round-trips), and the ConfirmDialog choose flow; render the fact-gated `OptimizationChecklistView` (directive vs confirm-ask, benefit lines, header aggregate, done-toggle) in the Choices stage and mirrored in the report; and upgrade `InterviewCard` (additive rendering) + `OptimizationReportView`/`FindingSummaryCard` to the born-premium spec. Combine the nav badge counts. Every new component uses the DESIGN-ELEVATION-SPEC canonical recipes from the first commit.

Purpose: The interactive product spine users actually see — the Action Center, the A/B scenario choice, and the actionable checklist — all luxury-lean.
Output: Five new components + the Optimize scenarios stage + Dashboard widget mount + born-premium upgrades + types, passing the taste-skill v2 blocking audits and §6 preservation audit.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/reference/SCENARIOS-SPEC.md
@.planning/reference/DESIGN-ELEVATION-SPEC.md
@resources/js/Pages/Optimize/Index.tsx
@resources/js/hooks/useApi.ts
@resources/js/Components/SpendifiAI/ActionResponsePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Action Center UI (Dashboard widget + Optimize surface, Stage-0, empty state) + nav badge combine</name>
  <files>resources/js/Components/SpendifiAI/ActionCenterWidget.tsx, resources/js/Pages/Dashboard.tsx, resources/js/Layouts/AuthenticatedLayout.tsx, resources/js/types/spendifiai.d.ts</files>
  <read_first>
    - 14-RESEARCH.md "Action Center Data Model" (GET /optimizer/action-center response shape: stage0_items/checklist_items/monitor_prompts/calendar_items/total_open/empty) + DRIFT-09 (nav badge combines pendingOptimizationCount + pendingActionCount)
    - DESIGN-ELEVATION-SPEC §3.11 (scenario/checklist action card canonical recipe — big benefit amount at text-[22px] font-[800] text-sw-success font-tabular), §3.9 (premium empty state template), §4.1 (hover/focus/press states)
    - 14-PATTERNS.md Action Center UI analog (Dashboard Where-to-Cut + ActionResponsePanel); resources/js/hooks/useApi.ts (enabled=hasBankConnected pattern); resources/js/Pages/Dashboard.tsx (mount point) + AuthenticatedLayout badge render
  </read_first>
  <action>
    Create ActionCenterWidget consuming GET /api/v1/optimizer/action-center via useApi (enabled=hasBankConnected). Render big-checkmark to-do items across the four groups; each item shows title, an engine-computed benefit line (divide _cents by 100; font-tabular), and a due date where present; checking an item calls the checklist PATCH (for checklist items) with optimistic UI. Stage-0 items render for first-run users and disappear as completed (the endpoint recomputes). Use the §3.11 canonical action-card recipe and §3.9 premium empty state for the fully-optimized achievement moment ("You're fully optimized for now — we're watching for changes", ACT-05) — never a blank view. Mount the widget on Dashboard (an additive section; do not disturb existing Dashboard sections/charts). In AuthenticatedLayout, combine pendingOptimizationCount + pendingActionCount for the Optimize nav badge display (additive; existing badge logic preserved). Add the Action Center + item TypeScript types to spendifiai.d.ts.

    Born-premium (ELEV-03): the widget and all its cards use the new tokens (ring-1/gradient/shadow-sw-*, card-lift, stagger-children on the item list, font-tabular) from the first commit. Educational framing on any benefit copy ("may/could/consider"); no assertive/guaranteed-savings language (banned-phrase discipline).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Build + typecheck clean; Action Center renders on Dashboard + is ready for the Optimize surface; Stage-0 items reflect real state and disappear when done; empty state is the achievement moment; nav badge combines both counts; benefit figures tabular from _cents; born-premium recipes applied.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize 'scenarios' ViewMode — readiness panel, comparison cards, mix panel, choose flow + InterviewCard additive</name>
  <files>resources/js/Components/SpendifiAI/ObjectiveReadinessPanel.tsx, resources/js/Components/SpendifiAI/ScenarioComparisonCards.tsx, resources/js/Components/SpendifiAI/ScenarioMixPanel.tsx, resources/js/Components/SpendifiAI/InterviewCard.tsx, resources/js/Pages/Optimize/Index.tsx</files>
  <read_first>
    - SCENARIOS-SPEC §D.1 (4th ViewMode 'scenarios' + StageIndicator step), §D.2 (readiness panel: state chips, "answer 2 more questions" copy from ready+questions_to_unlock only, enqueue CTA → interview loop, tick-down, doc affordance), §D.3 (comparison rendering: 3-up grid, three-metric block, diverging-knob rows + trade-off lines, guard chips, Illustration badge + assumptions tooltip, disclaimers, citation line), §D.4 (ConfirmDialog choose), §E (GET scenarios JSON, compute body), §A.5.5 (InterviewCard prefill_display/choices/doc_affordance additive rendering)
    - DESIGN-ELEVATION-SPEC §3.11 (born-premium cards), §3.7 (Illustration/guard badges), §4.3 (expand/collapse for the mix panel)
    - resources/js/Pages/Optimize/Index.tsx (current ViewMode = 'findings'|'interview'|'report', StageIndicator); resources/js/Components/SpendifiAI/InterviewCard.tsx (additive rendering only — preserve existing behavior); ConfirmDialog component
  </read_first>
  <action>
    Add 'scenarios' to ViewMode and a "Choices" StageIndicator step between Interview and Report (§D.1); interview completion surfaces a "See your options" CTA. Build ObjectiveReadinessPanel (three objective cards from GET /optimizer/objectives/{year}; state chips Ready / "N questions to unlock" / "Confirm M values"; the copy rendered from ready + questions_to_unlock only — the frontend NEVER computes readiness; enqueue CTA → POST enqueue → the existing InterviewCard loop; re-fetch readiness per answer so the count ticks down; doc-affordance "upload a pay stub instead" secondary action). Build ScenarioComparisonCards (agreement → one merged card; conflict → 3-up grid, Balanced pre-selected; always the three-metric block — take-home Δ per-paycheck+/yr exact, federal-tax Δ signed, retirement +$/yr + FV range with the Illustration badge + assumptions tooltip; diverging knob rows highlighted with the §C.3 trade-off line; agreeing knobs collapsed; guard chips; citation line "Based on N facts you provided"; the static scenario disclaimer). Build ScenarioMixPanel (segmented control per diverging knob on grid values; each change POSTs compute and live-updates a "Your mix" outcome strip; "Choose my mix"). Wire "Choose this approach" → ConfirmDialog → POST choose → route to the checklist view. All money from _cents (divide by 100; font-tabular). InterviewCard: additively render choices/answer_type/prefill_display/doc_affordance when present (existing behavior untouched).

    Born-premium (ELEV-03) + educational frame: cards use the canonical recipes from the first commit; the Illustration badge is a persistent sw-info Badge (never plain text that reads as a promise); scenario copy uses "approaches to consider" framing with no assertive/guaranteed language (banned-phrase discipline).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Build + typecheck clean; the scenarios stage renders readiness cards that tick down, A/B/Balanced (or merged) comparison with knob-diffs/trade-offs/guard chips/Illustration badge/citation, a working mix panel, and the ConfirmDialog choose flow; InterviewCard renders the additive fields; frontend never computes readiness or dollars.</done>
</task>

<task type="auto">
  <name>Task 3: Checklist view + report mirror + born-premium report surfaces + empty states/skeletons + Wave-3 audit</name>
  <files>resources/js/Components/SpendifiAI/OptimizationChecklistView.tsx, resources/js/Components/SpendifiAI/OptimizationReportView.tsx, resources/js/Pages/Optimize/Index.tsx</files>
  <read_first>
    - SCENARIOS-SPEC §D.5 (checklist rendering: numbered imperative steps grouped by knob; directive vs confirm-ask by kind; benefit line per step; header aggregate; K1..K6 copy), §D.7 (report chosen_plan mirror)
    - DESIGN-ELEVATION-SPEC §3.11 (FindingSummaryCard born-premium + scenario/checklist action card), §3.6 (page display header), §3.9 (empty states), §3.10 (loading skeletons replacing full-page spinners), §5 Wave-3 audit checklist, §6 preservation audit
    - resources/js/Components/SpendifiAI/OptimizationReportView.tsx (section cards → shadow-sw-2 + semantic icon containers); resources/js/Pages/Optimize/Index.tsx (FindingSummaryCard + page header)
  </read_first>
  <action>
    Build OptimizationChecklistView: render the materialized items grouped by knob; a directive-kind step shows the imperative (payroll/portal/form named) with its engine benefit line; a confirm-ask-kind step renders the confirmation ask instead (D9.2 fact-gate); the header aggregates total unlocked benefit ("Completing these N actions ≈ …/mo take-home · …/yr tax · roughly X–Y by age (illustration)"); done-toggle PATCHes the checklist item. Render it in the Choices stage (below the cards) and mirror it in the report chosen_plan section. Long-horizon figures always labeled illustration with the stated assumptions; all figures from _cents + font-tabular.

    Upgrade OptimizationReportView section cards to shadow-sw-2 + semantic icon containers and FindingSummaryCard + the Optimize page header to the §3.11/§3.6 born-premium recipes (ELEV-03). Replace any full-page Loader2 spinners with the §3.10 skeleton cards and any blank empty states with the §3.9 premium empty state.

    Run the DESIGN-ELEVATION-SPEC §5 Wave-3 audit checklist, the §6 preservation audit, and the four taste-skill v2 blocking audits (shadow hierarchy, motion, no consecutive all-caps eyebrows, tabular numerals) for each new/changed surface; record ALL PASS in the SUMMARY — a single FAIL blocks the commit. Also spot-check banned-phrase/educational-framing compliance in scenario + checklist copy.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5 && php artisan test --compact 2>&1 | tail -10</automated>
  </verify>
  <done>Build + typecheck + full PHP suite green; checklist renders directive vs confirm-ask by kind with per-step benefit lines and a header aggregate, mirrored in the report; report surfaces + FindingSummaryCard + page header are born-premium; spinners replaced by skeletons and blanks by premium empty states; §5 Wave-3 + §6 preservation + four taste-skill blocking audits recorded ALL PASS; scenario/checklist copy passes banned-phrase/educational-framing spot-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API _cents → display | Client must divide by 100 and never invent figures |
| scenario/checklist copy → user | Educational-frame + no guaranteed-savings language |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-10-01 | Information Disclosure / Repudiation | assertive/guaranteed savings copy | high | mitigate | Educational "approaches to consider" framing; Illustration badge on long-horizon; banned-phrase spot-check (SAFE-01) |
| T-14-10-02 | Tampering | client-side dollar math | medium | mitigate | Figures come from API _cents (divide by 100 only); readiness/benefit never computed in the frontend |
| T-14-10-03 | Denial of Service (a11y) | new animations | low | mitigate | CSS-only motion + prefers-reduced-motion override (14-04); §6 audit gate |
| T-14-10-04 | Elevation of Privilege | Action Center data | low | mitigate | useApi enabled=hasBankConnected; endpoints scoped server-side (14-09) |
| T-14-10-SC | Tampering | package installs | low | accept | Zero new packages; framer-motion absent, CSS-only motion (Pitfall 9) |
</threat_model>

<verification>
- `npm run build` + `npx tsc --noEmit` — clean
- `php artisan test --compact` — full suite, zero NEW failures
- §5 Wave-3 audit checklist + §6 preservation audit (all PASS) + four taste-skill v2 blocking audits per surface, recorded in SUMMARY
- Manual owner walkthrough of the Action Center + scenarios stage after push (taste bar)
</verification>

<success_criteria>
The Action Center renders on Dashboard + Optimize with big-checkmark items, benefit lines, due dates, disappearing Stage-0 items, and an achievement empty state; the scenarios stage shows A/B/Balanced (or merged) with knob-diffs, trade-offs, guard chips, Illustration badges, and a working choose→checklist flow; the checklist shows fact-gated steps with engine benefit lines and a header aggregate; all new components are born-premium; the nav badge combines both counts; educational framing holds and no dollars are computed client-side.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-10-SUMMARY.md` when done.
</output>