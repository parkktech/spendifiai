---
phase: 14-action-center-scenarios-design-elevation
plan: 09
type: execute
wave: 5
depends_on: ["14-08"]
files_modified:
  - app/Services/ChangeMonitor.php
  - app/Models/OptimizationCalendarEvent.php
  - database/migrations/2026_07_05_000001_create_optimization_calendar_events_table.php
  - app/Http/Controllers/Api/ActionCenterController.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - routes/console.php
  - routes/api.php
  - tests/Feature/ChangeMonitorTest.php
  - tests/Feature/ActionCenterTest.php
autonomous: true
requirements: [MON-01, MON-02, ACT-02, ACT-03, ACT-04]
must_haves:
  truths:
    - "ChangeMonitor unifies verification watch (checklist item done → 2-4-week window → verified outcome via the SavingsLedger claimed→verified pattern) and change detection (income shift ≥2 pay cycles, CrossSourceReview discrepancy, life-event) in ONE service (MON-01, D13.5/D14, ACT-04)"
    - "Change detection fires one prompt per detected change per freshness window, requires ≥2 pay-cycle persistence, dedupes against open findings, and is 28-day activity-gated (MON-01 cadence guard; stale users = zero work)"
    - "Predictive calendar watchers fire bonus lead-time alerts (prior-year/interview/offer-letter sourced) with config lead time, presenting the 3-option bonus set; year-end items are gated on confirmed business context + business type and carry the net-cost honesty statement (MON-02, D15/D16)"
    - "GET /optimizer/action-center returns Stage-0 items derived deterministically from live DB state (bank, credit-card accounts, email, interview completed, paystub ready) that disappear when completed, plus checklist/monitor/calendar items with benefit lines and due dates (ACT-02/ACT-03)"
    - "A nav badge counts unchecked Action Center checklist items via an additive pendingActionCount prop, leaving pendingOptimizationCount unchanged (ACT-01 support, backwards-compatible)"
  artifacts:
    - "app/Services/ChangeMonitor.php; create_optimization_calendar_events migration + model; ActionCenterController + route; HandleInertiaRequests pendingActionCount prop; change-monitor scheduled task"
  key_links:
    - "Income-shift persistence anchors on a dedicated optimization_calendar_events.metadata timestamp + OptimizationFinding.created_at dedupe — NOT report.stale_since (resolves research A3)"
    - "Detection emits OptimizationFinding(finding_type=change_detected) + AIQuestion + DOC-05 request with template (educational/benefit-forward) copy — zero Claude for standard copy (D14/D17)"
    - "Stage-0 completion checks: hasBankConnected; BankAccount type='credit'; hasEmailConnected; InterviewSession status='completed'; TaxDocument category=pay_stub status=ready (verified against live enums)"
---

<objective>
Build the ChangeMonitor (MON-01/02) and the Action Center backend (ACT-02/03/04, plus ACT-01 nav-badge support). One `ChangeMonitor` service unifies the verification watch (D13.5: checklist item done → claimed → verified when the projected change lands, reusing the SavingsLedger claimed→verified pattern) and change detection (D14: income shift ≥2 pay cycles, CrossSourceReview discrepancy, life-event → `OptimizationFinding(change_detected)` + AIQuestion + DOC-05 request with template copy), guarded by the cadence rule and the 28-day activity gate. Predictive calendar watchers (D15/D16) add bonus lead-time alerts and business-gated year-end items with the net-cost honesty statement, persisted in an additive `optimization_calendar_events` table. `ActionCenterController` aggregates Stage-0 onboarding items (deterministic from live DB state), checklist items, monitor prompts, and calendar items into one composed response with benefit lines and due dates; `HandleInertiaRequests` gains an additive `pendingActionCount` prop for the nav badge.

Purpose: The Action Center's backend spine — the lifecycle-adaptive item source plus the proactive monitor that verifies expected changes and surfaces unexpected ones.
Output: The ChangeMonitor service + scheduled task, the calendar-events store, the ActionCenterController + route, the badge prop, and the MON/ACT tests.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-RESEARCH.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-PATTERNS.md
@app/Services/ReportStalenessPolicy.php
@app/Models/SavingsLedger.php
@routes/console.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChangeMonitor — verification watch + income-shift detection + calendar store + scheduled task</name>
  <files>app/Services/ChangeMonitor.php, app/Models/OptimizationCalendarEvent.php, database/migrations/2026_07_05_000001_create_optimization_calendar_events_table.php, routes/console.php, tests/Feature/ChangeMonitorTest.php</files>
  <read_first>
    - 14-RESEARCH.md "ChangeMonitor Design" (checkVerificationWindows/detectIncomeShifts/runCalendarWatchers signatures, scheduling closure, calendar-events migration, income-shift persistence filter, Open Question 1 / Assumption A3 — use a dedicated anchor, not stale_since), "Don't Hand-Roll" (cadence via OptimizationFinding.created_at dedupe), Pitfall 8 (cadence guard gap)
    - app/Services/ReportStalenessPolicy.php isMaterialChange (reuse the income/savings threshold comparison), app/Models/SavingsLedger.php (status Claimed/Verified, source_type, source_id, monthly_savings, verified_at, month)
    - app/Models/OptimizationChecklistItem.php (done_at — verification side reads done items, 14-08); app/Models/OptimizationFinding.php (finding_type change_detected, deadline); CrossSourceReviewService + life-event (FLAG-27) finding sources
    - routes/console.php activity-gate precedent (28-day threshold, whereHas bankConnections); CLAUDE.md forward-only additive migration
  </read_first>
  <action>
    Create the forward-only additive migration create_optimization_calendar_events_table per 14-RESEARCH (id, user_id FK cascade, tax_year, event_type, expected_at, lead_time_days default 21, alert_fired_at nullable, metadata json — periods/types only, NO money values, timestamps, index [user_id, tax_year]). Create the OptimizationCalendarEvent model (scopeForUser).

    Create ChangeMonitor with checkVerificationWindows(userId, taxYear) [D13.5/ACT-04: for each checklist item done within the 2-4-week window, watch transaction/deposit data for the projected change; when it lands, record a SavingsLedger claimed→verified transition and mark the item verified] and detectIncomeShifts(userId, taxYear) [D14/MON-01: reuse ReportStalenessPolicy::isMaterialChange for the income comparison; require ≥2 pay-cycle persistence anchored on a dedicated optimization_calendar_events.metadata detection timestamp (NOT report.stale_since — resolves A3); dedupe against any open OptimizationFinding(change_detected) with the same finding_key in the freshness window; only when persistence + dedupe + activity gate all pass, emit OptimizationFinding(finding_type='change_detected') + AIQuestion + DOC-05 document request with template (educational, benefit-forward) copy — zero Claude for standard copy]. Cadence guard: one prompt per detected change per freshness window (Pitfall 8).

    Add the additive daily change-monitor scheduled task to routes/console.php, activity-gated exactly like the shipped sweeps (whereHas bankConnections + last_active_at > now()->subDays(28)), calling checkVerificationWindows/detectIncomeShifts/runCalendarWatchers per user.

    ChangeMonitorTest: income shift with ≥2-pay-cycle persistence emits exactly one finding (a one-off bonus deposit does NOT); dedupe/no-nag within the freshness window; verification window marks a checklist item VERIFIED when the projected delta appears; all monitor paths skip inactive (>28d) users (zero work).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ChangeMonitor</automated>
  </verify>
  <done>Migration additive/applied; verification watch marks items verified via the SavingsLedger pattern; income-shift detection honors ≥2-cycle persistence + dedupe + activity gate; one-off deposits do not prompt; scheduled task activity-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar watchers — bonus lead-time alerts + business-gated year-end items (honesty guardrail)</name>
  <files>app/Services/ChangeMonitor.php, tests/Feature/ChangeMonitorTest.php</files>
  <read_first>
    - SCENARIOS-SPEC out-of-scope note (federal-only; bonus is a scenario domain within D10) + 14-CONTEXT D15 (bonus prediction sources + 3-option set + config lead time) and D16 (year-end items gated on confirmed business context + business type; HONESTY GUARDRAIL verbatim binding text)
    - config/optimization-objectives.php bonus_election domain groundwork; config/optimizer-scenarios.php (bonus scenario knobs via the engine); OptimizationFinding.deadline / net_cash_cost / cliff_bonus_value fields
    - 14-CONTEXT deferred list: YEAR-01 full Q4 engine is OUT — build the predictive watcher infrastructure only, not the full bracket-trajectory projector
  </read_first>
  <action>
    Extend ChangeMonitor::runCalendarWatchers(): (a) Bonus lead-time alerts (D15/MON-02) — predict the bonus from prior-year pattern (IncomeDetectorService/history), interview fact, or offer-letter extraction; when now is within the config lead time of the expected payroll cutoff, fire an alert presenting the 3-option bonus set (Option A 0% deferral/max cash, Option B max deferral/bracket management, Option C standing election) with take-home/withholding/401k-headroom figures computed by the engine; persist as an optimization_calendar_events row (event_type='bonus') and set alert_fired_at (dedupe). (b) Year-end window items (D16) — GATED on the user's confirmed business/personal context AND confirmed business type; every purchase-timing item carries the net-cost HONESTY GUARDRAIL statement verbatim from D16 (net cash vs tax saved, "only if you needed it anyway"); use finding deadline/net_cash_cost per FLAG-13 fields. All standard copy is template-driven (zero Claude). Scope to watcher infrastructure only — no full Q4 bracket-trajectory engine (deferred).

    Extend ChangeMonitorTest: a calendar watcher fires the bonus alert with the configured lead time and does not re-fire (alert_fired_at dedupe); a year-end item renders only when business context + business type are confirmed and always includes the net-cost honesty statement; watchers are activity-gated.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ChangeMonitor && vendor/bin/pint --dirty</automated>
  </verify>
  <done>Bonus alerts fire with config lead time and dedupe on alert_fired_at; year-end items are business-context-gated and carry the honesty guardrail verbatim; all standard copy is zero-Claude; pint clean.</done>
</task>

<task type="auto">
  <name>Task 3: ActionCenterController (Stage-0 derivation + aggregation + benefit/due) + nav badge prop</name>
  <files>app/Http/Controllers/Api/ActionCenterController.php, app/Http/Middleware/HandleInertiaRequests.php, routes/api.php, tests/Feature/ActionCenterTest.php</files>
  <read_first>
    - 14-RESEARCH.md "Action Center Data Model" (composed-query controller — NOT a new ActionItem model; response shape; Stage-0 completion checks) + DRIFT-08 (credit-card check via BankAccount type='credit') + DRIFT-09 (pendingActionCount additive prop, keep pendingOptimizationCount)
    - RESOLVED anchors: TaxDocumentCategory::PayStub='pay_stub' + DocumentStatus::Ready='ready'; InterviewSession status string 'completed'; User::hasBankConnected()/hasEmailConnected()
    - app/Http/Middleware/HandleInertiaRequests.php pendingOptimizationCount (~L66, guarded by hasBankConnected) — add the sibling prop the same way; routes/api.php optimizer group (add optimizer/action-center INSIDE the auth:sanctum group, NO bank.connected)
  </read_first>
  <action>
    Create ActionCenterController::show returning the composed response (14-RESEARCH shape): stage0_items derived deterministically from live DB state — link_bank (hasBankConnected), link_credit_cards (BankAccount where user_id + type='credit' exists), link_email (hasEmailConnected), do_interview (InterviewSession forUser status='completed' exists), upload_paystub (TaxDocument where category=TaxDocumentCategory::PayStub + status=DocumentStatus::Ready exists) — each item omitted/marked complete once its prerequisite condition is met (endpoint always recomputes; no Stage-0 done-state persistence, ACT-02); checklist_items (optimization_checklist_items WHERE done_at IS NULL, with benefit_line_params → benefit lines, ACT-03); monitor_prompts (OptimizationFinding finding_type='change_detected' status open); calendar_items (optimization_calendar_events with alert ready, due dates); total_open + empty flag. Register GET optimizer/action-center additively.

    HandleInertiaRequests: add an additive pendingActionCount prop querying optimization_checklist_items WHERE user_id + done_at IS NULL (guarded by hasBankConnected, 0 otherwise); leave pendingOptimizationCount byte-for-byte unchanged (backwards compat, ACT-01 nav-badge support).

    ActionCenterTest: the Stage-0 derivation matrix (each of the five items flips completed only when its real DB condition holds — incl. the credit-card type query and the paystub category+status query); checklist/monitor/calendar aggregation with benefit lines + due dates; pendingActionCount reflects unchecked items and is 0 without a bank; cross-user scoping.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ActionCenter && vendor/bin/pint --dirty</automated>
  </verify>
  <done>Action Center endpoint returns deterministic Stage-0 items + aggregated checklist/monitor/calendar items with benefit lines and due dates; pendingActionCount is additive and back-compatible; Stage-0 checks use the verified enums; cross-user scoped; pint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduled monitor→AI/doc requests | Background detection can generate cost + prompts |
| bank/deposit data→detection | Transaction patterns drive change findings |
| client→action-center API | Authenticated user reads their aggregated items |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09-01 | Denial of Service (trust/noise) | change detection nagging | high | mitigate | ≥2-pay-cycle persistence + freshness-window dedupe + activity gate (MON-01 cadence guard, Pitfall 8) |
| T-14-09-02 | Denial of Service (cost) | monitor Claude/doc requests | high | mitigate | Template copy (zero Claude); all monitor paths 28-day activity-gated |
| T-14-09-03 | Information Disclosure | money in calendar metadata | medium | mitigate | metadata stores periods/types only, no money values (schema note) |
| T-14-09-04 | Elevation of Privilege | cross-user action items | medium | mitigate | Controller scoped to auth()->id(); pendingActionCount guarded by the user's own bank state |
| T-14-09-05 | Repudiation / Harm | year-end "buy to save tax" advice | high | mitigate | Business-context + business-type gate; verbatim net-cost honesty guardrail on every purchase-timing item (D16) |
| T-14-09-SC | Tampering | package installs | low | accept | Zero new packages; migration additive/forward-only |
</threat_model>

<verification>
- `php artisan test --compact --filter=ChangeMonitor` — verification watch, income-shift persistence/dedupe, bonus/year-end watchers, activity gate
- `php artisan test --compact --filter=ActionCenter` — Stage-0 derivation matrix, aggregation, badge prop
- `php artisan test --compact` — full suite, zero NEW failures
- `vendor/bin/pint --dirty`
</verification>

<success_criteria>
One ChangeMonitor service verifies expected changes and surfaces persistent unexpected changes without nagging, activity-gated; calendar watchers fire bonus alerts and business-gated year-end items with the honesty guardrail; the Action Center endpoint composes deterministic Stage-0 + checklist/monitor/calendar items with benefit lines and due dates; the nav badge extension is additive and back-compatible.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-09-SUMMARY.md` when done.
</output>
