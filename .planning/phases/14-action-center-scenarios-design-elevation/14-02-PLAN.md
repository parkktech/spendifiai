---
phase: 14-action-center-scenarios-design-elevation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/Services/TaxRulesEngineService.php
  - tests/Unit/TaxRulesEngineScenarioTest.php
  - tests/Feature/Scenarios/AcaInvariantTest.php
  - tests/Unit/NoLiteralGuardTest.php
autonomous: true
requirements: [SCN-04]
must_haves:
  truths:
    - "TaxRulesEngineService computes SCN-01 per-paycheck withholding, SCN-02 employee FICA, SCN-03 match capture, SCN-04 FV range, SCN-05 projected MAGI, SCN-06 ACA headroom, and SCN-07 full-vector outcome — all cents-in/cents-out, zero Claude/HTTP (D10, D17)"
    - "computeScenarioOutcome clamps a COPY of the knob vector regardless of input, and the ACA cliff guard (cliff-before-Roth) runs AFTER limits and BEFORE tax math (§B.4 Step 2)"
    - "For any marketplace-enrollee baseline, no emitted outcome exceeds cliff−buffer OR it flags aca_cliff_applied with Roth knobs at 0 (§B.5 invariant, proven by a 200-baseline property test)"
    - "futureValueRangeCents returns a RANGE (low/high) with an assumptions array — never a single guaranteed figure (D9.7 illustration)"
    - "Every threshold/limit traces to config (tax-rules.php or optimizer-scenarios.php) — no raw numeric literal in the new methods (no-literal grep guard)"
  artifacts:
    - "app/Services/TaxRulesEngineService.php with SCN-01…SCN-07 public methods + private helpers (append-only, no existing signature changed)"
    - "tests/Unit/TaxRulesEngineScenarioTest.php, tests/Feature/Scenarios/AcaInvariantTest.php, tests/Unit/NoLiteralGuardTest.php"
  key_links:
    - "single_or_mfs W-4 status maps to the single withholding table (Pub 15-T column convention, M11); annual-tax math always uses the true confirmed status"
    - "callers pass integer CENTS; config stores whole dollars and the engine converts internally (Pitfall 5)"
---

<objective>
Add the seven pure scenario computation methods (SCN-01…SCN-07) to `TaxRulesEngineService` per SCENARIOS-SPEC §B.3–B.6. These are the deterministic dollar-math core of D10: W-4 per-paycheck withholding approximation, employee-share FICA (§125 HSA FICA-exempt, 401k not), employer match capture, long-horizon FV-range illustration (D9.7), projected MAGI, ACA cliff headroom, and the full knob-vector outcome (`computeScenarioOutcome`) with ordered clamps and the hard cliff-before-Roth ACA guard. Zero Claude, zero HTTP, integer cents throughout, every number from config.

Purpose: All scenario dollar figures originate here so downstream (solver, controllers, checklist benefit lines) never compute money elsewhere — keeping EstimatedValueGuard green and SAFE-03 intact.
Output: Seven append-only engine methods + engine unit tests + the ACA 200-baseline property test + a no-literal grep guard.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-VALIDATION.md
@.planning/reference/SCENARIOS-SPEC.md
@app/Services/TaxRulesEngineService.php
@config/tax-rules.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SCN-01/02/03 — withholding, employee FICA, match capture</name>
  <files>app/Services/TaxRulesEngineService.php, tests/Unit/TaxRulesEngineScenarioTest.php</files>
  <behavior>
    - estimatePeriodWithholdingCents: MFJ, biweekly, gross $3,000, 401k $300 pretax, 2 kids under 17 → matches a hand-computed Pub 15-T percentage-method figure using standardDeductionCents + computeBracketTax − CTC credits, floored at 0
    - single_or_mfs status uses the 'single' bracket/standard-deduction tables (M11); head_of_household uses its own
    - employeeFicaCents: annual FICA wages below ss_wage_base returns ss_rate/2 + medicare_rate/2 split; above the wage base caps social-security at ss_wage_base; additional-medicare surtax excluded (documented)
    - matchCaptureCents: gross × min(contributionPct, thresholdPct) × matchPct, pcts as 0–100 floats; contribution below threshold returns the reduced capture; at/above threshold returns the full match
  </behavior>
  <read_first>
    - SCENARIOS-SPEC §B.3 (SCN-01/02/03 signatures + step comments), §B.1 K1 [M11] single/MFS mapping note
    - TaxRulesEngineService.php existing computeTax/computeBracketTax/standardDeductionCents/validateYear/validateFilingStatus (reuse; do NOT change signatures) + the cents/dollars convention docblock
    - config/tax-rules.php se_tax.* (ss_rate, medicare_rate, ss_wage_base), detection.ctc_amount, detection.odc_amount, brackets, standard_deduction
  </read_first>
  <action>
    Write the failing TaxRulesEngineScenarioTest cases for SCN-01/02/03 first (hand-computed expected cent values at bracket/threshold boundaries), then implement the three append-only public methods per §B.3. estimatePeriodWithholdingCents: map single_or_mfs→single tables (document the M11 divergence in a code comment), annualWages = (periodGross − periodPreTax) × periods, adjusted = max(0, annualWages − standardDeductionCents(status)), tentative = computeBracketTax(adjusted, brackets[status]), credits = deps17 × ctc_amount×100 + depsOther × odc_amount×100, return max(0, round((tentative − credits)/periods)). employeeFicaCents: ss/medicare employee halves from config with ss_wage_base cap, additional-medicare intentionally excluded. matchCaptureCents per the formula. All values from config — no literals. Reuse existing validators.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TaxRulesEngineScenario</automated>
  </verify>
  <done>SCN-01/02/03 tests green with exact hand-computed cents; single_or_mfs→single mapping verified; FICA wage-base cap verified; no existing engine test regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SCN-04/05/06/07 — FV range, MAGI, ACA headroom, computeScenarioOutcome</name>
  <files>app/Services/TaxRulesEngineService.php, tests/Unit/TaxRulesEngineScenarioTest.php</files>
  <behavior>
    - futureValueRangeCents: annuity FV at growth_low and growth_high from optimizer-scenarios.assumptions → {low_cents, high_cents, horizon_years, growth_rate_low, growth_rate_high, assumptions[]}; low ≤ high; zero/negative horizon → zeros with horizon_years=0
    - projectedMagiCents ≈ gross + se_income − trad401k − hsa − deductible trad IRA (partial-limit-capped)
    - acaCliffHeadroomCents: positive = room below the 400%-FPL cliff, negative = over; threshold selection mirrors AcaCliffMonitor (married_joint → family4 threshold else single)
    - computeScenarioOutcome (SCN-07): clamps a COPY (401k limit incl. catch-ups, mandatory-Roth-catchup forced Roth, HSA coverage/Medicare-lookback, shared-IRA combined-YTD + Roth phase-out + trad deductibility, transfer surplus cap), THEN the ACA cliff guard (Roth401k→Trad, then RothIRA→TradIRA, then flag), THEN annual tax (confirmed status) + per-paycheck take-home + retirement deltas; returns the §B.6 shape incl. the clamped/guarded vector and guards.clamps[]
  </behavior>
  <read_first>
    - SCENARIOS-SPEC §B.3 (SCN-04/05/06/07 signatures), §B.4 (computeScenarioOutcome 5-step algorithm), §B.5 (ACA invariant), §B.6 (outcome shape)
    - TaxRulesEngineService existing remaining401kRoomCents / remainingIraRoomCents / remainingHsaRoomCents / rothIraEligibility / traditionalIraDeductibility / requiresMandatoryRothCatchup / marginalRate (reuse for clamps)
    - config/optimizer-scenarios.php assumptions (growth rates, aca_cliff_buffer_cents), config/tax-rules.php detection.aca_* + 401k/ira/hsa limits + catch-ups
  </read_first>
  <action>
    Write failing tests first for the §T.9 vector cases (a) MFJ 3-kids W-4-misaligned baseline, (b) age-61 super-catch-up clamp, (c) mandatory-Roth catch-up reassignment, (d) Roth-IRA phase-out cap, (e) shared-IRA combined-YTD clamp, (f) Medicare HSA guard, (g) safe-harbor floor when prior-year liability absent — plus SCN-04 range monotonicity/zero-horizon and SCN-05/06 headroom. Then implement the four methods. computeScenarioOutcome MUST clamp a copy (never mutate/trust input — Pitfall 4/security boundary) and run the ACA cliff guard AFTER limit clamps and BEFORE any tax math (cliff-before-Roth, §0.5 binding); subsidy/clawback dollars are NEVER computed (FLAG-22 rail). Append the guards array (aca_cliff_applied, aca_need_remaining_cents, mandatory_roth_catchup_applied, medicare_hsa_guard, safe_harbor_floor_applied, clamps[]). All thresholds from config.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TaxRulesEngineScenario</automated>
  </verify>
  <done>All §T.9 vector cases green; computeScenarioOutcome returns the §B.6 shape with correct clamps/guards; ACA guard forces Roth→Traditional before tax math; FV output is always a labeled range.</done>
</task>

<task type="auto">
  <name>Task 3: ACA invariant property test + no-literal grep guard</name>
  <files>tests/Feature/Scenarios/AcaInvariantTest.php, tests/Unit/NoLiteralGuardTest.php</files>
  <read_first>
    - SCENARIOS-SPEC §B.5 (the testable invariant statement), §T.10 (200-baseline property test), §T.13 + §F (no-literal grep guard, FLAG-08 materiality-test pattern)
    - an existing property-style or randomized Pest test in tests/ (analog for seeding randomized baselines)
    - 14-VALIDATION.md Validation Architecture item 2 (ACA-cliff arithmetic guard invariant)
  </read_first>
  <action>
    AcaInvariantTest: generate 200 randomized marketplace-enrollee baselines with MAGI near the 400%-FPL cliff (vary gross, filing status, YTD elections). For each, run computeScenarioOutcome across the solver knob candidates and assert the §B.5 invariant: projectedMagiCents(baseline, outcome.knobs) ≤ cliff − buffer, OR guards.aca_cliff_applied is true with aca_need_remaining_cents > 0 and both Roth knobs at 0. Mark this test for the CI property profile per 14-CONTEXT.

    NoLiteralGuard: statically read the source of the new SCN-01…SCN-07 methods in TaxRulesEngineService and assert no raw numeric threshold literal appears (money/limit/rate values must come from config('tax-rules.*') or config('optimizer-scenarios.*')). Filter comment lines before counting (grep hygiene) and scope the assertion to the new methods' line range only. Small structural constants (e.g. divisor 100 for cents, loop bounds) may be allowlisted explicitly.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=AcaInvariant && php artisan test --compact --filter=NoLiteralGuard && vendor/bin/pint --dirty</automated>
  </verify>
  <done>ACA invariant holds across 200 randomized baselines; the no-literal guard passes over the new engine methods; pint clean; full engine suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client knobs → engine | Untrusted knob vectors reach the computation (via later controllers) |
| marketplace enrollee MAGI | A Roth allocation could push a subsidized enrollee over the ACA cliff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-01 | Tampering | computeScenarioOutcome knob input | high | mitigate | Engine clamps a COPY regardless of input (security boundary, Pitfall 4); validation is UX-only |
| T-14-02-02 | Harm (financial) | ACA cliff via Roth knob | high | mitigate | Hard arithmetic cliff-before-Roth guard (§B.4 Step 2); 200-baseline invariant property test |
| T-14-02-03 | Information Disclosure | dollar figures leaking to Claude | high | mitigate | Engine is pure — zero Claude/HTTP; all figures stay in cents on server; SAFE-03 preserved |
| T-14-02-04 | Tampering | hardcoded IRS constants | medium | mitigate | No-literal grep guard forces every threshold to config |
| T-14-02-SC | Tampering | package installs | low | accept | Zero new packages this phase |
</threat_model>

<verification>
- `php artisan test --compact --filter=TaxRulesEngineScenario` — SCN-01…07 boundary + vector cases
- `php artisan test --compact --filter=AcaInvariant` — 200-baseline cliff invariant
- `php artisan test --compact --filter=NoLiteralGuard` — every threshold traces to config
- `php artisan test --compact` — full suite, zero NEW failures (EstimatedValueGuardTest stays green)
- `vendor/bin/pint --dirty`
</verification>

<success_criteria>
Seven append-only pure engine methods compute every scenario figure in cents from config with zero Claude; computeScenarioOutcome clamps defensively and enforces cliff-before-Roth; the ACA invariant is proven over 200 randomized baselines; no raw literals in the new methods; no existing engine signature or test changed.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-02-SUMMARY.md` when done.
</output>
