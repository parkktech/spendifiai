---
phase: 14-action-center-scenarios-design-elevation
plan: 05
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - app/Services/ObjectiveReadinessService.php
  - app/Services/InterviewOrchestratorService.php
  - app/Services/AI/PaystubFactExtractorService.php
  - app/Services/IncomeOptimizerDataAssemblerService.php
  - app/Http/Controllers/Api/OptimizationObjectiveController.php
  - app/Http/Controllers/Api/InterviewController.php
  - routes/api.php
  - tests/Feature/Scenarios/ObjectiveReadinessTest.php
  - tests/Feature/Scenarios/ObjectiveEnqueueTest.php
  - tests/Feature/Scenarios/PrefillPointerTest.php
autonomous: true
requirements: [SCN-01]
must_haves:
  truths:
    - "ObjectiveReadinessService computes per-objective readiness (blocking_missing, confirm_needed, optional_missing, questions_to_unlock) in two tiers (known for math, confirmed for directives) as the SINGLE readiness source (SCN-01, M5)"
    - "A prerequisite answered 'no' flips dependent blocking facts to not_applicable (counts as resolved); T4 W-4 facts are optional-with-suppression, never blocking (M12)"
    - "enqueueGaps front-inserts blocking-missing gap keys before pending finding keys, deduped vs queue/asked/answered, battery stays last, initial_cap not applied (§A.4.1, M6 — the only enqueue path)"
    - "Gap questions built from config question_templates reach the user with ZERO Claude calls (D17 template-first; SCN-03 path); typed answer conversion (money→cents, choice→validate, integer/year) applies via the template with 422 on mismatch"
    - "Suggested-confirm questions store only a prefill_source POINTER — never a dollar value — in AIQuestion.options; InterviewController::next() resolves prefill_display/prefill_value transiently at read time (SAFE-03, §A.5.5)"
    - "GET /optimizer/objectives/{year} returns keys/labels/states only — no money values in the body (§E, no /\\d{4,}/ regression)"
  artifacts:
    - "app/Services/ObjectiveReadinessService.php; OptimizationObjectiveController + 2 routes; InterviewController::next() additive fields; PaystubFactExtractorService additive maps + RETIREMENT_STATEMENT_FACT_MAP; assembler estimated_age backfill"
  key_links:
    - "readiness() consumes ScenarioFactResolverService::resolveAll (14-03); the config fact map + templates (14-01) drive both readiness and template questions"
    - "createOptimizationQuestion gains an additive early branch: template exists → build AIQuestion from template, skip wordQuestion() entirely (D17)"
---

<objective>
Build the objective readiness engine and the additive interview/extractor touches that make objective-driven data acquisition work (SCN-01, and the SCN-03 template-question path). `ObjectiveReadinessService` (the single readiness source, M5) consumes the resolver to compute two-tier readiness and front-inserts gap questions via `enqueueGaps()` (M6, the only enqueue path). The orchestrator gains additive branches — the deterministic template-question path in `createOptimizationQuestion()` (D17 zero-Claude), config-merged prerequisite gates, typed answer conversion + template volatility/tax_year in `recordAnswer()`, and prefill-pointer resolution — plus additive `PaystubFactExtractorService` maps, a new `RETIREMENT_STATEMENT_FACT_MAP`, and the assembler `estimated_age` backfill. Finally `OptimizationObjectiveController` exposes readiness + enqueue, and `InterviewController::next()` gains the additive Phase-14 response fields.

Purpose: Users see "answer 2 more questions to unlock Retirement optimization," the count ticks down per answer, and every gap question is a zero-Claude template.
Output: The readiness service, additive orchestrator/extractor/assembler changes, the objectives controller + routes, the interview-next additive fields, and tests (§T.3–6, §T.8).

SCN-03 traceability (split delivery): SCN-03 is split — the deterministic question TEMPLATES live in 14-01 (config); THIS plan delivers the SCN-03 enqueue endpoint (the `createOptimizationQuestion()` template branch + the ObjectiveEnqueueTest `assertNothingSent` zero-Claude proof). Attribute SCN-03's binding zero-Claude acceptance to 14-05 and its template groundwork to 14-01.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-RESEARCH.md
@.planning/reference/SCENARIOS-SPEC.md
@app/Services/InterviewOrchestratorService.php
@app/Http/Controllers/Api/InterviewController.php
@app/Services/AI/PaystubFactExtractorService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ObjectiveReadinessService — two-tier readiness + enqueueGaps</name>
  <files>app/Services/ObjectiveReadinessService.php, tests/Feature/Scenarios/ObjectiveReadinessTest.php</files>
  <behavior>
    - readiness(user, year) returns, per objective, blocking_missing / confirm_needed / optional_missing / ready / questions_to_unlock / completeness_pct computed over the config fact map + resolveAll()
    - readiness/enqueue iterate ONLY the three canonical readiness objectives (take_home, tax_burden, retirement); the config `bonus_election` scenario domain (14-01 D15 groundwork, tagged is_scenario_domain) is NOT a readiness objective and is skipped by readiness/enqueue (it is consumed by 14-09 calendar watchers, never surfaced as a readiness card)
    - a blocking fact resolved but confirmed=false lands in confirm_needed and does NOT block readiness; ready = count(blocking_missing)==0
    - conditional-blocking facts (B5/B6 HSA, R6/R7 match) only block when their condition holds; a prerequisite answered 'no' flips dependents to not_applicable (resolved)
    - T4 (w4.filing_status / w4.dependents_claimed) is optional-with-suppression — surfaced under confirm/ask, never in blocking_missing (M12)
    - enqueueGaps(user, year, objective) startOrResume, front-inserts template-covered blocking keys before existing queue keys, dedupes vs queue∪asked∪answered, keeps battery last, and ignores initial_cap; idempotent on double call
  </behavior>
  <read_first>
    - SCENARIOS-SPEC §A.7 (two-tier), §A.8.1 (readiness DTO + computation, M5 single source), §A.4.1 (enqueueGaps front-insert, M6), §A.4.3 (prerequisite→not_applicable rule), §A.2 (blocking/optional/conditional per objective), §E (GET objectives response — keys/labels/states only)
    - app/Services/ScenarioFactResolverService.php (resolveAll from 14-03), app/Services/InterviewOrchestratorService.php startOrResume/buildInitialQueue/isAlreadyAnswered
    - 14-VALIDATION.md item 3 (readiness tick-down, known-vs-confirmed gating)
  </read_first>
  <action>
    Write failing ObjectiveReadinessTest first (blocking vs optional; not_applicable via prerequisite='no'; questions_to_unlock; conditional-blocking B5/B6+R6/R7; T4 suppression; enqueue front-insert order + dedupe + battery-last + initial_cap-not-applied + idempotent double-POST). Then implement ObjectiveReadinessService with the resolver + orchestrator injected. readiness() is the ONLY readiness computer (M5 — no optimizer-scenarios readiness config). enqueueGaps() is the ONLY enqueue path (M6). completeness_pct is presentational only (blocking double-weighted; not_applicable counts as resolved). No caching in v1.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ObjectiveReadiness</automated>
  </verify>
  <done>Readiness two-tier + conditional-blocking + not_applicable + T4-suppression tests green; enqueue front-insert/dedupe/battery-last/idempotent green.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrator additive branches + paystub maps + assembler estimated_age backfill</name>
  <files>app/Services/InterviewOrchestratorService.php, app/Services/AI/PaystubFactExtractorService.php, app/Services/IncomeOptimizerDataAssemblerService.php, tests/Feature/Scenarios/PrefillPointerTest.php</files>
  <read_first>
    - SCENARIOS-SPEC §A.3 (template branch in createOptimizationQuestion — build from template, skip wordQuestion), §A.4.3 (config-merged gate map alongside GATED_PROBES), §A.5.3 (recordAnswer typed conversion + template volatility/taxYear; non-template keys keep legacy behavior), §A.5.4 (PaystubFactExtractorService additive maps + RETIREMENT_STATEMENT_FACT_MAP, all proposals/D4-gated), §A.5.5 (suggested-confirm prefill_source pointer), §A.6.4 (assembler estimated_age backfill)
    - app/Services/InterviewOrchestratorService.php createOptimizationQuestion(), wordQuestion(), recordAnswer() (currently hardcodes volatility 'stable'/taxYear null), GATED_PROBES const, BAND_CONFIDENCE
    - app/Services/AI/PaystubFactExtractorService.php PAYSTUB_FACT_MAP + proposeFacts + yes/no convention; app/Services/IncomeOptimizerDataAssemblerService.php buildProfile()
    - 14-CONTEXT specifics: "detectors must NOT be rewritten to use canonical keys — only the resolver uses the alias map"; DRIFT-02 (recordAnswer additive guard)
  </read_first>
  <action>
    createOptimizationQuestion(): add an additive early branch — if $factKey exists in config('optimization-objectives.question_templates'), build the AIQuestion from the template (question text; options carry choices/answer_type/objective_tags; suggested-confirm sets options.prefill_source POINTER with band=auto/ai_confidence=1.0 per BAND_CONFIDENCE) and skip wordQuestion() entirely (D17). Finding-driven questions keep the existing Claude path untouched.

    Prerequisite gates: merge GATED_PROBES with config('optimization-objectives.prerequisites') pairs at construction (array_merge; const wins on collision). Do not touch buildInitialQueue ordering.

    recordAnswer(): before recordFact, look up the template for $factKey; if answer_type='money_dollars' convert dollars→cents-string, validate integer/year digit-strings, validate choice against template choices (422 on mismatch); set volatility + taxYear from the template (tax_year_scoped→session tax year, else null) — ONLY when a template exists. Non-template keys keep the exact current hardcoded behavior (DRIFT-02). Suggested-confirm: resolve the prefill pointer server-side at answer time and recordFact the resolved value as source_type='interview_answer'.

    PaystubFactExtractorService: additive PAYSTUB_FACT_MAP entries (federal_tax_withheld→pay.federal_withholding_per_period_cents, gross_pay→pay.gross_per_period_cents; money/annual/tax_year-scoped) and a NEW RETIREMENT_STATEMENT_FACT_MAP (account_balance→retirement.statement_balance_cents, ytd_contributions→retirement.statement_ytd_contributions_cents cross-check only). All writes stay proposals (source_type='document_extraction', D4 confirm gate untouched). Pay-frequency derivation is NOT added here (M7 — it lives in the resolver).

    Assembler buildProfile(): additively backfill estimated_age from UserTaxFact::currentFact(userId,'person.birth_year') when present (taxYear − birth_year). No column/API change.

    PrefillPointerTest: assert a suggested-confirm AIQuestion.options contains no dollar value (no /\\d{4,}/ pattern) and a prefill_source pointer instead; assert answering confirm records the resolved value as interview_answer.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=PrefillPointer && php artisan test --compact --filter=Interview</automated>
  </verify>
  <done>Template branch skips Claude for template keys; gate map merged; recordAnswer typed conversion + template volatility/taxYear applied only for template keys (legacy behavior preserved otherwise); paystub maps + RETIREMENT_STATEMENT_FACT_MAP added as proposals; estimated_age backfilled; no dollar value in stored options; existing interview tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: OptimizationObjectiveController + routes + InterviewController::next() additive fields</name>
  <files>app/Http/Controllers/Api/OptimizationObjectiveController.php, app/Http/Controllers/Api/InterviewController.php, routes/api.php, tests/Feature/Scenarios/ObjectiveEnqueueTest.php</files>
  <read_first>
    - SCENARIOS-SPEC §E (GET /optimizer/objectives/{year} response shape — keys/labels/states only; POST enqueue response; InterviewController::next() additive fields objective_tags/answer_type/choices/prefill_display/prefill_value/doc_affordance), §A.5.5 (transient prefill resolution)
    - 14-RESEARCH DRIFT-01 (current next() response shape L83-96 — additive keys only, existing keys untouched)
    - routes/api.php optimizer group (~L333) — add `optimizer/objectives` prefix INSIDE the existing auth:sanctum group (NO bank.connected, mirroring shipped optimizer groups), throttle 60,1 (show) / 10,1 (enqueue)
    - app/Http/Controllers/Api/InterviewController.php next() (add fields; do not remove/rename existing keys)
  </read_first>
  <action>
    Create OptimizationObjectiveController: show(year) returns ObjectiveReadinessService::readiness() projected to the §E shape (keys/labels/states, NO money values — safe to log); enqueue(year, objective) validates {objective} against config('optimization-objectives.objectives') keys (404 otherwise, Pitfall 2) and calls enqueueGaps(), returning session + enqueued keys + queue_size + message. Register the two routes additively in the existing optimizer group.

    InterviewController::next(): add the additive response fields per §E — objective_tags, answer_type, choices (from the template), prefill_display/prefill_value (transient: resolve options.prefill_source pointer at read time, never stored), doc_affordance. Existing keys stay byte-for-byte.

    ObjectiveEnqueueTest: template questions send zero HTTP to Anthropic (Http::fake + assertNothingSent — the SCN-03 zero-Claude proof); typed conversion money→cents; choice mismatch 422; POST 'income' (wrong id) → 404 (M4); GET objectives body contains no /\\d{4,}/ money values besides year/ids.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ObjectiveEnqueue && vendor/bin/pint --dirty</automated>
  </verify>
  <done>Objectives show/enqueue routes work under auth:sanctum without bank.connected; enqueue is zero-Claude (assertNothingSent); wrong objective id → 404; next() carries additive fields with transient prefill; objectives body has no money values; pint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→objectives/enqueue API | Authenticated user requests readiness/gap-enqueue |
| AIQuestion.options JSON | Unencrypted column that must never carry a dollar value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05-01 | Information Disclosure | prefill value in options JSON | high | mitigate | prefill_source pointer only; prefill_display/value resolved transiently at next() read time (SAFE-03, §A.5.5); PrefillPointerTest regression |
| T-14-05-02 | Denial of Service (cost) | gap questions via Claude | high | mitigate | Template-first branch skips wordQuestion for template keys (D17); ObjectiveEnqueueTest asserts assertNothingSent |
| T-14-05-03 | Information Disclosure | money in readiness API body | medium | mitigate | §E response is keys/labels/states only; no-/\\d{4,}/ regression test |
| T-14-05-04 | Input Validation | wrong objective id / typed answers | medium | mitigate | {objective} validated → 404; typed conversion 422 on mismatch |
| T-14-05-05 | Elevation of Privilege | cross-user interview/session | medium | mitigate | Endpoints scoped to auth()->id(); orchestrator session ownership preserved |
| T-14-05-SC | Tampering | package installs | low | accept | Zero new packages; all changes additive |
</threat_model>

<verification>
- `php artisan test --compact --filter=ObjectiveReadiness` — two-tier + conditional + enqueue
- `php artisan test --compact --filter=ObjectiveEnqueue` — zero-Claude + typed conversion + 404
- `php artisan test --compact --filter=PrefillPointer` — no money in options
- `php artisan test --compact` — full suite; existing interview tests unaffected
- `vendor/bin/pint --dirty`
</verification>

<success_criteria>
The single readiness source computes two-tier per-objective readiness that ticks down per answer; gap questions are zero-Claude config templates with typed conversion; suggested-confirm uses pointers only; the objectives API and additive interview-next fields work without touching existing behavior.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-05-SUMMARY.md` when done.
</output>
