---
phase: 14-action-center-scenarios-design-elevation
plan: 07
type: execute
wave: 2
depends_on: ["14-04"]
files_modified:
  - resources/js/Components/SpendifiAI/StatCard.tsx
  - resources/js/Components/SpendifiAI/Badge.tsx
  - resources/js/Components/SpendifiAI/SubscriptionCard.tsx
  - resources/js/Pages/Dashboard.tsx
  - resources/js/Pages/Subscriptions.tsx
  - resources/js/Pages/Transactions.tsx
autonomous: true
requirements: [ELEV-02]
must_haves:
  truths:
    - "StatCard has the double-bezel anatomy: outer gradient frame + inner core, value at text-[28px] font-[800] font-tabular, sentence-case (non all-caps) label, semantic icon-container variants (D12 §3.3)"
    - "SubscriptionCard, Badge, and generic content cards use ring-1/gradient/shadow-sw-1 with card-lift hover; the cancelled/recharged/missed state tints are preserved (D12 §3.4/§3.7)"
    - "The Dashboard stat grid staggers in on mount via stagger-children; financial figures carry font-tabular; transaction rows use the gradient-fade hover (D12 §3.8)"
    - "npm run build and php artisan test --compact pass; the §6 preservation audit passes for Wave 2"
  artifacts:
    - "Upgraded StatCard.tsx (with iconVariant prop), Badge.tsx, SubscriptionCard.tsx, Dashboard.tsx, Subscriptions.tsx, Transactions.tsx"
  key_links:
    - "All recipes consume the Wave-1 tokens/utilities from 14-04 (shadow-sw-*, card-lift, font-tabular, stagger-children) — this plan adds no new tokens"
    - "Class replacements only — no API/route/field/label changes; existing subscription state hierarchy visually preserved"
---

<objective>
Deliver DESIGN-ELEVATION-SPEC Wave 2 (D12): upgrade the core dashboard surfaces to the premium recipes using the Wave-1 tokens. StatCard gets the double-bezel anatomy, display-scale tabular value, sentence-case label, and semantic icon-container variants; SubscriptionCard, Badge, and generic content cards get the ring-1/gradient/shadow-sw-1 treatment with `card-lift` hover (preserving the cancelled/recharged/missed state tints); the Dashboard stat grid staggers in and financial figures become tabular; Transaction rows get the gradient-fade hover. Pure presentational class replacements — zero API/route/field changes.

Purpose: The most-seen surfaces read premium before the new Phase-14 UI arrives, keeping visual consistency.
Output: Six upgraded frontend files passing the §5 Wave-2 audit checklist and §6 preservation audit.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/reference/DESIGN-ELEVATION-SPEC.md
@resources/js/Components/SpendifiAI/StatCard.tsx
@resources/js/Components/SpendifiAI/SubscriptionCard.tsx
@resources/js/Components/SpendifiAI/Badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatCard double-bezel + Badge gradient/ring variants</name>
  <files>resources/js/Components/SpendifiAI/StatCard.tsx, resources/js/Components/SpendifiAI/Badge.tsx</files>
  <read_first>
    - DESIGN-ELEVATION-SPEC §3.3 (StatCard before→after, the iconVariant table: accent/success/danger/warning/neutral), §3.7 (Badge before→after + status variant table)
    - resources/js/Components/SpendifiAI/StatCard.tsx (current props + class set) and Badge.tsx (current variants) — preserve every prop name and the existing variant API
  </read_first>
  <action>
    StatCard: apply the §3.3 after-recipe — outer gradient "bezel" frame wrapper (rounded-xl p-px gradient border, shadow-sw-1, card-lift), inner core (rounded-[calc(0.75rem-1px)] gradient bg, p-5, shadow-sw-inset), value at text-[28px] font-[800] tracking-[-0.035em] leading-none font-tabular, title as sentence-case text-[11px] font-medium tracking-[0.02em] (drop uppercase/tracking-wider). Add an `iconVariant` prop (accent default | success | danger | warning | neutral) mapping to the §3.3 gradient/ring container classes; keep the existing default behavior when the prop is absent. Do NOT rename or remove existing props.

    Badge: add the §3.7 gradient+ring+shadow treatments for success/warning/info/neutral variants and the notification-badge glass-edge (ring-1 ring-white/20 + accent shadow). Preserve the existing variant names and public API so all current call sites keep working.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Build + typecheck clean; StatCard renders double-bezel with tabular 28px value and sentence-case label and honors iconVariant; Badge variants render gradient+ring; no prop/API removed.</done>
</task>

<task type="auto">
  <name>Task 2: SubscriptionCard + Subscriptions grid + Transactions row hover</name>
  <files>resources/js/Components/SpendifiAI/SubscriptionCard.tsx, resources/js/Pages/Subscriptions.tsx, resources/js/Pages/Transactions.tsx</files>
  <read_first>
    - DESIGN-ELEVATION-SPEC §3.4 (SubscriptionCard state variants before→after: active/cancelled/recharged/missed), §3.5 (button recipes), §3.8 (table/transaction row hover + header cells)
    - resources/js/Components/SpendifiAI/SubscriptionCard.tsx (current state-variant class map — preserve the semantic hierarchy), Subscriptions.tsx grid, Transactions.tsx row + header markup
  </read_first>
  <action>
    SubscriptionCard: replace the border-based state variants with the §3.4 ring-1 + gradient + shadow variants (active → card-lift hover; cancelled+saved → emerald tint + shadow-sw-success; recharged → amber; missed → slate), preserving the existing status→style mapping so the visual hierarchy is unchanged. Upgrade its buttons to the §3.5 primary/secondary/danger recipes.

    Subscriptions.tsx: apply card-lift to the grid cards; upgrade the stat bars to the content-card recipe. Transactions.tsx: apply the §3.8 gradient-fade row hover and the non-all-caps 11px medium table header cells. Class replacements only — no data, filter, pagination, or label changes.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && php artisan test --compact 2>&1 | tail -15</automated>
  </verify>
  <done>Build clean + full PHP suite green; SubscriptionCard state tints preserved with card-lift; transaction rows use gradient-fade hover; no functional change.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard stagger + card upgrades + font-tabular + Wave-2 audit</name>
  <files>resources/js/Pages/Dashboard.tsx</files>
  <read_first>
    - DESIGN-ELEVATION-SPEC §3.4 (content cards), §4.2 (stagger-children usage — apply to the stat card grid only, NOT table rows), §5 Wave-2 audit checklist, §6 preservation audit
    - resources/js/Pages/Dashboard.tsx (stat card grid container, inline card divs, financial figure spans) — preserve every section, chart, and data binding
  </read_first>
  <action>
    Add `stagger-children` to the Dashboard stat-card grid container (entrance stagger on mount; do NOT apply to any table/list of many rows — performance, §4.2). Upgrade inline card divs that use `rounded-xl border border-sw-border bg-sw-card shadow-sm` to the §3.4 content-card recipe (ring-1 + gradient + shadow-sw-1; card-lift where interactive). Add `font-tabular` to financial figure spans (amounts, percentages). Do NOT rearchitect any section, chart, or data flow (anti-goal §7). Run the §5 Wave-2 audit checklist and the §6 preservation audit (taste-skill v2 blocking audits: shadow hierarchy, motion tastefulness, no all-caps eyebrow in consecutive sections, tabular numerals) and record ALL PASS in the SUMMARY — a single FAIL blocks the commit.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && php artisan test --compact 2>&1 | tail -10 && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Build + typecheck + full PHP suite green; stat grid staggers in on hard-refresh with no layout shift; financial figures are tabular; §5 Wave-2 + §6 preservation audits recorded ALL PASS; reduced-motion disables stagger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presentational TSX → UI | Class-only changes; no data/auth/route surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07-01 | Tampering | subscription state hierarchy | low | mitigate | §3.4 recipe preserves the status→tint mapping; §6 audit verifies visual hierarchy |
| T-14-07-02 | Denial of Service (perf) | stagger on large lists | low | mitigate | stagger-children applied to the stat grid only, never table rows (§4.2) |
| T-14-07-03 | Denial of Service (a11y) | new hover/entrance motion | low | mitigate | prefers-reduced-motion override (from 14-04); §6 audit gate |
| T-14-07-SC | Tampering | package installs | low | accept | Zero new packages; CSS/Tailwind-only motion |
</threat_model>

<verification>
- `npm run build` + `npx tsc --noEmit` — clean
- `php artisan test --compact` — full suite, zero NEW failures
- §5 Wave-2 audit checklist + §6 preservation audit (all PASS) recorded in SUMMARY
- taste-skill v2 blocking audits in the SUMMARY per frontend task
</verification>

<success_criteria>
StatCard, Badge, SubscriptionCard, Dashboard, Subscriptions, and Transactions read premium via the Wave-1 tokens with zero functional change; subscription state hierarchy and all data bindings preserved; build + tests clean; Wave-2 and preservation audits pass.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-07-SUMMARY.md` when done.
</output>
