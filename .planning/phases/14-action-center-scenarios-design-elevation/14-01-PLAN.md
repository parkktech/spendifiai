---
phase: 14-action-center-scenarios-design-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/tax-rules.php
  - config/optimizer-scenarios.php
  - config/optimization-objectives.php
  - config/services.php
  - config/optimization-report.php
  - app/Services/NarrationService.php
  - app/Services/OptimizationReportNarratorService.php
  - app/Services/InterviewOrchestratorService.php
  - app/Services/AI/TransactionCategorizerService.php
  - app/Jobs/GenerateOptimizationReport.php
  - app/Listeners/NarrateOptimizationFindings.php
  - app/Http/Controllers/Admin/AiUsageController.php
  - routes/api.php
  - tests/Unit/ClaudeBudgetTest.php
  - tests/Unit/TemplateFirstNarrationTest.php
  - tests/Unit/BannedPhraseTemplatesTest.php
  - tests/Feature/ActivityGateTest.php
autonomous: true
requirements: [SCN-03]
must_haves:
  truths:
    - "A gap question built from a config question_template reaches the user with ZERO Claude/HTTP calls (D17 template-first; SCN-03 zero-Claude path)"
    - "A standard finding narration whose finding_type has a config template returns deterministically without any HTTP call (D17 template-first)"
    - "Narration/wording call sites resolve claude-haiku-4-5 via services.anthropic.model_narration/model_wording, falling back to the global model when unset (D17 per-call-site model)"
    - "A daily budget cap at limit causes the Claude call site to skip gracefully (log + null return) with no HTTP request (D17 budget caps)"
    - "GenerateOptimizationReport and NarrateOptimizationFindings skip inactive users (>28d) and dispatch zero AI work (D17 activity gate)"
    - "GET /api/admin/ai-usage returns per-purpose daily call counters for the last 7 days (D17 admin spend surface)"
    - "The categorization Claude call site resolves claude-haiku-4-5 via services.anthropic.model_categorization (global fallback) and increments a `categorization` day-counter, while the existing confidence-threshold routing (≥0.85 auto / 0.60–0.84 review / question) is proven unchanged by TransactionCategorizerServiceTest (D17.2 — the biggest recurring-cost lever, behind the safety net)"
    - "Every Phase-14 deterministic-copy config template string contains no banned assertive/guaranteed-savings phrase (deterministic-copy liability closed)"
  artifacts:
    - "config/optimization-objectives.php with full §A.2 fact map, fact_aliases, pay_periods_per_year, all question_templates, prerequisites, plus the bonus_election scenario domain (D15 groundwork consumed by 14-09)"
    - "config/optimizer-scenarios.php with assumptions, grids, divergence, tax_objective_priority, tradeoff_templates, checklist_templates skeleton"
    - "config/tax-rules.php detection.odc_amount = 500"
    - "config/services.php anthropic per-purpose model keys + daily_budget_* keys"
    - "app/Http/Controllers/Admin/AiUsageController.php"
  key_links:
    - "NarrationService template-first branch reads config finding narration templates before any callClaude()"
    - "Every Claude call site increments a Cache day-counter and honors the per-purpose daily budget cap"
    - "Activity gate reuses config('spendifiai.sync.active_threshold_days', 28) and User::last_active_at (commit 4e75c46 precedent)"
---

<objective>
Establish the Phase-14 config foundations and the D17 AI Cost Discipline before any scenario/monitor code exists. This plan (a) creates the three additive config files every later plan reads — `config/optimization-objectives.php` (fact map + all question_templates = the SCN-03 deterministic templates), `config/optimizer-scenarios.php` (engine/solver assumptions), and `config/tax-rules.php` += `detection.odc_amount`; and (b) implements D17: per-call-site model config (narration/wording → Haiku), template-first zero-Claude paths for gap questions and standard finding narrations, per-purpose daily budget caps + call counters, the 28-day activity gate on the two remaining background AI paths, and the Admin spend surface.

Purpose: SCN-03's binding acceptance is "zero Claude calls" via "deterministic config-driven templates." D17 makes template-first the default and caps spend so the pre-revenue site never racks up AI bills. Both are load-bearing for every downstream plan.
Output: 3 config files, per-purpose model resolution + budget/counter + activity-gate plumbing across the sanctioned Claude call sites (narration, wording, and the D17.2 categorization tier), an admin AI-usage endpoint, and the D17/SCN-03 zero-call test harness.

SCN-03 traceability (split delivery): this plan ships the SCN-03 deterministic question/narration TEMPLATES (config in Task 1/Task 2); the SCN-03 enqueue endpoint plus the binding zero-Claude proof (`assertNothingSent`) ship in 14-05 (ObjectiveEnqueueTest). The requirement is split across 14-01 (templates) and 14-05 (enqueue + proof) so the verifier attributes each half correctly.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-RESEARCH.md
@.planning/reference/SCENARIOS-SPEC.md
@.planning/reference/enhanced-profile-integration-notes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three additive config files (odc, optimizer-scenarios, optimization-objectives)</name>
  <files>config/tax-rules.php, config/optimizer-scenarios.php, config/optimization-objectives.php</files>
  <read_first>
    - SCENARIOS-SPEC §A.1.2 (new fact keys), §A.1.3 (alias map — M3: do NOT alias w4.filing_status ↔ profile.filing_status), §A.2 (full fact-requirements map for take_home/tax_burden/retirement), §A.3 (config/optimization-objectives.php shape + question_templates), §F (config additions), §M (M1/M2/M4/M8 key names)
    - config/tax-rules.php existing `2026 => detection` block (find the array to append odc_amount into)
    - config/optimization-report.php (analog for config file style)
  </read_first>
  <action>
    Append `'odc_amount' => 500,` inside the existing `2026 => detection` block of config/tax-rules.php per DRIFT-04 and §F (Credit for Other Dependents, IRC §24(h)(4); $500 non-refundable). Additive only — touch no existing key.

    Create config/optimizer-scenarios.php returning `assumptions` (illustrative_growth_rate_low 0.04, illustrative_growth_rate_high 0.07, default_retirement_age 65, aca_cliff_buffer_cents 200000, min_take_home_ratio 0.90, min_take_home_ratio_cash_constrained 0.97, auto_transfer_max_surplus_share 0.80, income_objective_transfer_share 0.50, hsa_grid_step_cents 25000), `grids` (roth_share_pct [0,25,50,75,100], ira_room_fractions [0,0.25,0.5,0.75,1.0]), `divergence` epsilons per §C.1, `tax_objective_priority` [match_capture,hsa,trad_401k,trad_ira], `tradeoff_templates` (§C.3 token strings), and a `checklist_templates` skeleton (§D.5 step + benefit-line token slots per knob K1..K6). Per M5 do NOT add a `readiness` key; per M8 do NOT add a pay-frequency periods map here (it lives once in optimization-objectives.php).

    Create config/optimization-objectives.php returning: `objectives` (take_home, tax_burden, retirement — canonical M4 ids) each with a `facts` map (requirement_id => {canonical_key, blocking, chain, and suppresses where applicable}) transcribing every row of §A.2.1–A.2.4 including per-fact source chains and the M12 optional-with-suppression `w4_on_file` entry; `fact_aliases` per §A.1.3 (the three canonical→alias entries ONLY — the w4/profile filing-status alias stays deleted, M3); `pay_periods_per_year` (weekly 52, biweekly 26, semimonthly 24, monthly 12, M8); `question_templates` — one entry per askable fact in §A.2 with question text, answer_type (choice/integer/year/money_dollars), choices/min/max, volatility, tax_year_scoped, label, doc_affordance, and prerequisite where the fact is gated (§A.4.3); `prerequisites` gate pairs from §A.4.3. All fact keys use canonical names from §A.1.2 (M1 pay.frequency, M2 hsa.coverage_type). These templates are SCN-03's deterministic zero-Claude gap questions per D17.

    Additionally add a fourth `bonus_election` scenario domain to the `objectives` map (D15) following the SAME three-objective shape — a `facts` map for the bonus-relevant askable facts (bonus timing + expected-amount source pointers per D15's prior-year/interview/offer-letter prediction sources) plus `question_templates` for those facts — and tag it as a scenario domain (e.g. `is_scenario_domain => true`) so it is distinguishable from the three canonical readiness objectives. This is the D15 groundwork that 14-09's calendar watchers read (per 14-CONTEXT §specifics: "add a `bonus_election` objective domain … following the three-objective pattern"). It is a scenario domain within D10, NOT one of the three readiness objectives — readiness (14-05) still iterates only take_home/tax_burden/retirement.
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo config('tax-rules.2026.detection.odc_amount'); echo PHP_EOL; echo count(config('optimization-objectives.objectives')); echo PHP_EOL; echo count(config('optimizer-scenarios.assumptions')); echo PHP_EOL; echo (int) array_key_exists('take_home', config('optimization-objectives.objectives')); echo PHP_EOL; echo (int) array_key_exists('bonus_election', config('optimization-objectives.objectives'));"</automated>
  </verify>
  <done>odc_amount resolves to 500; config('optimization-objectives.objectives') contains the three canonical readiness ids (take_home, tax_burden, retirement) PLUS the bonus_election scenario domain; optimizer-scenarios assumptions/grids/divergence load; no `readiness` key in optimizer-scenarios; no w4↔profile filing-status alias in fact_aliases.</done>
</task>

<task type="auto">
  <name>Task 2: D17 per-call-site model config + template-first narration + budget caps + activity gate</name>
  <files>config/services.php, config/optimization-report.php, app/Services/NarrationService.php, app/Services/OptimizationReportNarratorService.php, app/Services/InterviewOrchestratorService.php, app/Services/AI/TransactionCategorizerService.php, app/Jobs/GenerateOptimizationReport.php, app/Listeners/NarrateOptimizationFindings.php</files>
  <read_first>
    - 14-RESEARCH.md "D17 AI Cost Discipline — Implementation Guide" (per-purpose model resolution pattern, call-counter pattern, activity-gate extension) and DRIFT-05/06/07
    - app/Services/NarrationService.php (constructor model line ~56, narrateFinding ~70, callClaude/Http::post pattern ~134-145, SYSTEM_PROMPT const)
    - app/Services/OptimizationReportNarratorService.php (its model resolution + narrateSection)
    - app/Services/InterviewOrchestratorService.php wordQuestion() (model read only — do not touch createOptimizationQuestion/recordAnswer here; those are owned by plan 14-05)
    - app/Jobs/GenerateOptimizationReport.php handle(); app/Listeners/NarrateOptimizationFindings.php handle()
    - app/Models/User.php isReturningUser()/syncTier() + config('spendifiai.sync.active_threshold_days', 28)
    - app/Services/AI/TransactionCategorizerService.php constructor model line ~31 (reads the global `services.anthropic.model`) + the CONFIDENCE_* routing constants (~L20-27) and the outbound Http::post call site (~L548) — the routing branch must stay byte-for-byte untouched; tests/Unit/Services/TransactionCategorizerServiceTest.php (the confidence-threshold routing safety net that MUST pass unchanged after the model swap)
  </read_first>
  <action>
    services.php: add to the `anthropic` block (additive) `model_narration` (env ANTHROPIC_MODEL_NARRATION default claude-haiku-4-5), `model_wording` (env ANTHROPIC_MODEL_WORDING default claude-haiku-4-5), `model_extraction` (env ANTHROPIC_MODEL_EXTRACTION default = existing global model), `model_categorization` (env ANTHROPIC_MODEL_CATEGORIZATION default claude-haiku-4-5 — D17.2), `daily_budget_narration` (env CLAUDE_DAILY_BUDGET_NARRATION default 200), `daily_budget_wording` (env default 100), `daily_budget_categorization` (env CLAUDE_DAILY_BUDGET_CATEGORIZATION default null → the budget helper treats null/absent as PHP_INT_MAX = uncapped, so existing categorization throughput is unchanged unless a cap is explicitly configured). Leave the existing `model` key untouched (global fallback). Do NOT modify .env/.env.example (CLAUDE.md rule 12).

    NarrationService: resolve `$this->model` from `config('services.anthropic.model_narration', config('services.anthropic.model'))`. Add a private budget/counter helper that, per purpose, reads a Cache day-key `claude_calls_{purpose}_{date}`, compares to `config("services.anthropic.daily_budget_{purpose}")` (default PHP_INT_MAX), logs+returns false at cap, else Cache::increment and returns true. Add a template-first early-return branch at the top of narrateFinding(): when the finding's finding_type has a matching template in a new additive `finding_narration_templates` key in config/optimization-report.php, build the narration string deterministically from the finding_type + the engine-computed figure (existing non-encrypted estimated_value figure) via token substitution and return WITHOUT calling callClaude(). Only genuinely bespoke finding_types fall through to the (budget-gated, Haiku) Claude path. Keep SYSTEM_PROMPT and the Http mocking surface identical — only the model string and the guard change.

    OptimizationReportNarratorService: resolve its model from `model_narration` with the same fallback; wrap its Claude call site with the same budget guard for purpose `narration`.

    InterviewOrchestratorService::wordQuestion(): resolve its model from `config('services.anthropic.model_wording', config('services.anthropic.model'))` and wrap the call with the budget guard for purpose `wording`. Change ONLY the model string + guard — no behavior change to non-template wording.

    TransactionCategorizerService (D17.2 — the single biggest recurring-cost lever, owner-LOCKED): resolve `$this->model` (constructor line ~31) from `config('services.anthropic.model_categorization', config('services.anthropic.model'))` so categorization runs on Haiku with the global model as fallback. Wrap ONLY the existing outbound Http::post call site (~L548) with the same per-purpose counter/guard for purpose `categorization`: ALWAYS `Cache::increment` the `claude_calls_categorization_{date}` day-counter (for the admin surface), and skip gracefully at cap ONLY when `daily_budget_categorization` is configured (default uncapped = PHP_INT_MAX, so batch throughput is unchanged). Touch ONLY the model string + the counter/guard wrapper — do NOT alter the CONFIDENCE_* constants, the confidence-routing branch, the batch/rate-limit loop, the prompt, or the response parsing. The existing confidence-threshold routing (≥0.85 auto / 0.60–0.84 review / question) is the safety net that makes Haiku categorization safe (D17.2) and MUST remain proven by TransactionCategorizerServiceTest.

    GenerateOptimizationReport::handle() and NarrateOptimizationFindings::handle(): add the 28-day activity gate at the top (load the User, if last_active_at older than active_threshold_days log + return before any AI dispatch). Reuse the console.php precedent exactly. The gate applies to background/scheduled paths only, never on-demand endpoints.

    Add the `finding_narration_templates` key to config/optimization-report.php (additive; token strings keyed by finding_type for the standard narrations, no dollar literals — figures come from the finding row at render time).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=TemplateFirstNarration && php artisan test --compact --filter=ClaudeBudget && php artisan test --compact --filter=TransactionCategorizerService</automated>
  </verify>
  <done>narrateFinding returns template output with no HTTP for template finding_types; model resolves to model_narration then falls back to global; budget helper skips at cap with no HTTP; wordQuestion resolves model_wording; both background handlers return early for inactive users. Categorization resolves model_categorization (Haiku) with global fallback and increments the `categorization` day-counter; the existing TransactionCategorizerServiceTest confidence-threshold routing (≥0.85 auto / review / question) passes UNCHANGED (D17.2 safety net proven).</done>
</task>

<task type="auto">
  <name>Task 3: Admin AI-usage endpoint + D17/SCN-03 zero-call test harness</name>
  <files>app/Http/Controllers/Admin/AiUsageController.php, routes/api.php, tests/Unit/ClaudeBudgetTest.php, tests/Unit/TemplateFirstNarrationTest.php, tests/Unit/BannedPhraseTemplatesTest.php, tests/Feature/ActivityGateTest.php</files>
  <read_first>
    - routes/api.php admin group (`Route::prefix('admin')->middleware('admin')`, ~line 441) — add the new route INSIDE this existing group, additively
    - app/Http/Controllers/Admin/ConsentAdminController.php (analog admin controller + response shape)
    - 14-RESEARCH.md "Admin surface: GET /api/admin/ai-usage" + call-counter pattern; test map rows for D17 (ActivityGate, ClaudeBudget, TemplateFirst)
    - tests/Pest.php helpers (createAuthenticatedUser). Note: `Http::fake()->assertNothingSent()` is the standard Laravel HTTP-fake API — use it directly; there is NO in-repo analog test to copy (the API is Laravel-native).
  </read_first>
  <action>
    Create AiUsageController@index returning, for each purpose (narration, wording, extraction, categorization), the Cache counter values `claude_calls_{purpose}_{date}` for the current day and the prior 6 days (7-day window). Register GET /ai-usage inside the existing admin prefix group (auth + admin middleware already applied by the group).

    Write ClaudeBudgetTest: seed the Cache day-counter at the configured cap for a purpose, assert the next call site skips (Http::fake + assertNothingSent) and returns null/false; assert a below-cap call increments the counter.

    Write TemplateFirstNarrationTest: for a finding_type present in finding_narration_templates, assert narrateFinding produces deterministic text with Http::fake()->assertNothingSent(); assert a bespoke finding_type without a template DOES attempt the Http call (Http::fake asserts a sent request) using the Haiku model string.

    Write ActivityGateTest: a user with last_active_at older than the threshold triggers GenerateOptimizationReport::handle() and NarrateOptimizationFindings::handle() with zero AI HTTP (assertNothingSent); a recently-active user is NOT gated. Also assert GET /api/admin/ai-usage returns the 7-day counter structure for an admin and 403 for a non-admin.

    Write BannedPhraseTemplatesTest (closes the deterministic-copy liability gap, SAFE-01): iterate every string leaf of the Phase-14 deterministic-copy config arrays that are present — `optimization-objectives.question_templates`, `optimizer-scenarios.tradeoff_templates`, `optimizer-scenarios.checklist_templates`, `optimization-report.finding_narration_templates`, and any monitor/DOC-05 template-copy config key — and assert none contains any term from the project banned-phrase list (the assertive/guaranteed-savings terms enumerated in DESIGN-ELEVATION-SPEC / the taste-skill banned discipline). Keep the banned terms in the test's own data provider, NOT inlined into any template file; match case-insensitively. Gracefully skip config arrays not yet present so the test is green at this wave and auto-covers later-added copy (14-09 monitor/DOC-05) once it lands.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ActivityGate && php artisan test --compact --filter=BannedPhraseTemplates && vendor/bin/pint --dirty</automated>
  </verify>
  <done>All four test files green (incl. BannedPhraseTemplates); admin endpoint returns per-purpose 7-day counters and enforces admin middleware; every present Phase-14 config template string is banned-phrase clean; pint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Claude API | User/document-derived text crosses into an LLM payload |
| scheduled job→Claude API | Background jobs can silently generate cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01-01 | Denial of Service (cost) | NarrationService / narrator / wording | high | mitigate | Per-purpose daily budget caps skip gracefully at limit; template-first removes the call entirely for standard outputs (D17) |
| T-14-01-02 | Denial of Service (cost) | GenerateOptimizationReport / NarrateOptimizationFindings | high | mitigate | 28-day activity gate blocks AI dispatch for inactive users (D17) |
| T-14-01-03 | Information Disclosure | finding narration payload | medium | mitigate | Template-first path emits no dollar figures into Claude; sanctioned Claude paths keep the shipped no-cents payload discipline (SAFE-03) |
| T-14-01-04 | Elevation of Privilege | GET /admin/ai-usage | medium | mitigate | Route nested in existing `admin` middleware group; test asserts 403 for non-admin |
| T-14-01-SC | Tampering | npm/pip/composer installs | low | accept | Phase 14 installs zero new packages (14-RESEARCH Package Legitimacy Audit); no install step |
</threat_model>

<verification>
- `php artisan test --compact --filter=TemplateFirstNarration` — template-first zero-call proof
- `php artisan test --compact --filter=ClaudeBudget` — budget cap graceful skip
- `php artisan test --compact --filter=ActivityGate` — inactive user → zero AI dispatch + admin endpoint auth
- `php artisan test --compact --filter=TransactionCategorizerService` — categorization confidence-routing safety net unchanged after the Haiku swap (D17.2)
- `php artisan test --compact --filter=BannedPhraseTemplates` — every present Phase-14 config template string is banned-phrase clean
- `php artisan test --compact` — full suite (zero NEW failures; DashboardFinancialBlocksTest pre-existing failure excluded)
- `vendor/bin/pint --dirty`
</verification>

<success_criteria>
The three config files exist and load; every sanctioned Claude call site resolves a per-purpose model, honors a daily budget cap, and template-first paths make standard gap questions and finding narrations zero-Claude; the two background AI handlers skip inactive users; the admin AI-usage endpoint reports per-purpose daily counters. All additive; no existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-01-SUMMARY.md` when done.
</output>
