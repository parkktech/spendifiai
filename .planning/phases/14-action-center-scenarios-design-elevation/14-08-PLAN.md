---
phase: 14-action-center-scenarios-design-elevation
plan: 08
type: execute
wave: 4
depends_on: ["14-05", "14-06", "14-03"]
files_modified:
  - database/migrations/2026_07_04_000001_create_optimization_checklist_items_table.php
  - app/Models/OptimizationChecklistItem.php
  - app/Policies/OptimizationChecklistItemPolicy.php
  - app/Services/ScenarioChecklistService.php
  - app/Http/Controllers/Api/ScenarioController.php
  - app/Http/Controllers/Api/OptimizationChecklistController.php
  - app/Http/Requests/ComputeScenarioRequest.php
  - app/Http/Requests/ChooseScenarioRequest.php
  - app/Services/OptimizationReportGeneratorService.php
  - app/Services/OptimizationReportNarratorService.php
  - config/optimization-report.php
  - routes/api.php
  - tests/Feature/Scenarios/ScenarioControllerTest.php
  - tests/Feature/Scenarios/ScenarioChooseTest.php
  - tests/Feature/Scenarios/OptimizationChecklistTest.php
  - tests/Feature/Scenarios/NoClaudeScenarioTest.php
autonomous: true
requirements: [SCN-06, SCN-07, SCN-08]
must_haves:
  truths:
    - "GET /optimizer/scenarios/{year} emits three named options (take_home/retirement/balanced) when knobs diverge and a single merged plan when they agree, with knob_diffs + trade-off lines + fact-set citation; only ready objectives produce option cards (SCN-06, M5)"
    - "POST choose recomputes server-side from the server's own solver output (never trusts client knobs), snapshots the ScenarioFactSet, writes scenario.chosen_option + scenario.chosen_knobs (encrypted), materializes the checklist, and marks the report stale; re-choosing supersedes and re-materializes (SCN-07, Pitfall 4)"
    - "Materialized checklist items are fact-gated: confirmed-fact steps render the directive, unconfirmed-fact steps render the confirm-ask; every step carries an engine-computed benefit line; the header aggregates total unlocked benefit (SCN-08, D9.2/D9.7)"
    - "Checking a fact-writing step done writes reality facts (employer.contribution_pct supersession; retirement.elected_roth_share_pct) via UserTaxFact::recordFact(source_type='user_edit')"
    - "Zero Claude/HTTP in objectives/scenarios/checklist endpoints (assertNothingSent); the report chosen_plan narrator payload carries no cents fields (SAFE-03)"
  artifacts:
    - "create_optimization_checklist_items migration + model + policy; ScenarioChecklistService; ScenarioController + OptimizationChecklistController + ComputeScenarioRequest + ChooseScenarioRequest + routes; report chosen_plan section + narrateScenarioComparison"
  key_links:
    - "choose fires the existing MarkOptimizationReportStale pattern inline (no new listener); benefit_line_params carries engine cents on a user-scoped authed row (OptimizationFinding.estimated_value_cents precedent)"
    - "narrateScenarioComparison is a new method on the EXISTING narrator (keeps the sanctioned-call-sites invariant); payload = knob names + objective ids + guard flags only"
---

<objective>
Deliver the scenario comparison + choice-to-checklist flow (SCN-06/07/08) and the report integration. Build the durable checklist store (additive `optimization_checklist_items` migration + model + policy), the `ScenarioChecklistService` that materializes fact-gated steps with engine-computed benefit lines and writes reality facts on done-toggle, the `ScenarioController` (show = agreement/options/knob_diffs + 60s cache; compute = live mix panel; choose = server recompute + fact-set snapshot + two fact writes + materialization + report-stale) and `OptimizationChecklistController` (show/toggle), and the report `chosen_plan` section + `narrateScenarioComparison()` narrator method. Zero Claude in every scenario/checklist endpoint; no cents in the narrator payload.

Purpose: The user compares A/B/Balanced (or a merged plan), picks one, and gets a persistent D9 action checklist whose benefit lines all trace to the engine.
Output: The checklist store + service, two controllers + form requests + routes, the report section + narrator method, and the SCN-06/07/08 + no-Claude tests (§T.14–17).
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-PATTERNS.md
@.planning/reference/SCENARIOS-SPEC.md
@app/Http/Controllers/Api/OptimizationReportController.php
@app/Listeners/MarkOptimizationReportStale.php
@app/Services/OptimizationReportGeneratorService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Checklist store + ScenarioChecklistService (materialization, fact-gating, done-toggle reality facts)</name>
  <files>database/migrations/2026_07_04_000001_create_optimization_checklist_items_table.php, app/Models/OptimizationChecklistItem.php, app/Policies/OptimizationChecklistItemPolicy.php, app/Services/ScenarioChecklistService.php, tests/Feature/Scenarios/OptimizationChecklistTest.php</files>
  <behavior>
    - materialize(user, year, chosenOption, chosenKnobs, factSet) creates one item group per knob whose chosen value differs from current, from config checklist_templates, with benefit_line_params (engine cents from attributeBenefits) and a header aggregate
    - a step whose anchoring facts are all confirmed → kind='directive'; any unconfirmed anchor → kind='confirm_ask' (D9.2)
    - toggling a fact-writing step done writes reality facts: K3 supersedes employer.contribution_pct; K2 writes retirement.elected_roth_share_pct — via UserTaxFact::recordFact(source_type='user_edit')
    - re-materialization scoped-deletes prior rows WHERE user_id+tax_year+source_type='scenario_choice' only (never user data)
  </behavior>
  <read_first>
    - SCENARIOS-SPEC §D.5 (checklist templates + benefit lines + store schema + completion writes reality facts), §D.6 (re-choose scoped delete), §A.8.2/M9 (fact_set_id linkage)
    - 14-PATTERNS.md optimization_checklist_items migration (cascadeOnDelete GDPR; benefit_line_params JSON cents on authed row — OptimizationFinding.estimated_value_cents precedent)
    - app/Models/UserTaxFact.php recordFact (source_type='user_edit'); app/Models/OptimizationFinding.php (estimated_value_cents JSON-on-authed-row precedent)
    - CLAUDE.md: forward-only additive migration; `php artisan migrate` only
  </read_first>
  <action>
    Write failing OptimizationChecklistTest first (materialization from templates; directive vs confirm-ask by fact-confirmation; done-toggle reality-fact writes; re-choose re-materializes with scoped delete). Create the forward-only additive migration create_optimization_checklist_items_table per §D.5 (id, user_id FK cascade, tax_year, source_type, source_id, fact_set_id FK scenario_fact_sets [M9], knob, step_key, kind, benefit_line_params json, position, done_at nullable, timestamps, index [user_id, tax_year]). Create the model (scopeForUser, benefit_line_params cast array) and OptimizationChecklistItemPolicy (user_id ownership). Create ScenarioChecklistService::materialize() and the done-toggle reality-fact writer per §D.5. `php artisan migrate` (forward-only).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=OptimizationChecklist</automated>
  </verify>
  <done>Migration additive/applied; items materialize fact-gated with engine benefit params + header aggregate; done-toggle writes reality facts; re-choose scoped-deletes and re-materializes; policy enforces ownership.</done>
</task>

<task type="auto">
  <name>Task 2: ScenarioController + OptimizationChecklistController + form requests + routes + choose flow</name>
  <files>app/Http/Controllers/Api/ScenarioController.php, app/Http/Controllers/Api/OptimizationChecklistController.php, app/Http/Requests/ComputeScenarioRequest.php, app/Http/Requests/ChooseScenarioRequest.php, routes/api.php, tests/Feature/Scenarios/ScenarioControllerTest.php, tests/Feature/Scenarios/ScenarioChooseTest.php, tests/Feature/Scenarios/NoClaudeScenarioTest.php</files>
  <read_first>
    - SCENARIOS-SPEC §E (all scenario + checklist routes, GET scenarios JSON shape, compute/choose bodies, InterviewController-style throttles), §C.2/C.3 (agreement + trade-off lines), §D.6 (choose persistence: snapshotFactSet, two fact writes, materialize, report-stale), §A.8.1/E readiness projection (M5), Pitfall 4 (choose recomputes server-side)
    - 14-PATTERNS.md ScenarioController analog (OptimizationReportController stale-while-revalidate + throttle split); DashboardCacheService pattern for 60s cache keyed on fact_set_hash
    - routes/api.php optimizer group (~L333) — add optimizer/scenarios + optimizer/checklist prefixes INSIDE the existing auth:sanctum group (NO bank.connected)
    - app/Listeners/MarkOptimizationReportStale.php (fire the existing user-action stale path inline — no new listener)
  </read_first>
  <action>
    ScenarioController::show(year): assembleBaseline from the resolver, run the three solvers, apply the §C.2 agreement rule (merged vs three options), build knob_diffs with §C.3 template trade-off lines (token substitution — zero Claude), attach the readiness projection (M5 — only ready objectives produce options), fact_set citation summary (labels + source types, never values), and the static disclaimer. Cache 60s keyed scenarios:{user}:{year}:{fact_set_hash}. All money fields integer cents with _cents suffix.

    compute(year): ComputeScenarioRequest validates numeric bounds + grid membership + non-negative cents; return computeScenarioOutcome for the mix panel (engine clamps regardless — validation is UX, clamping is the boundary).

    choose(year): ChooseScenarioRequest validates option_key enum ({take_home,tax_burden,retirement,balanced,merged,custom}; knobs required iff custom). ALWAYS recompute from the server's own solver output (never trust client knob figures — Pitfall 4); snapshotFactSet; write scenario.chosen_option + scenario.chosen_knobs (encrypted value; metadata.fact_set_id — M9) via recordFact(source_type='user_edit'); materialize the checklist; fire the MarkOptimizationReportStale user-action path inline. Return chosen + checklist.

    OptimizationChecklistController::show(year) + update(item) (PATCH done toggle → §D.5 reality-fact writes; route-model binding + policy). Register all routes additively with the §E throttles ({objective}/{year} validation → 404). Validate year ∈ {currentYear, currentYear−1} like the report controller.

    Tests: ScenarioControllerTest (agreement→merged, divergent→3 options + knob_diffs, readiness gating hides options for not-ready objectives, cross-user 403, throttles); ScenarioChooseTest (server recompute ignores hostile client knobs, writes both facts + fact-set snapshot, re-choose supersedes + old checklist rows scoped-deleted, report marked stale); NoClaudeScenarioTest (Http::fake + assertNothingSent across objectives/scenarios/checklist endpoints).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ScenarioController && php artisan test --compact --filter=ScenarioChoose && php artisan test --compact --filter=NoClaudeScenario</automated>
  </verify>
  <done>Scenarios show emits merged/3-options with knob_diffs + citations + no money leakage; compute clamps hostile input; choose recomputes server-side, writes facts + snapshot, materializes, marks stale, supersedes on re-choose; all three endpoint groups are zero-Claude; cross-user 403.</done>
</task>

<task type="auto">
  <name>Task 3: Report chosen_plan section + narrateScenarioComparison (no-dollars payload)</name>
  <files>app/Services/OptimizationReportGeneratorService.php, app/Services/OptimizationReportNarratorService.php, config/optimization-report.php, tests/Feature/Scenarios/ScenarioControllerTest.php</files>
  <read_first>
    - SCENARIOS-SPEC §D.7 (chosen_plan section — injected when scenario.chosen_option exists, inserted before documents_missing wrapper, figures from a fresh SCN-07 run), §C.3 (narrateScenarioComparison payload = knob names + objective ids + guard flags, no cents)
    - app/Services/OptimizationReportGeneratorService.php generate() (section assembly + the documents_missing wrapper index); OptimizationReportNarratorService.php narrateSection (the sanctioned Claude call site + no-dollars payload discipline)
    - config/optimization-report.php (additive chosen_plan section config)
  </read_first>
  <action>
    OptimizationReportGeneratorService::generate(): additively inject a `chosen_plan` section (title "Your Chosen Approach — Action Plan", section_type topical) when UserTaxFact currentFact scenario.chosen_option exists, inserted BEFORE the documents_missing wrapper. Contents: option label, three-metric summary from a FRESH computeScenarioOutcome run (never stored prose), and the aggregated unlocked checklist steps (D9.6). Add narrateScenarioComparison() to the EXISTING narrator (keeps the two-sanctioned-call-sites invariant): payload of knob names + objective ids + guard flag names — NO cents fields, ever (same exclusion as narrateSection; honor the D17 model_narration + budget guard from 14-01). Add the additive chosen_plan config entry to config/optimization-report.php. Extend ScenarioControllerTest (or a report test) to assert the section injects only when a choice exists and the narrator payload builder emits no cents-suffixed money fields (regression grep on the payload builder).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=Scenario && vendor/bin/pint --dirty</automated>
  </verify>
  <done>chosen_plan section injects only when a choice exists, with fresh engine figures, before documents_missing; narrateScenarioComparison exists on the existing narrator with a no-cents payload; config additive; full scenario suite + pint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→choose | Client may submit a manipulated knob vector |
| checklist item→user | Cross-user item access via route-model binding |
| narrator payload→Claude | Chosen-plan prose must not carry dollar figures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08-01 | Tampering | ScenarioController::choose knobs | high | mitigate | choose recomputes from server solver output; never trusts client knobs (Pitfall 4); ScenarioChooseTest |
| T-14-08-02 | Elevation of Privilege | cross-user checklist item | high | mitigate | OptimizationChecklistItemPolicy (user_id) + route-model binding + scopeForUser; cross-user 403 test |
| T-14-08-03 | Information Disclosure | cents in narrator payload | high | mitigate | narrateScenarioComparison payload = names/ids/flags only; no-cents regression grep (SAFE-03) |
| T-14-08-04 | Denial of Service (cost) | scenario/checklist endpoints | high | mitigate | Zero-Claude endpoints (NoClaudeScenarioTest assertNothingSent); throttles per §E |
| T-14-08-05 | Information Disclosure | scenario.chosen_knobs at rest | medium | mitigate | encrypted value column + $hidden on UserTaxFact; benefit_line_params on authed user-scoped rows only |
| T-14-08-SC | Tampering | package installs | low | accept | Zero new packages; migration additive/forward-only |
</threat_model>

<verification>
- `php artisan test --compact --filter=ScenarioController` — merged/3-options, readiness gating, 403, chosen_plan section
- `php artisan test --compact --filter=ScenarioChoose` — server recompute, fact writes, snapshot, re-choose, report-stale
- `php artisan test --compact --filter=OptimizationChecklist` — materialization, fact-gating, done-toggle reality facts
- `php artisan test --compact --filter=NoClaudeScenario` — zero HTTP in objectives/scenarios/checklist
- `php artisan test --compact` — full suite, zero NEW failures (EstimatedValueGuard green)
- `vendor/bin/pint --dirty`
</verification>

<success_criteria>
Scenarios render as merged or A/B/Balanced with engine figures + citations; choose persists a recomputed, snapshotted choice and materializes a fact-gated checklist with engine benefit lines; done-toggles write reality facts; the report gains a chosen_plan section narrated with no dollars; every scenario endpoint is zero-Claude.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-08-SUMMARY.md` when done.
</output>
