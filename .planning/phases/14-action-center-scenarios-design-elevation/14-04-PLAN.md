---
phase: 14-action-center-scenarios-design-elevation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/css/app.css
  - resources/js/Layouts/AuthenticatedLayout.tsx
autonomous: true
requirements: [ELEV-01]
must_haves:
  truths:
    - "The 41 additive elevation tokens (shadow scale, motion, display type, gradients, surfaces) resolve as CSS vars in the browser (D12 luxury-lean)"
    - "The sidebar collapse uses the spring cubic-bezier transition, not a linear width change (D12)"
    - "The active nav item shows a rounded gradient pill indicator, not border-l-2 (D12)"
    - "The top header is h-14 with backdrop-blur and a 2-layer shadow (D12)"
    - "prefers-reduced-motion disables the new animations; light and dark modes both render correctly"
    - "npm run build passes with the new tokens; brand logo, Inter, and sw-accent blue are unchanged (§6 preservation audit)"
  artifacts:
    - "resources/css/app.css second @theme block + card-surface/card-lift/btn-press utilities + stagger-children/sw-fade-up + reduced-motion override"
    - "resources/js/Layouts/AuthenticatedLayout.tsx shell polish (sidebar, header, nav pill, avatar, bell)"
  key_links:
    - "The new @theme block is added IMMEDIATELY AFTER the existing @theme closing brace (Pitfall 6), not inside it"
    - "All motion is CSS/Tailwind only — framer-motion is NOT in package.json (Pitfall 9)"
---

<objective>
Deliver DESIGN-ELEVATION-SPEC Wave 1 (D12 Luxury Lean): the additive token system and the app-shell polish. This is the lowest-risk, zero-functional-change design foundation that Waves 2–3 (plans 14-07, 14-10) build on. Add the 41 `sw-*` elevation tokens (4-level shadow scale, motion durations/easings, display type scale, gradient recipes, refined surfaces), the `card-surface`/`card-lift`/`btn-press` utilities, the `stagger-children`/`sw-fade-up` entrance animation, and the master `prefers-reduced-motion` override to `app.css`; then apply the sidebar/header/nav/avatar/bell recipes to `AuthenticatedLayout.tsx`.

Purpose: Premium depth, motion vocabulary, and typographic drama that later surfaces inherit — while preserving brand logo, Inter, sw-accent blue, all URLs/APIs, accessibility, and both color modes.
Output: The extended token block + utilities in app.css and the elevated app shell, passing the §6 preservation audit and `npm run build`.
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/reference/DESIGN-ELEVATION-SPEC.md
@resources/css/app.css
@resources/js/Layouts/AuthenticatedLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 41 elevation tokens + utilities + motion to app.css</name>
  <files>resources/css/app.css</files>
  <read_first>
    - DESIGN-ELEVATION-SPEC §2 (the full @theme extension block, verbatim), §4.2 (stagger-children + sw-fade-up keyframes)
    - resources/css/app.css lines 11–50 (existing @theme block; find its closing brace — the insertion point is IMMEDIATELY AFTER it per Pitfall 6)
  </read_first>
  <action>
    Insert the DESIGN-ELEVATION-SPEC §2 block IMMEDIATELY AFTER the existing `@theme { }` closing brace as a SECOND `@theme { }` declaration (not inside the first — Tailwind v4 merges them; nesting shadows variables per Pitfall 6). Add all 41 tokens exactly as specified: elevation/shadow scale (--shadow-sw-1..4, accent, success, inset), motion durations (--duration-sw-instant..reveal) and easings (--ease-sw-spring/out/in/smooth), display type scale (--font-size/line-height/letter-spacing/font-weight-sw-display-xl/lg/md/sm), gradient recipes (--gradient-sw-card/accent-glass/success-glass/accent-bar), refined surfaces (--color-sw-card-ring/frame/hover-deep, --color-sw-icon-accent/success/danger/warning/neutral, --font-variant-numeric-sw).

    After the @theme block add, as direct CSS rules (not inside @theme): the `.font-tabular`, `.card-surface`, `.card-lift`(+ :hover), `.btn-press`(+ :active) utilities; the `.stagger-children > *` animation with nth-child delays and the `@keyframes sw-fade-up`; and the master `@media (prefers-reduced-motion: reduce)` override (both the global animation/transition reset and the stagger-children none rule). All additive — do not alter any existing sw-* token or rule.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build passes; grep confirms --shadow-sw-1, --ease-sw-spring, --font-size-sw-display-xl, --gradient-sw-accent-bar, and .card-lift/.stagger-children exist in app.css; the new @theme block sits after the original's closing brace.</done>
</task>

<task type="auto">
  <name>Task 2: Elevate AuthenticatedLayout shell (sidebar, header, nav pill, avatar, bell) + Wave-1 preservation audit</name>
  <files>resources/js/Layouts/AuthenticatedLayout.tsx</files>
  <read_first>
    - DESIGN-ELEVATION-SPEC §3.1 (sidebar + NavItem active/hover), §3.2 (top header, avatar, bell), §5 Wave 1 audit checklist, §6 preservation audit template
    - resources/js/Layouts/AuthenticatedLayout.tsx (current sidebar aside className, header, NavItem active/hover classes, user avatar button, notification bell button) — preserve all routes, nav labels, badge counts, dark-mode toggle, email banner logic
  </read_first>
  <action>
    Apply the §3.1/§3.2 class recipes exactly: sidebar `<aside>` → spring cubic-bezier width transition + `border-sw-card-frame` + soft right-edge shadow (replace duration-300 linear + hard border); NavItem active state → gradient-fade background + `::before` rounded pill indicator with the logo gradient (replace `border-l-2`); NavItem hover → `bg-sw-surface/80` with the smooth easing; top header → `h-14` + `bg-sw-card/90 backdrop-blur-sm` + custom 2-layer shadow (replace h-16 + shadow-sm); user avatar → rounded-xl accent→violet gradient with ring/ring-offset + active:scale-95; notification bell → surface tint + hover shadow-sw-1 + active:scale-95.

    Scope discipline (D12 / §3 intro): class replacements only — do NOT change layout structure, routes, nav labels, badge logic, the brand logo SVG, dark-mode toggle, or the email-connection banner. Verify dark-mode variants still resolve (sidebar/header must not turn white in dark mode).

    Run the §6 PRESERVATION AUDIT (URLs, nav labels, form field names, sw-accent primary, Inter, brand logo, functionality, dark mode, WCAG AA, reduced-motion) and the §5 Wave-1 audit checklist; record both PASS/FAIL in the SUMMARY. A single FAIL blocks the commit.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Build + typecheck clean; sidebar collapse is spring (not linear); active nav shows pill not border-l-2; header is h-14 with backdrop-blur; §6 preservation audit and §5 Wave-1 checklist recorded ALL PASS in SUMMARY; dark mode + reduced-motion verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static CSS/TSX → rendered UI | Purely presentational; no data or auth surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04-01 | Tampering | app.css @theme merge | low | mitigate | Additive second @theme block after the original (Pitfall 6); npm build gate proves no variable shadowing |
| T-14-04-02 | Denial of Service (a11y) | new animations | low | mitigate | prefers-reduced-motion master override; §6 audit gate |
| T-14-04-SC | Tampering | npm installs | low | accept | Zero new packages; CSS-only motion, framer-motion absent (Pitfall 9) |
</threat_model>

<verification>
- `npm run build` — clean with 41 new tokens
- `npx tsc --noEmit` — AuthenticatedLayout typechecks
- §6 preservation audit (all PASS) + §5 Wave-1 audit checklist recorded in SUMMARY
- Manual: sidebar spring, nav pill, header blur, dark mode, reduced-motion (taste-skill v2 blocking audit)
</verification>

<success_criteria>
All 41 tokens + utilities + motion vocabulary live in app.css; the app shell is elevated to the D12 spec with zero functional change; brand/Inter/sw-accent preserved; build clean; §6 audit passes.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-04-SUMMARY.md` when done.
</output>
