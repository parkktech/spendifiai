---
phase: 14-action-center-scenarios-design-elevation
plan: 06
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - app/Services/ScenarioSolverService.php
  - tests/Unit/ScenarioSolverTest.php
  - tests/Feature/Scenarios/ScenarioAgreementTest.php
autonomous: true
requirements: [SCN-05]
must_haves:
  truths:
    - "ScenarioSolverService runs the six-knob solver over take_home, tax_burden, and retirement, attributing per-paycheck and per-year benefit figures ENTIRELY from TaxRulesEngineService (SCN-05, zero Claude, zero new dollar math outside the engine)"
    - "assembleBaseline is built FROM ScenarioFactResolverService::resolveAll — never ad-hoc fact reads (M15); annualization lives here, not in solve() (no now()-dependent branching in solvers)"
    - "Solvers respect the min_take_home_ratio floor (0.90; 0.97 when cash-constrained) and never leave employer match on the table (Pitfall 7, Pitfall — dominant free-return rule)"
    - "diffKnobs flags a dimension as diverging only when it exceeds its config epsilon; Balanced is always COMPUTED (midpoint synthesis re-run through the engine), never averaged outcomes"
    - "Solver is deterministic: same baseline twice → identical knob vectors"
  artifacts:
    - "app/Services/ScenarioSolverService.php (assembleBaseline, solve×3, Balanced synthesis, diffKnobs, attributeBenefits)"
    - "tests/Unit/ScenarioSolverTest.php + tests/Feature/Scenarios/ScenarioAgreementTest.php"
  key_links:
    - "Every candidate is evaluated through TaxRulesEngineService::computeScenarioOutcome (clamps/guards apply inside the engine)"
    - "attributeBenefits does ≤6 single-knob engine calls; interaction remainder goes only to the header aggregate (D9.7), never an individual step"
---

<objective>
Build `ScenarioSolverService` (SCN-05): the deterministic six-knob solver over the three objectives, plus baseline assembly, Balanced synthesis, knob divergence detection, and per-knob benefit attribution — all figures sourced from `TaxRulesEngineService`. `assembleBaseline()` is built from the resolver's `resolveAll()` (M15) so provenance stays consistent; each objective solver (TAKE_HOME, TAX_BURDEN, RETIREMENT) fills knobs greedily per config priority while honoring the take-home floor and never leaving employer match; BALANCED is a computed midpoint synthesis re-run through the engine; `diffKnobs` uses config epsilons; `attributeBenefits` feeds the D9 benefit lines with exact per-knob figures.

Purpose: The engine-backed vectors and benefit attributions that the scenario API (14-08) serializes and the checklist materializes.
Output: The solver service and its determinism/dominance/agreement/no-literal tests (§T.10–13).
</objective>

<execution_context>
@/home/spendifi/public_html/.claude/gsd-core/workflows/execute-plan.md
@/home/spendifi/public_html/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-CONTEXT.md
@.planning/phases/14-action-center-scenarios-design-elevation/14-VALIDATION.md
@.planning/reference/SCENARIOS-SPEC.md
@app/Services/TaxRulesEngineService.php
@app/Services/ScenarioFactResolverService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assembleBaseline (from resolver) + three objective solvers</name>
  <files>app/Services/ScenarioSolverService.php, tests/Unit/ScenarioSolverTest.php</files>
  <behavior>
    - assembleBaseline(user, year) builds the §B.2 baseline array from resolveAll() output: annual_gross_cents (C2 chain), pay_periods_per_year (pay.frequency map), confirmed filing_status, w4_on_file, family, age, target_retirement_age (fact or config default), current run-rates from annualized YTD, employer match trio, hsa_coverage_type, marketplace/cash-constrained flags, monthly_surplus_cents (dashboard waterfall), annual_withholding, fact_set_hash — annualization happens here, not in solve()
    - solve(baseline, 'take_home'): K3 = max(current_pct, match_threshold_pct); K2 roth_share 0; K4/K5 unchanged; K6 = income_objective_transfer_share × per-paycheck take-home gain
    - solve(baseline, 'tax_burden'): greedy fill in tax_objective_priority order (match_capture→hsa→trad_401k→trad_ira) honoring the floor; roth shares 0; K6 0
    - solve(baseline, 'retirement'): raise deferral to floor-allowed max; roth_share from rothVsTraditionalBand; fill HSA then shared IRA; K6 funds the K5 amounts
    - every candidate evaluated via computeScenarioOutcome; take-home floor guard takeHome(candidate) ≥ min_take_home_ratio × takeHome(current); never below full match
  </behavior>
  <read_first>
    - SCENARIOS-SPEC §B.2 (assembleBaseline shape + M15), §B.7 (the three objective solvers + floor guard + determinism), §B.1 (knob set), §B.6 (outcome shape)
    - app/Services/TaxRulesEngineService.php computeScenarioOutcome/marginalRate/rothVsTraditionalBand/remaining*RoomCents (call, do not modify)
    - app/Services/ScenarioFactResolverService.php resolveAll (14-03); config/optimizer-scenarios.php assumptions/grids/tax_objective_priority
    - 14-VALIDATION item 4 (solver determinism, benefit cents from engine)
  </read_first>
  <action>
    Write failing ScenarioSolverTest first: determinism (same baseline twice → identical vectors), objective dominance sanity (take_home option's take-home ≥ others; retirement option's retirement-delta ≥ others; tax_burden option's federal-tax ≤ others within epsilon), and the take-home floor holds (never below 90% baseline; never below full-match). Then implement assembleBaseline (from resolveAll — M15) and the three solvers per §B.7 as deterministic greedy heuristics: fixed grids, fixed iteration order, integer math, no now()-dependent branching in solve() (annualization lives in assembleBaseline). All dollar figures come from computeScenarioOutcome — the solver invents no money math.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ScenarioSolver</automated>
  </verify>
  <done>Determinism, dominance, and floor tests green; baseline assembled from resolveAll; solvers deterministic with no now() in solve().</done>
</task>

<task type="auto">
  <name>Task 2: Balanced synthesis + diffKnobs + attributeBenefits</name>
  <files>app/Services/ScenarioSolverService.php, tests/Feature/Scenarios/ScenarioAgreementTest.php</files>
  <read_first>
    - SCENARIOS-SPEC §B.7 BALANCED (per-knob midpoints snapped to grid, re-run through engine), §B.8 (attributeBenefits ≤6 single-knob calls + interaction remainder to header only), §C.1 (diffKnobs epsilons), §C.2 (agree vs conflict; Balanced-seeded-from-TB rule; lowest-tax badge)
    - config/optimizer-scenarios.php divergence epsilons + grids
  </read_first>
  <action>
    Implement Balanced as a COMPUTED synthesis: per-knob midpoints between the TAKE_HOME and RETIREMENT vectors, snapped to each knob's grid, then re-run through computeScenarioOutcome (re-clamped, re-guarded) — never average outcomes. Implement the §C.2 special case: when tax_burden diverges from BOTH poles, seed Balanced from TB and label it "Balanced — also lowest 2026 tax." Implement diffKnobs(a,b) using the §C.1 config epsilons (w4.* never diverges by construction). Implement attributeBenefits(baseline, chosenKnobs): for each knob dimension, computeScenarioOutcome with only that dimension changed and record its three deltas; the interaction remainder (chosen-total minus sum of singles) is attributed to the header aggregate only (D9.7), never an individual step. Long-horizon figures stay range-framed (illustration); short-horizon are exact.

    ScenarioAgreementTest: converged baseline (diffKnobs empty) → the agreement path yields one merged plan; a divergent baseline → exactly three distinct vectors with non-empty knob_diffs; the Balanced-seeded-from-TB rule fires when applicable; the minimum-federal-tax option is identifiable for the lowest-tax badge.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ScenarioAgreement && vendor/bin/pint --dirty</automated>
  </verify>
  <done>Balanced is computed (not averaged) and snaps to grid; diffKnobs respects epsilons; agreement→merged and conflict→3 options proven; attributeBenefits sources all figures from the engine with the interaction remainder only in the header; pint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline facts → solver | Resolved (some unconfirmed) facts drive money math |
| solver output → benefit lines | Attributed figures become user-facing benefit claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06-01 | Information Disclosure | dollar math outside the engine | high | mitigate | Solver invents no money math; every figure from computeScenarioOutcome; EstimatedValueGuard stays green |
| T-14-06-02 | Harm (financial) | zero take-home scenario | high | mitigate | min_take_home_ratio floor guard on every candidate (Pitfall 7) |
| T-14-06-03 | Tampering | non-determinism from now() | medium | mitigate | Annualization confined to assembleBaseline; solve() has no now()-branching; determinism test |
| T-14-06-SC | Tampering | package installs | low | accept | Zero new packages |
</threat_model>

<verification>
- `php artisan test --compact --filter=ScenarioSolver` — determinism, dominance, floor
- `php artisan test --compact --filter=ScenarioAgreement` — merged vs 3-option, Balanced synthesis
- `php artisan test --compact` — full suite, zero NEW failures
- `vendor/bin/pint --dirty`
</verification>

<success_criteria>
The solver produces deterministic, engine-backed knob vectors for all three objectives plus a computed Balanced, honors the take-home floor and full-match rule, detects divergence via config epsilons, and attributes benefits entirely from the engine.
</success_criteria>

<output>
Create `.planning/phases/14-action-center-scenarios-design-elevation/14-06-SUMMARY.md` when done.
</output>
